# 팀킥 아키텍처

React·TypeScript·Vinext 화면을 Cloudflare Worker에서 제공한다. 기존 Sites 스타터의 Vite 플러그인과 Worker fetch 진입점을 유지한다. Shadcn/Radix의 사이드바·탭·모달·선택·확인 창·표를 사용한다.

## 모듈

- app/teamkick.tsx: 화면 선택·계정·달력·요약.
- app/screens.tsx: 팀·매칭·입력 흐름.
- lib/model.ts: 역할·상태 전이·출석·통계.
- lib/store.ts: D1 로딩과 원자적 변경.
- /api/app: 사용자에게 허용된 상태 조회, 검증된 명령 실행.

## 인증과 권한

자체 회원가입으로 신원을 확인한다. 이메일과 비밀번호로 가입·로그인하며 비밀번호는 PBKDF2-HMAC-SHA256(반복 600,000회, 계정마다 다른 난수 소금)으로만 저장한다. 원문은 저장하지 않고 비교는 시간이 일정한 방식으로 한다. 로그인하면 난수 세션 토큰을 HttpOnly·Secure·SameSite=Lax 쿠키로 내려주고, 서버에는 토큰의 SHA-256만 저장해 저장소가 유출되어도 세션을 재사용할 수 없게 한다. 세션은 30일 후 만료되고 로그아웃 시 삭제한다. 로그인 실패가 10번 연속되면 15분 잠근다. 로그인 실패 응답은 가입 여부를 구분해 알려주지 않는다.

계정과 세션은 문서 저장소(entities)가 아니라 별도 accounts·sessions 테이블에 둔다. 상태 로딩은 모든 entities 행을 메모리로 읽으므로, 비밀번호 해시와 세션 토큰을 그 경로에서 분리한다.

클라이언트의 사용자 ID·역할은 신원으로 사용하지 않는다. 매 명령마다 팀 소속·역할·승인 상태를 검증한다.

운영자는 일회용 코드의 SHA-256을 검증한 뒤 사용자 ID를 저장하여 설정한다. 첫 로그인 사용자를 자동 운영자로 삼지 않는다. 원문 코드는 소스·배포 자산에 포함하지 않는다. 한 번 설정하면 바꿀 수 없지만, 저장된 운영자 ID에 해당하는 계정이 존재하지 않을 때에 한해 코드를 다시 제시하면 재설정할 수 있다. 계정 체계를 바꾼 뒤 아무도 운영자가 될 수 없는 상태를 막기 위한 경로이며, 살아 있는 운영자 계정은 코드를 알아도 가져갈 수 없다.

공통 경기 정보와 팀별 비공개 출석·개인 기록·안내를 분리한다. 응답은 선택한 팀으로 제한하며 상대팀의 출석·초대 토큰·공지를 공개하지 않는다. 운영자에게 다른 팀의 선수 기록 접근 권한이 자동 부여되지 않는다.

POST는 요청 출처, 크기, 입력 범위, 역할을 검사한다. 성공 작업 기준 사용자당 분당 40건을 제한하고 요청 ID를 7일간 보관해 재전송 중복을 막는다.

## 저장과 동시성

D1 스키마는 Drizzle migration으로 관리하며 실행 중 DDL을 수행하지 않는다.

- entities: 팀·팀원·경기·팀별 기록·신청·공지·알림·초대·감사 이력 등의 개별 JSON 문서 행.
- state_revision: 상태의 수정 버전.
- write_guards: 현재 버전과 기대 버전이 같아야 하는 CHECK 제약.

상태를 읽고 규칙을 적용한 후 변경된 문서만 준비된 SQL로 저장한다. 버전 검증·변경·버전 증가를 하나의 D1 batch에서 수행한다. 경합 시 전체 롤백 후 최신 상태로 최대 4회 검증한다. 동시 매칭 수락도 한 건만 확정된다.

현재는 작은 팀 파일럿을 위해 요청마다 전체 문서 상태를 읽는다. 응답은 권한으로 제한하지만, 팀 수 증가 시 읽기 비용·감사/알림 크기·전역 경합이 늘 수 있다. 확대 전 팀별 SELECT, 이력 보관 정책, 명시적 관계형 컬럼·제약, 경기 단위 경합으로 전환해야 한다.

## 용병 모집

경기별·팀별 side 문서가 모집 정원(guestNeeded)과 상태(guestStatus: none/open/closed)를 가지고, 신청은 guests 컬렉션의 개별 문서다. 승인 인원이 정원에 도달하면 서버가 상태를 closed로 바꾸고 남은 대기자를 모집 종료로 처리한다. 승인 대기자는 완료 인원에 포함하지 않는다.

정원 초과는 두 가지로 막는다. 명령 안에서 모집 상태와 현재 승인 인원을 다시 확인하고, 동시 승인은 매칭 수락과 같은 state_revision 검증으로 한 건만 저장된다. 마지막 한 자리를 동시에 승인해도 정원을 넘지 않는다.

용병은 팀원이 되지 않는다. 승인해도 members에 추가하지 않으므로 출석 대상과 선수 통계에 들어가지 않고 팀원 기록과 겹치지 않는다. 같은 팀의 승인·대기 팀원은 용병으로 신청할 수 없다. 용병에게는 공개 경기 정보와 자신의 신청 상태만 제공하며 선수 명단·투표·출석·공지·팀 안내는 포함하지 않는다. 모집 관리와 승인·거절은 주장과 운영진 권한이다. 경기를 취소하면 모집도 함께 종료하고, 승인된 용병을 취소하면 경기 시작 전까지 자리를 다시 연다.

## 통계와 설치

투표 이력으로 출석 초안을 계산하고 실제 출석을 확정해 저장한다. 마감은 서버 시간으로 적용한다. 경기 완료와 출석 확정은 주장이 직접 수행한다. 취소 경기·미확정 기록을 분리하며 정정은 경기별 대체 방식이다.

PWA manifest·아이콘·오프라인 안내를 제공한다. 인증된 페이지/API는 서비스 워커에 캐시하지 않고 오프라인 쓰기도 하지 않는다. 설치 지원은 브라우저에 따라 다르다. 푸시·예약 작업은 연결되어 있지 않다.

샘플은 UI 체험용 메모리 상태다. 새로고침하면 초기화되며 실제 팀 저장에 영향을 주지 않는다.

## 검증 범위

tests/core.test.mjs는 실제 도메인·저장소·API 소스를 실행한다. 테스트 인증과 Node SQLite D1 어댑터로 권한·경합 롤백·마감·집계·정보 분리·재가입·중복 요청을 검증한다. 배포된 로그인과 실기기 UI 검증을 대체하지 않는다.

일정 읽기·상세 열기 WebMCP 도구를 지원 브라우저에서만 등록한다. 기능 감지·정리·실패 처리를 포함한다. 허용된 지원 컨텍스트가 없어 실행 검증은 수행하지 않았다. 브라우저·시각·E2E 검증은 사용자 요청이 없으므로 이번 작업에서 수행하지 않는다.
