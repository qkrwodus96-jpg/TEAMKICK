# ARCHITECTURE.md — TEAMKICK 구조

최종 갱신: 2026-09-14

이 문서는 **현재 구현**과 **목표 구조**를 구분해 기록한다.
요구사항은 `PROJECT_CONTEXT.md`를 본다.

---

## 1. 현재 구현 — 확인 결과

**확인된 기술 스택: 없음. 애플리케이션 소스가 저장소에 존재하지 않는다.**

2026-09-14 `/home/user/TEAMKICK`에서 직접 확인:

| 확인 대상 | 결과 |
|---|---|
| 추적 파일 | `README.md` (10바이트) 1개뿐 |
| 커밋 | 초기 커밋 `2207d24` 1개 |
| 미커밋 변경 | 없음 |
| `package.json` / 잠금 파일 | 없음 |
| 앱 진입점 | 없음 |
| 서버 · DB · 인증 · 업로드 구성 | 없음 |
| `.openai/hosting.json` | 없음 |
| `AGENTS.md` / 기존 요구사항 문서 | 없음 |

기존 ChatGPT Sites 저장 버전 v1에 73개 파일 보관본이 있다고 보고되었으나
(`PROJECT_CONTEXT.md` 0절 참고), **그 소스는 이 환경에서 접근할 수 없다.**
프레임워크, 언어, DB, 인증 방식, 배포 방식은 **모두 미확인**이다.

아래 2절 이하는 요구사항에서 도출한 **목표 구조와 제약**이며,
원본 소스를 확인한 뒤 기존 스택에 맞춰 **최소 변경으로** 조정한다.

---

## 2. 원본 확보 시 즉시 확인할 항목 (T00 체크리스트)

순서대로 확인하고 결과를 이 문서 1절에 덮어쓴다.

1. **언어·프레임워크**: 진입점, 라우팅 방식, 렌더링 방식(SSR/SPA/정적)
2. **패키지·잠금 파일**: `package.json`, lock 파일 종류 → 패키지 매니저 확정
3. **실행 방법**: 설치 / 개발 서버 / 빌드 / 테스트 / 린트 명령 (실제 실행)
4. **인증**: 세션 방식(쿠키/JWT), 비밀번호 해시, 소셜 로그인 여부,
   서버에서 역할을 어떻게 읽는지
5. **DB**: 종류, 스키마·마이그레이션 위치, 접속 설정 방식
6. **권한 검증 위치**: 클라이언트 전용인지, 서버·DB 정책까지 있는지
7. **파일 저장**: 업로드 대상(로컬 디스크 / 오브젝트 스토리지), 접근 제어 방식
8. **푸시**: 구현 여부, 사용 중인 서비스, 토큰 저장 위치
9. **배경 작업**: 출석 초안·예약 알림을 처리할 서버 측 스케줄러 존재 여부
10. **환경변수**: 필요한 키 목록 (`.env.example` 유무)
11. **배포**: 현재 호스팅 방식과 빌드 산출물

---

## 3. 목표 구조 — 요구사항에서 도출되는 제약

스택과 무관하게 반드시 성립해야 하는 구조적 조건이다.

### 3.1 인증과 권한 (요구사항 B, C, 7절 검증 기준)

- 권한 판단의 **단일 출처는 서버**다. 클라이언트가 보낸 역할·팀 ID 값을
  신뢰해 접근을 허용하지 않는다.
- 권한 축이 **2개**다. 둘을 섞지 않는다.
  - **서비스 운영자 권한**: 계정 단위, 일반 회원과 별도 체계
  - **팀 내 역할**: `(계정, 팀)` 쌍 단위. 같은 계정이 팀마다 다른 역할을 가진다.
- 팀 내 상태는 최소한 다음을 구분해야 한다:
  `가입 대기 / 승인 팀원 / 운영진 / 주장 / 과거 선수(제외됨)`
- 운영진 권한은 **기능별 위임**이어야 한다. 하나의 "운영진" 플래그로
  가입 승인·매칭 결정·주장 인계·기록 확정을 한꺼번에 주지 않는다.
- **선수 포지션·프로필 필드와 팀 운영 역할 필드를 물리적으로 분리**한다.
  본인 프로필 수정 API는 역할 필드를 절대 쓰지 못해야 한다.
  (요구사항 C의 사용자 제보가 이 경계와 관련 있을 가능성이 높다 — 미검증 추정)
- 역할 변경·팀원 제외 후 **기존 세션에도 현재 권한이 적용**돼야 한다.
  세션 토큰에 역할을 박아두고 만료까지 신뢰하는 구조면 수정 대상이다.
- 팀 정지는 해당 팀 범위에만 적용되고 계정의 다른 팀 권한에 영향을 주지 않는다.

### 3.2 데이터 모델 — 요구사항이 강제하는 관계

정확한 테이블명은 기존 코드를 따르되, 다음 관계는 반드시 표현돼야 한다.

- **계정 ↔ 팀은 다대다**이고 그 연결 자체가 역할·상태·가입 시점을 가진다.
- **경기**는 팀별 레코드가 아니라 **공통 경기 ID** 하나에 양 팀이 연결된다.
  (요구사항 F: 수락 시 양쪽 달력에 같은 경기가 보여야 함)
  투표·출석·내부 메모·집합 시간은 `(경기, 팀)` 단위로 분리한다.
- **외부팀**은 계정·승인 팀이 아니라 경기에 붙는 **이름 문자열**이다.
  나중에 같은 이름의 팀이 가입해도 자동 연결하지 않는다.
- **선수 ID는 안정적**이어야 한다. 탈퇴·개명 후에도 과거 기록이
  같은 선수를 가리키고, 당시 표시 정보(이름 등)를 보존한다.
- **출석은 `(경기, 팀, 선수)` 고유 제약**을 가져야 한다.
  확정 재실행·중복 요청에도 행이 늘지 않는다(멱등).
- **경기 결과는 버전을 가진다.** 제안 → 확인 → 확정이며,
  상대가 다른 값을 내면 이견 상태로 남고 덮어쓰지 않는다.
  결과 확인은 특정 결과 버전에 연결한다.
- **감사 이력**이 필요한 대상: 운영자 개입, 팀 정지·복구, 주장 인계,
  경기 취소 사유, 결과·기록 정정(수정자·시각·사유·변경 내용).

### 3.3 동시성 — 서버에서 막아야 하는 것

화면 비활성화로 해결하지 않는다. DB 제약 또는 트랜잭션으로 막는다.

| 상황 | 요구 조건 |
|---|---|
| 매칭 동시 수락 | 한 상대팀만 확정, 나머지는 모집 종료 |
| 용병 마지막 자리 동시 승인 | 정원 초과 없음 |
| 출석 확정 중복 클릭 / 작업 재실행 | 집계 중복 없음 |
| 투표 중복 요청 | 중복 행 없음 |
| 팀 승인 | 팀 활성화 + 최초 주장 지정이 함께 성공 (원자적) |
| 주장 인계 | 무주장 팀 상태가 생기지 않음 |
| 공지 푸시 재시도 | 같은 공지 중복 발송 없음 |
| 네트워크 실패 후 재시도 | 중복 데이터 없음 |

시간 충돌 판정: 취소되지 않은 경기 구간의 **겹침**.
`이전 종료 == 다음 시작`은 **충돌 아님**. 수락 직전에 재확인한다.

### 3.4 배경 작업 (요구사항 E, I)

- 출석 초안 생성과 투표 마감 24시간 전 알림은
  **브라우저를 켜두지 않아도** 처리돼야 한다 → 서버 측 스케줄러 필요.
- 작업은 **재실행 가능(idempotent)** 해야 한다. 지연·재시도 시
  같은 기준으로 복원되고, **수동 수정한 출석을 덮어쓰지 않는다.**
- 일정이 변경되면 예약 알림을 재계산한다.
- 1인 운영·서버비 최소화 조건 때문에 상시 워커 프로세스보다
  호스팅에 내장된 스케줄 실행을 우선 검토한다. (결정은 T04/T08에서)

### 3.5 파일 저장 (요구사항 J)

- 팀 로고·프로필 이미지는 **실제 영속 저장소**에 올라가야 한다.
  재로그인·다른 기기에서 보여야 하므로 브라우저 저장소·메모리는 불가.
- 컨테이너 로컬 디스크는 배포마다 사라질 수 있으므로 원본 확인 시
  현재 저장 위치가 영속적인지 반드시 확인한다.
- 업로드 시 파일 종류·크기를 서버에서 검증한다.
- 접근 권한을 적용한다. 개인 데이터가 공용 캐시에 남지 않게 한다.
- 저장 실패를 성공 화면으로 표시하지 않는다.

### 3.6 푸시 (요구사항 I)

- **앱 내 알림함(DB)과 기기 푸시(외부 전송)는 별개 경로**로 구현하고
  결과를 각각 확인할 수 있어야 한다.
- 기기 토큰의 등록·갱신·삭제, 로그아웃·팀 탈퇴 시 정리가 필요하다.
- 발송 직전에 수신자의 현재 팀 권한을 재확인한다.
- 푸시 클릭 진입 시에도 권한을 다시 검사한다.
- 토큰·인증정보를 로그·문서에 남기지 않는다.
- iOS/Android 실제 지원 경로와 필요한 개발자 계정은 T08에서
  **구현 시점의 공식 문서로** 확인해 기록한다. (현재 미확인)

### 3.7 외부 의존성 — 구장 검색 (요구사항 D)

- 장소 검색은 외부 API가 필요하다. 사용 서비스와 키는 미정.
- **API 장애·미설정이어도 수기 입력으로 일정 저장이 가능**해야 한다.
  즉 장소 검색은 부가 기능이고, 저장 스키마는 검색 결과 없이도 유효해야 한다.
- 검증되지 않은 주소를 생성하지 않는다. 선택한 후보의 이름·주소·좌표를 저장한다.

### 3.8 모바일·배포 (요구사항 A)

- 기본 탭 5개: 홈 / 일정 / 매칭 / 기록 / 우리팀
- 웹앱/PWA와 네이티브 앱의 기능 차이(특히 iOS 푸시, 스토어 심사)는
  원본 확인 후 이 문서에 표로 기록한다.
- **웹 배포 성공은 앱스토어 대응 완료가 아니다.**

---

## 4. 실행 환경 (2026-09-14 이 컨테이너에서 확인)

| 항목 | 값 |
|---|---|
| OS | Linux 6.18.44 (컨테이너, 임시) |
| 시스템 시간대 | **UTC** (서비스 기준은 Asia/Seoul — 날짜 경계 계산 주의) |
| node / npm | v22.22.2 / 10.9.7 |
| pnpm / yarn | 10.33.0 / 1.22.22 |
| python3 | 3.11.15 |
| psql (클라이언트) | 16.13 — **DB 서버는 미구성** |
| docker | 29.3.1 |
| flutter / dart | 설치 안 됨 |

컨테이너는 임시다. 커밋·푸시하지 않은 작업은 세션 종료 시 사라진다.

---

## 5. 비용에 영향을 주는 항목 (1인 운영 · 서버비 최소화)

원본 확인 후 각 항목의 실제 선택과 요금을 **공식 문서로** 확인해 채운다.

| 항목 | 필요 이유 | 현재 상태 |
|---|---|---|
| 앱 호스팅 | 서버 권한 검증이 필요하므로 정적 호스팅만으로는 불가 | 미확인 |
| 데이터베이스 | 다중 사용자·동시성 제약 처리 | 미확인 |
| 파일 저장소 | 로고·프로필 이미지 영속 저장 | 미확인 |
| 스케줄 실행 | 출석 초안, 예약 알림 | 미확인 |
| 푸시 발송 | 기기 푸시 | 미확인 |
| 장소 검색 API | 구장 주소 | 미확인 |
| Apple 개발자 계정 | iOS 배포·푸시 | 미확인 (유료, 사용자 결정 필요) |
| Google Play 개발자 계정 | Android 배포 | 미확인 (유료, 사용자 결정 필요) |

유료 계약은 임의로 진행하지 않는다. 필요 시점에 사용자에게 모아서 요청한다.

---

## 6. 기존 코드 유지·수정 범위

원본 확보 전이라 **아직 결정할 수 없다.**
확보 후 다음 원칙으로 판단하고 선택 이유를 이 절에 기록한다.

- 작동 중인 기능과 디자인은 유지한다.
- 기존 스택 위에서 최소 변경으로 요구사항을 충족한다.
- 큰 재작성은 **구체적 문제와 대안 비교**가 있을 때만 검토한다.
- 잠금 파일과 실행 방식을 존중하고 의존성을 불필요하게 늘리지 않는다.
- 요구사항 7절의 권한·동시성 검증을 기존 구조로 만족할 수 없을 때에만
  구조 변경을 제안하고, 그 근거를 여기에 남긴다.
