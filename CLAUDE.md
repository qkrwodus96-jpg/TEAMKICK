# CLAUDE.md — TEAMKICK 개발 규칙

TEAMKICK(모바일 표기: 팀킥) 저장소의 상시 개발 기준이다.
상세 제품 요구사항은 `PROJECT_CONTEXT.md`, 구조는 `ARCHITECTURE.md`,
작업 목록은 `TASKS.md`, 현재 위치는 `HANDOFF.md`에 있다.

## 1. 문서 읽는 순서

새 세션은 다음 순서로만 읽고 시작한다.

1. `CLAUDE.md` (이 문서)
2. `HANDOFF.md` — 현재 작업 위치와 다음 행동
3. `TASKS.md` — 작업 상태와 완료 조건

그다음 **현재 작업에 필요한 문서와 코드만** 추가로 확인한다.
변경 근거가 없으면 매 세션마다 프로젝트 전체를 다시 분석하지 않는다.

- 제품 요구사항이 필요할 때만 `PROJECT_CONTEXT.md`
- 스택·데이터 모델·권한 구조가 필요할 때만 `ARCHITECTURE.md`

## 2. 현재 저장소 상태 (2026-09-14 확인)

원본 소스가 `main`에 들어와 병합했다. **118개 파일, 실제 동작하는 앱이다.**

- 스택: vinext(Vite 기반 Next 호환) + React 19 + TypeScript,
  Cloudflare Workers에서 서빙, **Cloudflare D1** 저장, Drizzle migration,
  shadcn/Radix UI, PWA(manifest·오프라인 안내).
- 인증: **ChatGPT Sites 로그인 전용.** 서버가 `oai-authenticated-user-*`
  헤더로 신원을 읽는다. 자체 회원가입은 없다.
- 상태 저장: `entities` 단일 문서 테이블 + `state_revision` 버전 +
  `write_guards` CHECK 제약으로 낙관적 동시성 제어.
- 모든 명령은 `POST /api/app` 하나로 들어가고 `lib/model.ts`가 권한을 검증한다.

기존 문서는 역할이 다르니 함께 유지한다:
`PRD.md`(원본 요구사항), `ARCHITECTURE.md`(구조), `TASKS.md`(작업),
`PROJECT_CONTEXT.md`(확정 요구사항과 현재 코드의 차이).

## 3. 실행·검증 명령 (실제 실행해 확인함)

```bash
pnpm install --frozen-lockfile          # 의존성 설치 (exit 0 확인)
pnpm dev                                # 개발 서버 → http://localhost:5173
node --test tests/core.test.mjs         # 도메인·저장·API 테스트 10/10 통과
node node_modules/typescript/bin/tsc --noEmit   # 타입 검사 통과
pnpm lint                               # 기존 no-explicit-any 오류 77건 있음(시작 시점부터)
pnpm db:generate                        # 스키마 변경 시 migration 생성
```

**로컬 첫 실행 시 주의**: 로컬 D1에는 테이블이 없어 `/api/app`이 503이 된다.
`drizzle/0000_bitter_cardiac.sql`을 로컬 D1 파일에 적용해야 한다
(`.wrangler/state/v3/d1/miniflare-D1DatabaseObject/*.sqlite`).

로컬 개발 서버는 mock 로그인을 붙인다. `/signin-with-chatgpt`를 한 번 호출해
`__sites_local_auth=1` 쿠키를 받으면 고정 사용자(`local_seedy`)로 로그인된다.
**로컬 mock 사용자는 1명뿐이라 다중 사용자 권한 검증은 브라우저가 아니라
`tests/core.test.mjs`에서 한다.**

운영자 초기 설정 코드의 원문은 저장소에 없다(`lib/owner-config.ts`에 SHA-256만
있음). 코드가 필요한 검증은 사용자에게 요청한다.

## 4. 개발 규칙

1. 기존 디자인과 **작동 중인 기능을 최대한 유지**한다.
   기존 스택을 확인한 뒤 최소 변경으로 요구사항을 충족한다.
   큰 재작성은 구체적 문제와 대안 비교가 있을 때만 검토한다.
2. 실서비스 기준으로 만든다. 로그인·업무 데이터·이미지·알림은
   실제 시스템(서버 DB, 파일 저장소, 푸시)에 연결한다.
   브라우저 로컬 저장소나 고정 배열로 실서비스 구현을 대체하지 않는다.
3. **미연결 기능을 성공 화면이나 임의 데이터로 작동하는 것처럼 보이지 않는다.**
4. 권한은 화면뿐 아니라 **서버·DB·파일 접근에서도** 검증한다.
   클라이언트가 보낸 역할 값을 신뢰하지 않는다.
5. 비밀번호·관리자 비밀값·API 키를 코드에 하드코딩하지 않는다.
   토큰·인증정보·개인정보를 로그나 인계 문서에 남기지 않는다.
6. 기존 잠금 파일과 실행 방식을 존중하고 불필요한 의존성을 늘리지 않는다.
7. 현재 API·플랫폼 지원·요금은 **구현 시점의 공식 문서로** 확인한다.
8. 단순 UI 변경 때문에 테스트를 늘리지 않는다.
   권한·동시성·데이터 집계에는 실제 실패를 잡는 검증을 둔다.
9. 기준 시간대는 Asia/Seoul, 기본 언어는 한국어.
   (이 컨테이너의 시스템 시간대는 UTC이므로 날짜 경계 계산에 주의한다.)

## 5. 보고 규칙 — 코드 작성 / 검증 / 배포를 구분한다

한 문장에 섞지 않고 항상 아래를 구분해 보고한다.

- **코드 작성 완료**: 코드가 존재한다.
- **로컬 검증 완료**: 실제로 실행해 결과를 확인했다.
- **외부 연동 검증 완료**: 실제 DB/저장소/푸시로 확인했다.
- **배포 완료**: 배포 후 실제 동작을 확인했다.

상태 표기는 다음을 구분해 쓴다:
작동 확인 / 코드만 존재 / 데모 데이터 / 미검증 / 미구현.

시작 시점부터 있던 오류와 작업 중 발생시킨 오류를 구분해 기록한다.
테스트 통과는 **실제 실행 결과가 있을 때만** 기록한다.
실행이 불가능하면 명령, 이유, 미검증 위험을 남긴다.
실사용자에게 테스트 공지나 푸시를 발송하지 않는다.

## 6. 자율 진행 기준

**사용자 확인 없이 진행한다**

- 로컬 코드 수정과 그에 필요한 검증
- 되돌릴 수 있는 세부 선택 (가정을 `PROJECT_CONTEXT.md`에 기록하고 진행)
- 문서 갱신, 로컬 테스트 데이터 생성

**사용자 지시와 실제 권한 범위를 따른다**

- 유료 서비스 계약, 운영 데이터 삭제
- 실사용자 대상 발송, 공개 범위 변경
- 외부 계정·API 키가 필요한 설정

API 키·계정이 없으면 필요한 설정과 영향 범위를 기록하고,
그 의존성 없이 진행할 수 있는 작업은 계속한다.

## 7. 작업 단위 진행 규칙

작업 하나마다: ① 현재 상태·완료 조건 확인 → ② 코드 수정 →
③ 해당 변경에 필요한 검증 → ④ `TASKS.md`·`HANDOFF.md` 갱신 →
⑤ 다음 진행 가능한 작업으로 이동.

- 설명이나 계획만 제출하고 구현 가능한 작업을 남겨두지 않는다.
- 같은 실패를 근거 없이 반복하지 않는다.
- 막히면 **원인 / 시도한 해결 / 필요한 입력**을 `HANDOFF.md`에 기록하고,
  독립적으로 진행 가능한 다음 작업을 계속한다.
- 사용자 결정이나 계정 설정이 꼭 필요한 질문은 모아서 짧게 한다.
- 이미 전달받은 요구사항을 다시 묻지 않는다.
- 운영 권한을 우회하거나, 실제로 없는 도구·계정을 썼다고 주장하지 않는다.
- 세션 밖에서 작업이 자동 진행된다고 약속하지 않는다.

## 8. Astra 검토 사용 기준

일상적인 구현·실행·테스트·오류 해결은 Claude Code에서 완료한다.
Astra 검토는 **중요한 변경이나 해결하기 어려운 문제에 선택적으로만** 사용한다.
Astra의 답변을 다음 작업의 필수 조건으로 만들지 않는다.
검토가 필요해진 시점에만 `REVIEW_PACKET.md`를 작성한다
(양식은 `PROJECT_CONTEXT.md` 부록 참고). 비밀키·토큰·실사용자
개인정보는 제외한다.

## 9. 진행 보고 양식

사용자에게는 항상 다음 5항목으로 짧게 보고한다.

1. 이번 변경
2. 실제 검증
3. 남은 문제
4. 다음 작업
5. 사용자가 해야 할 일 (없으면 "없음")
