# TEAMKICK 작업 인계

최종 갱신: 2026-09-19
작업 폴더: `/home/user/TEAMKICK`
브랜치: `claude/fervent-galileo-8sulep`
이 브랜치에 배포본 1.7.1(`codex/account-recovery-20260919`)을 합쳐 두었다.
PR #21 은 2026-09-18 병합 완료(T39-T62), PR #20 은 2026-09-17 병합 완료.
**아직 `main` 에는 1.7.1 도 이 브랜치의 작업도 들어가지 않았다.**
미커밋 변경: 없음
버전: `APP_VERSION=1.7.1`, `BUILD=2026-09-19-account-recovery`
**다음 세션이 가장 먼저 할 일: 아래 "사용자가 기다리는 7가지" 를 처리한다.**
현재 작업 ID: **배포본 1.7.1 을 저장소에 합쳤다. 밀려 있던 화면 작업을 잇는다.**

### 2026-09-19 — 배포본 1.7.1 이 저장소에 들어왔다

`codex/account-recovery-20260919` 브랜치(커밋 `61f051c`, 15개 파일)를 받아 합쳤다.
GPT 가 `main`(`ab2ea14`)에서 깨끗하게 갈라져 나와 충돌은 `HANDOFF.md` 하나뿐이었다.
`scripts/*.sh` 는 Claude 가 먼저 고친 것과 **줄바꿈 말고 내용이 같아** 그대로 맞았다.

**이제 저장소가 배포본과 같다. 화면 파일을 고쳐도 된다.**

### 1.7.1 에서 바뀐 것 중 운영에 영향 있는 것

- **가입 정책이 바뀌었다.** T20 에서 Claude 가 만든 "소셜 키가 있으면 막고 없으면
  연다" 를 GPT 가 **`EMAIL_SIGNUP_ENABLED=true` 일 때만 열고 기본은 차단**으로
  바꿨다(`lib/signup-policy.ts`). **예전 비상 복구법(소셜 키 빼기)은 통하지 않는다.**
  가입이 막히면 환경 변수에 **`EMAIL_SIGNUP_ENABLED=true`** 를 넣는다.
  (운영자 계정이 이미 있으므로 지금의 위험은 아니다. 백지 복구 때만 쓴다.)
- **백업 형식이 2 로 바뀌었다.** 소셜 식별자(`provider_id`)를 보존하고, 그 값이 없는
  **예전 백업 파일은 복원을 거절한다.** 1.7.1 이전 백업은 더 이상 복구 수단이
  아니다. **새로 받아 두어야 한다.**

### 사용자가 기다리는 7가지 (2026-09-19 제보)

1.7.1 이 들어오기 전이라 손대지 못하고 밀어둔 것들이다. 이제 할 수 있다.

| # | 할 일 | 파악한 원인 |
|---|---|---|
| 1 | 용병 알림이 매칭의 **용병 탭**까지 가게 | 지금은 매칭 화면까지만 간다 |
| 2 | 경기 **시작·종료**에도 요일 | 투표 마감에만 붙어 있다고 제보 |
| 3 | 가입 직후 **기기 알림** 켜도록 안내 | 브라우저 권한이라 자동으로 켤 수는 없다 |
| 4 | **투표 마감을 화면에 표시** | **저장은 잘 된다**(직접 돌려 확인). 보여주는 곳이 없어 "반영 안 됨" 으로 보였다 |
| 5 | **D-day 를 한국 날짜 기준으로** | `Math.ceil((시작-지금)/하루)` 라 오늘 오전이면 D-1 이 된다 |
| 6 | 매칭 이름을 **"우리 팀 매칭 내역"** + 확정 시각 | **이미 팀원에게 보인다**(테스트로 고정함). 이름만 개인 것처럼 읽힌다. 확정 시각은 `decidedAt` 으로 쌓기 시작했다 |
| 7 | **용병 모집에도 검색란** | 없다 |

### 푸시 — 고장이 아니었다

`/api/push` 가 `ready:true, devices:1`. 원인은 **알림이 만든 사람 본인에게는 가지
않는다**는 것(`app/api/app/route.ts` 의 `x.userId!==user.userId`). 기기가 본인 폰
1대뿐이면 혼자 시험하는 동안 푸시가 영영 오지 않는다.

- `testWake()` 와 MY → 기기 알림의 **"이 기기로 시험 알림 보내기"** 를 넣었다.
- 푸시 서버가 거절한 이유(상태 코드·응답)를 버리지 않고 화면까지 올린다.
- **미검증**: 실제 푸시 서버 왕복. 이 환경은 외부 접속이 막혀 있다.

---

## 확인된 상태

### 실제로 확인한 사실

- 원본 소스가 `main`에 들어와 병합했다. **118개 파일, 실제 동작하는 앱이다.**
  이전 세션의 "소스 없음" 기준은 더 이상 유효하지 않다.
- 스택: vinext(Vite 기반 Next 호환) + React 19 + TypeScript,
  Cloudflare Workers + **D1**, Drizzle migration, shadcn/Radix, PWA.
- 인증은 **자체 회원가입(이메일 + 비밀번호)** 이다(`lib/auth.ts`).
  `app/chatgpt-auth.ts` 는 더 이상 쓰지 않는다.
- 모든 변경은 `POST /api/app` 하나로 들어가고 `lib/model.ts`가 서버에서
  팀 소속·역할·승인 상태를 검증한다. 클라이언트 역할 값을 신뢰하지 않는다.
- 동시성은 `state_revision` 버전 + `write_guards` CHECK 제약 + 최대 4회
  재시도로 처리한다. 요청 ID를 7일 보관해 재전송 중복을 막는다.
- **최근 실행 결과(2026-09-18)**: `node --test tests/core.test.mjs` **101/101 통과**,
  `tsc --noEmit` 통과. (이 저장소를 처음 받았을 때의 기준값은 10/10 이었다.)
- `pnpm lint`는 `no-explicit-any` **77건 오류**. **시작 시점부터 있던 문제**이며
  이번 작업이 만든 것이 아니다. 기존 코드 스타일이라 손대지 않았다.
- ~~로컬 D1에는 테이블이 없어 첫 실행 시 `/api/app`이 503이다.~~ **더 이상 아니다.**
  `ensureSchema()` 가 첫 요청에서 스키마를 만든다. 2026-09-18 빈 컨테이너에서
  `pnpm dev` 만으로 `/api/health` 가 모든 테이블 `true`, `setupError:null` 을 돌려줬다.
- 시작 시점에 없던 기능 중 **용병 모집·구장 검색·이미지 업로드는 구현했다.**
  아직 없는 것: **실제 기기 푸시**(`sw.js` 에 push 핸들러 없음), 광고 영역(보류),
  이메일 실재 확인.

### 아직 확인하지 못한 사항

- 실기기(iPhone·갤럭시) 확인. 이번엔 **헤드리스 Chromium 에뮬레이션만** 사용했다.
- 배포 환경의 실제 로그인·D1·접근 범위. 로컬 mock 로그인만 사용했다.
- 다중 사용자 동시 조작의 브라우저 검증. 로컬 mock 사용자는 1명뿐이라
  권한·동시성은 `tests/core.test.mjs`로만 확인했다.
- ~~달력·주소 복사·지도, 앱 내 알림·초대 링크, PWA 오프라인 동작.~~
  **2026-09-18 브라우저에서 확인 완료**(아래 표 참고). 남은 것은 **실기기 PWA 설치**뿐이다.
- 운영자 초기 설정 코드의 원문(저장소에 SHA-256만 있음).

---

## 배포 경로 — 이것부터 알고 시작한다

**GitHub 과 배포는 연결되어 있지 않다.** `main` 에 합쳐도 앱은 바뀌지 않는다.

```
GitHub main  ──✗ 연결 없음 ──  ChatGPT Sites (실제 서비스)
```

- **사용자가 보는 Sites 편집 화면에는 환경 변수만 있다. 코드 파일 목록이 없다**
  (2026-09-19 사용자 확인). 따라서 **사용자가 배포본의 소스를 꺼내올 수 없다.**
- 배포는 **GPT 가 자기 도구로 Sites 에 직접 코드를 써서** 한다. Claude Code 는
  이 컨테이너에서 `api.cloudflare.com` 과 `chatgpt.com` 둘 다 막혀 있어
  (확인함: HTTP 000) **배포할 수 없다.** GitHub 과 npm 만 열려 있다.
- 그래서 **배포본이 저장소보다 앞서는 일이 생긴다.** 실제로 1.7.1 이 그랬다.
  그 상태에서 저장소 기준으로 작업해 배포하면 **배포본의 수정이 덮어써져 사라진다.**

### 배포본이 저장소보다 앞설 때의 순서

1. **코드 작업을 멈춘다.** 저장소가 옛날 것이므로 무엇을 만들어도 덮어쓴다.
2. GPT 에게 **"GitHub 에 올려줘"** 를 시킨다. 이 길뿐이다 — 사용자도 Claude 도
   배포본 소스를 꺼낼 수 없다.
3. 올라온 것을 받아 검증하고(테스트·타입·lint) 문서를 갱신한 뒤 작업을 잇는다.

### 이 구조를 없애는 방법 (미결정)

1. **Sites 를 GitHub 에 연결** — 설정에 그런 메뉴가 있는지 확인이 필요하다. 있으면 가장 싸게 해결된다.
2. **Cloudflare 로 이사 + GitHub Actions 자동 배포** — Actions 는 GitHub 서버에서 돌아
   이 컨테이너의 접속 제한과 무관하다. 다만 D1 의 **실사용자 데이터 이사**가 따른다.
   앱에 백업·복원이 있어 가능은 하다(단, 1.7.1 에서 백업 형식이 2 로 바뀌었다).

---

## 이번에 완료한 작업

## 2026-09-19 — T20 검토 후 가입 정책·복구 보완

기준 소스: GitHub main `ab2ea14fd8de07f90e45b678b0f2fd66c63e7387`.
사용자 요청: 검토 후 배포와 실제 상태 확인까지 진행.

- `EMAIL_SIGNUP_ENABLED`가 정확히 `true`일 때만 이메일 신규 가입을 허용한다.
  미설정·빈 값·잘못된 값은 차단한다. 로컬에서 가입이 필요하면 명시적으로 켠다.
  소셜 제공자 키와 독립된 정책이며, `/api/app`이 같은 정책을 화면에 전달한다.
- 기존 이메일 로그인·복구·확인은 유지한다. 소셜 내부 주소에는 복구/확인 메일을 보내지 않는다.
- 재설정 링크는 로그인·샘플 상태와 무관하게 바로 양식을 연다. 취소 후 URL을 정리한다.
- 토큰 검사, 비밀번호 변경, 모든 이전 복구 링크 무효화, 기존 세션 폐기와 새 세션 발급을
  D1 batch 한 트랜잭션으로 처리한다. 기존 `write_guards` CHECK 제약을 재사용한다.
  같은 토큰이나 서로 다른 토큰의 동시 재설정도 한 건만 성공한다.
  이메일 확인 토큰도 트랜잭션으로 한 번만 사용하고 세션을 만들지 않는다.
- 재설정 메일 발송 실패 시 해당 토큰을 삭제해 즉시 재시도를 허용한다.
- 백업 형식 2에 `provider_id`를 담아 구글·네이버·카카오의 내부 계정 ID를 유지한다.
  형식 1의 local/카카오 백업도 수용한다. 구글·네이버 식별자가 빠진 옛 백업은
  데이터를 변경하기 전에 오류로 거부한다. 이메일에서 식별자를 추정하지 않는다.
- 계정 연결·자동 병합·추가 개인정보 수집은 하지 않는다. 같은 로그인 수단 사용 안내를 추가했다.
- 빌드를 막던 `scripts/build-verified.sh`, `scripts/sites-env.sh`의 CRLF를 LF로 고치고
  실행 권한을 복구했다. 의존성 변경과 수동 운영 DB 변경은 없다.

실제 검증: `node --test tests/core.test.mjs` **116/116 통과**, `tsc --noEmit` 통과.
SQLite 기반 실제 인증·백업 함수로 동시성, 중간 실패 롤백, 발송 실패 재시도,
복원 후 세 소셜 로그인 및 비밀번호 없는 local 계정 복구를 확인했다.
브라우저에서 재설정 링크의 직접 진입, 로그인 화면으로 복귀, 기본 신규 가입 버튼 0개 확인.
실제 OAuth 완료·이메일 수신·실기기 알림은 이 검증에 포함하지 않았다.

트랜잭션 근거: https://developers.cloudflare.com/d1/worker-api/d1-database/#batch
운영 상태 확인: https://teamkick.co.kr/api/health
예상 값: `build=2026-09-19-account-recovery`, `version=1.7.1`.

이하 2026-09-18 기록의 소셜 키에 따른 가입 허용 조건은 위 정책으로 대체되었다.


## 2026-09-18 (4) 이메일 가입 제거 (T20) + 검토 요청서

사용자 지시: "이메일 제거 삭제하고 gpt한테 보낼 프롬포트 보내줘",
"네이버 카카오 구글 가입히가 간격 일정하게 넓혀".

### T20 새 가입은 소셜 로그인으로만 받는다

- **서버가 관문이다.** `POST /api/auth` 의 `signup` 을 403 으로 거절한다.
  화면에서 감추는 것만으로는 부족하다 — 화면을 우회한 직접 호출도 막히는 것을 확인했다.
- **조건**: `kakaoReady()||socialReady("google")||socialReady("naver")`.
  즉 **소셜이 하나라도 준비되어 있을 때만** 막는다.
  **소셜 키가 하나도 없으면 막지 않는다.** 막아버리면 키를 넣기 전 배포나 로컬에서
  아무도 가입할 수 없고 **운영자 초기 설정(`setupOwner`)조차 못 한다** — 계정이
  먼저 있어야 하기 때문이다. 복구 수단이 코드 배포뿐이 되는 것을 피했다.
- **기존 이메일 계정은 그대로 쓴다.** `login`·`forgot`·`reset`·`verify`·`resendVerify`
  는 손대지 않았다. 세 경로가 200 인 것을 테스트로 고정했다.
- 화면: 회원가입 진입을 감추고, 설명을 "처음이시면 위 단추로 시작하세요. 따로
  가입하지 않아도 돼요." 로, 구분선을 "또는 이메일로" → "이미 이메일로 가입하셨다면" 으로 바꿨다.
- **마이그레이션 없음.** 스키마·기존 계정 데이터는 그대로다.
- `LEGAL.md` 갱신: 수집 항목 표에 신규 중단을 표시하고 소셜 회원번호·닉네임 줄을 더했다.
  **새로 수집하는 개인정보는 늘지 않고 줄어든다**(새 이용자의 이메일·비밀번호를 받지 않는다).

**확인한 것** (브라우저 · 양방향)

| 상황 | 결과 |
|---|---|
| 소셜 키 있음 · 로그인 화면 | 회원가입 단추 **0개** |
| 소셜 키 있음 · 화면 우회 가입 | **403** + 안내 문구 |
| 소셜 키 있음 · 기존 이메일 로그인 | **200** |
| 소셜 키 없음 · 로그인 화면 | 회원가입 단추 **1개** |
| 소셜 키 없음 · 가입 | **200**, 계정 생성됨 |

**변이 시험**: 서버 차단 한 줄을 지우면 테스트가 106/107 로 실패한다.

### 소셜 단추 간격·높이를 똑같이 맞췄다

간격 10 → **14px**. 구글 단추만 테두리 때문에 1px 높았는데(42/42/43)
`box-sizing:border-box` 와 `min-height:48px` 로 **48/48/48** 로 맞췄다.

### REVIEW_PACKET.md — GPT(Astra)에 보낼 검토 요청서

`PROJECT_CONTEXT.md` 부록 양식대로 작성했다. **비밀키·토큰·실사용자 개인정보 없음**
(넣지 않은 것을 grep 으로 확인했다). 판단을 구하는 질문 3개:

1. 차단 조건을 **런타임 환경변수**로 잡은 것이 맞는가? 배포 중 키가 비면 가입 문이
   **조용히 다시 열린다.** 반대로 고정하면 새 배포에서 아무도 가입 못 한다.
2. 기존 이메일 가입자가 잠기는 경로가 남아 있는가?
3. 소셜 계정과 기존 이메일 계정이 같은 사람일 때 **계정이 갈라지는 문제**를 지금
   구조로 나중에 합칠 수 있는가? (지금은 소셜에서 이메일을 일부러 받지 않는다.)

**검토 답에 따라 달라질 결정**도 함께 적어 뒀다 — 답이 "환경변수 기반은 위험하다"
면 `SIGNUP_MODE` 같은 명시적 스위치를 하나 더 둔다(되돌리기 쉽다).
질문 3 은 답을 받은 뒤에 정한다 — 소셜에서 이메일을 받기로 하면 수집 항목이 늘어
`LEGAL.md` 와 동의 문구를 함께 고쳐야 하고, **이쪽은 되돌리기 어렵다.**

### 검증

- `node --test tests/core.test.mjs` **107/107 통과**(새 테스트 2건)
- `tsc --noEmit` 통과, `pnpm lint` 오류 **77건 — 시작 시점과 같음**
- **미검증**: 실제 소셜 로그인 성공 경로(이 환경은 외부 API 차단).
  배포에서 셋 다 된다는 것은 **사용자 구두 확인**이 근거이고 내가 실행한 결과가 아니다.
  배포 후 **새 계정으로 소셜 가입이 실제로 되는지** 확인해야 한다.

---


## 2026-09-18 (3) 알림을 쓸모 있게 · 용병 참여 · 공지 알림 (T55-T60)

사용자 확인: **구글 로그인도 배포에서 된다.** 이로써 카카오·네이버·구글 셋 다 확인됐고
T20(이메일 가입 제거)을 막고 있던 조건이 풀렸다. **다만 가입 수단을 지우는 것은 되돌리기
어려워 사용자 승낙 없이 진행하지 않았다.**

### T55 로고를 누르면 홈으로 간다

머리말 로고가 그냥 그림이라 눌러도 아무 일이 없었다. 왼쪽 차림표와 휴대폰 머리말
두 곳 모두 단추로 바꿨다(`aria-label="홈으로"`). 확인: 매칭 화면 → 로고 → 홈.

### T56 알림을 몇 건인지 숫자로, 무슨 알림인지 한 줄로

- 예전에는 **빨간 점 하나**뿐이라 한 건인지 열 건인지 알 수 없었다.
  읽지 않은 개수를 숫자로 보여준다(99 넘으면 `99+`).
- **로고와 알림 사이에** 가장 최근 소식을 한 줄로 보여준다(누르면 알림함이 열린다).
  휴대폰에서는 자리가 좁아 제목만, 데스크톱에서는 제목과 내용을 함께 보여준다.
  자리를 넘치면 말줄임으로 줄인다.
- **홈 화면 아이콘에도 개수를 붙인다**(`setAppBadge`). 지원하지 않는 브라우저는
  조용히 넘어간다. 읽으면 지운다.

### T57 알림을 누르면 그 화면으로 간다

- 알림을 만들 때 **갈 화면을 함께 저장한다**(`to`). 제목 글자를 나중에 해석하지 않는다.
  매칭 신청·용병은 매칭, 공지는 홈, 가입·문의·팀 상태는 MY, 기록 정정은 기록,
  팀 등록 요청은 서비스 관리, 경기가 딸린 것은 일정.
- **고친 결함**: 처음에는 `gameId` 가 있으면 무조건 일정으로 보냈다. 그래서
  "새 매칭 신청" 을 눌러도 매칭이 아니라 일정으로 갔다. 서버가 적어 준 화면이
  먼저고, 일정으로 가는 알림일 때만 경기 창까지 열어 준다.
- **고친 결함(내가 만든 것)**: 목적지를 넣다가 `userNotice` 의 **teamId 자리**에
  화면 이름을 넣은 곳이 하나 있었다(용병 신청 거절). 그대로 뒀으면 그 알림이
  **아예 보이지 않았다.** 테스트로 막았다.
- 예전 알림에는 `to` 가 없다. 경기가 있으면 일정, 없으면 홈으로 되짚는다.

### T58 잠금화면 알림도 제 화면으로 · 가장 최근 소식으로

- **고친 결함**: 서비스 워커가 `unread[0]` 을 "최근" 으로 썼는데 알림은 오래된
  것부터 쌓인다. **가장 오래된 소식**을 잠금화면에 띄우고 있었다. 마지막 것으로 고쳤다.
- 알림을 누르면 `?to=` 를 실어 열고, 이미 열린 창에는 메시지로 알려 탭을 옮긴다.
- 서비스 워커도 홈 화면 아이콘 개수를 갱신한다.

### T59 승인된 용병이 참여 인원에 들어간다

사용자 요청: "용병 받으면 참여인원에 용병 포함하기."

- 서버가 경기별로 **승인된 용병 명단**(`guestRoster`)을 함께 내려준다.
  팀원 명단(`roster`)과는 **따로** 둔다 — 용병은 팀원이 아니고 출석·선수 통계에도
  넣지 않는다(ASM-04 유지).
- 첫 화면 다음 경기: "1명 참여 예정 / 2명 · 용병 1" 처럼 함께 센다.
- 경기 상세 · 참여 현황: `참여` 수에 포함하고 `용병 N명` 을 따로 보여주며,
  명단 아래에 용병을 `용병 참여` 표와 함께 적는다. 용병은 투표하지 않는다 —
  승인된 순간 뛰는 것이 정해지므로 참여로 센다.
- 테스트: 신청만 해서는 안 들어가고, 승인하면 들어가고, 확정을 취소하면 빠지고,
  팀원 명단에는 섞이지 않는 것을 고정했다.

### T60 공지 알림 보내기 단추

사용자 요청: "공지사항 알림 발송하기가 따로 있었으면 좋겠어."

- 공지를 올릴 때 한 번 가는 알림을 못 본 사람이 있다. **공지 상세에 `알림 보내기`**
  를 뒀다(주장만). 참여 투표 알림과 같은 규칙 — **6시간에 한 번**, 글쓴이 본인 제외.
- 확인: 팀원 1명에게 "1명에게 알림을 보냈어요.", 바로 또 누르면
  "방금 알림을 보냈어요. 6시간 뒤에 다시 보낼 수 있어요."(429).
  받을 팀원이 없으면 "알림을 받을 팀원이 없어요." 로 분명히 거절한다.
- 이 알림도 기기 알림을 켜 둔 팀원의 **잠금화면**으로 가고 홈 화면 아이콘 개수에 반영된다.

### 검증

- `node --test tests/core.test.mjs` **105/105 통과**(이번에 새 테스트 3건)
- `tsc --noEmit` 통과, `pnpm lint` 오류 **77건 — 시작 시점과 같음**
  (경고는 새 `<img>` 로 18 → 21. 기존 코드와 같은 종류다)
- 브라우저(390px·1280px): 로고 → 홈, 알림 개수 `12`, 요약 줄, 알림 3종을 눌러
  매칭·홈·일정으로 각각 이동, 용병 참여 인원, 공지 알림 보내기와 6시간 제한,
  다섯 탭 가로 넘침·콘솔 오류 없음
- **미검증**: 실기기의 잠금화면 알림과 홈 화면 아이콘 개수.
  이 환경은 푸시 키(VAPID)가 없고 헤드리스는 알림 권한을 주지 않는다.
  `setAppBadge` 도 헤드리스에서는 동작을 확인할 수 없다(예외는 잡아 둔다).

---


## 2026-09-18 (2) 사용자 제보 8건 + 로고 원본 적용 (T47-T54)

사용자가 실제 기기에서 쓰며 8가지를 알려줬다. **그중 네이버·카카오 로그인이
배포 환경에서 실제로 된다는 확인도 함께 왔다**(구글은 확인 못 받았다).

### T47 로고를 원본 파일로 바꿨다

사용자가 `public/` 에 원본을 올려줬다(`TEAMKICK_fullname_black_512.png`, 512x512).
예전에는 코드로 다시 그렸고(Arial Black + skewX) **글꼴이 원본과 달랐다.**

- `icon-512.png` ← 원본 그대로. `icon-192.png` ← 원본을 192 로 줄임.
- `icon-512-maskable.png` ← 원본 글자를 0.834 배로 줄여 정중앙에 다시 놓았다.
  안드로이드 안전 원(반지름 204.8) 안에 들어가는지 브라우저에서 다시 쟀다:
  **가장 먼 픽셀 196.4 < 204.8.**
- 첫 화면(`SplashMark`)·머리말(`BrandMark`) 도 같은 파일 하나를 쓴다.
  머리말은 가로 자리(116x80)라 `object-fit:cover` 로 **눌러 찌그러뜨리지 않고 잘라** 쓴다
  (원본은 정사각이고 글자는 가운데 띠에만 있어 위아래 빈 검정만 덜어낸다).
- 그림 파일이라 받는 동안 빈 자리가 보일 수 있어 `<head>` 에서 `preload` 한다.
  배경도 로고와 같은 검정이라 흰 번쩍임이 없다.
- 첫 화면 로고 크기를 `64vw → 86vw` 로 키웠다. 원본은 정사각 안에 글자가 세로 47% 만
  차지해서, 예전 크기 그대로 두면 글자가 작아 보였다.
- 탭 아이콘도 원본을 쓴다. `favicon.svg` 는 예전에 코드로 그린 것이라 지웠다.
- **테스트를 바꿨다.** 예전 테스트는 손으로 그린 SVG 의 `viewBox` 가 글자를 자르지
  않는지 보던 것이라 더는 맞지 않는다. 대신 (1) 첫 화면·머리말이 원본 파일을 쓰는지,
  (2) 코드로 다시 그리지 않는지(`<text>`·`skewX` 금지), (3) `object-fit:cover` 인지,
  (4) `preload` 가 있는지, (5) **manifest 에 적은 크기와 실제 PNG 크기가 같은지**
  (PNG 머리말을 직접 읽어 확인)를 고정했다.

### T48 (제보 1) 소셜 로그인 단추가 서로 붙어 있었다

카카오·네이버·구글 단추의 **세로 간격이 0px** 이라 한 덩어리로 보였다.
`.social-login{display:grid;gap:10px}` 로 감쌌다. 확인: 간격 10px / 10px.

### T49 (제보 2) 팀 공지가 오래된 것부터 나왔다

9/15 공지가 9/18 위에 있었다. `visibleState` 가 **저장된 차례 그대로**(=오래된 것 먼저)
내보내고 있었고 정렬이 아예 없었다. **고정한 공지 먼저, 그다음 최근 순**으로 바꿨다.
테스트로 고정했다(고정한 오래된 글이 맨 위, 나머지는 최신순).

### T50 (제보 3) 팀 공지 푸시 — 이미 가고 있었다. 문구가 틀렸다

"공지 푸시 버튼이 어딨어?" → **버튼이 없는 게 맞다. 공지를 올리면 자동으로 간다.**
`POST /api/app` 이 이번 저장으로 새로 생긴 알림을 받은 사람 모두에게 `wakeDevices` 를
부른다. 팀 공지도 거기에 들어간다(글쓴이 본인만 제외).

**틀린 것은 안내 문구였다.** 기기 알림 칸이 "경기 등록과 투표 마감을 잠금화면으로
알려드려요" 라고만 적어 두어, 공지는 안 가는 것처럼 보였다.

- 문구를 실제 동작에 맞췄다: "새 경기·팀 공지·매칭 신청·가입 신청 같은 새 소식".
- 공지 작성 창에 "올리면 팀원 모두의 알림함에 쌓이고, 기기 알림을 켜 둔 팀원에게는
  잠금화면 알림도 바로 갑니다. **따로 보내는 단추는 없어요.**" 를 적었다.

### T51 (제보 4) 앱 설치 단추가 "설치할 수 없다"고 했다

**사용자 짐작이 맞다. 이미 설치되어 있어서다.** 설치 여부를 `display-mode:standalone`
으로만 판단하는데, 설치한 뒤 **브라우저 탭으로** 들어오면 그 값이 거짓이라 설치를 또
권했다. 그 상태에서 누르면 브라우저가 거절한다.

- 한 번 설치하면(`appinstalled`, 또는 standalone 으로 열림) 기억해 두고 다시 권하지 않는다.
- `prompt()` 는 **한 번만** 부를 수 있다. 예전에는 두 번째 호출의 예외를 아무도 받지 않아
  눌러도 반응이 없는 것처럼 보였다. 이제 잡아서 "이미 설치되어 있을 수 있어요.
  아래 방법으로 직접 추가해주세요." 를 화면에 적는다.
- **미검증**: 헤드리스 브라우저는 `beforeinstallprompt` 를 띄우지 않아
  **실제 설치 창까지는 여기서 확인할 수 없다.** 코드 경로만 고쳤다.

### T52 (제보 5·7) 매칭 검색 줄을 새로 짰다

- **글자 잘림**: 390px 에서 검색칸이 **136px** 밖에 안 됐고 날짜칸과 한 줄에 눌려
  자리표시 글자가 잘렸다. 줄을 나누고 각 칸이 스스로 줄어들게 했다
  (확인: 칸 234px > 글자 139px, 가로 넘침 없음).
- **지역 분리**: 예전에는 지역이 검색어 안에 섞여 있어 따로 고를 수 없었다.
  `모든 지역` + 18개 지역을 고르는 칸을 따로 뒀다.
- **검색 단추**: 치는 대로가 아니라 **검색을 눌렀을 때** 적용된다(사용자 요청).
  확인: 입력만 했을 때 목록이 그대로였고, 누른 뒤에 걸러졌다.
- **정렬**: 모집 목록에 정렬이 아예 없어 저장된 차례대로 나왔다.
  **가까운 경기부터** 로 바꿨다. 확인: 9월 25일 → 10월 5일 → 10월 10일 → 11월 20일.
- **주소로도 찾는다**: 예전에는 지역·구장·팀 이름만 봤다. `서울 마포구 월드컵로` 로
  등록한 구장을 "마포" 로 찾으면 **아무것도 안 나왔다.** 주소를 넣었다.
  확인: 주소에만 있는 "해운대" 로 찾아진다.
- 조건이 걸려 있으면 **조건 지우기** 가 보인다.

### T53 (제보 6) 투표 마감에 요일이 없었다

경기 시작·종료에는 "10월 10일 (토) 10:00 – 12:00" 이 붙는데 **투표 마감에는 없었다**
(경기 만들기·우리 팀 경기 설정 두 곳). 같은 안내를 붙였다. 마감은 끝 시각이 없는
한 시점이라 "종료 시각을 정해주세요" 는 붙이지 않는다.
확인: "10월 9일 (금) 21:00".

### T54 (제보 8) 10월 10일 모집글이 안 보인 건 — **원인 미확정**

여기서 그대로 따라 해봤다. 10월 10일 경기를 **상대팀 모집으로** 만들었더니
서버도 화면도 모두 정상으로 보여줬다. **재현되지 않았다.**

가능성이 남은 것 두 가지를 이번 변경이 함께 덮는다:

1. **정렬이 없어 아래에 묻혔다** — 모집 목록에 정렬이 없었다(T52 에서 고침).
2. **검색어가 주소를 못 찾았다** — "마포" 처럼 주소에만 있는 말로 찾으면 빈 화면이
   됐다(T52 에서 고침).

그래도 아니라면 남는 것은 **경기를 만들 때 `상대팀을 모집할게요` 를 체크하지 않은
경우**다. 체크하지 않으면 우리 팀 일정에는 들어가지만 모집 목록에는 올라가지 않는다.
빈 화면에 그 이유를 적어 두었다.

**사용자에게 물어볼 것**: 그 경기가 `일정` 탭에는 보이는가? 만들 때
`상대팀을 모집할게요` 를 체크했는가? 화면에 다른 조건(지역·형식·날짜)이 남아 있었는가?

---


## 2026-09-18 세션 — T42 브라우저 확인 + 미검증 항목 정리 (T44-T46)

### 실행 환경 기준값 (이 컨테이너에서 실제로 실행함)

`pnpm install --frozen-lockfile` 성공 → `node --test tests/core.test.mjs`
**시작 시점 99/99 통과**(HANDOFF 에 45/45 로 적혀 있던 것은 낡은 값이다),
`tsc --noEmit` 통과, `pnpm lint` **오류 77건**(시작 시점과 같음, 이번 작업이 만든 것 없음).
작업 후: **테스트 101/101 통과**(새 테스트 2건), `tsc --noEmit` 통과, lint 오류 77건 그대로.

로컬 D1 은 이제 **첫 실행에서 스스로 만들어진다**(`ensureSchema`). 예전 인계 문서의
"drizzle SQL 을 손으로 넣어야 한다"는 더 이상 맞지 않는다. `/api/health` 가
모든 테이블 `true`, `setupError:null` 을 돌려줬다. `storageReady:true`(로컬 R2 흉내).
운영자 초기 설정은 `.dev.vars` 의 `OWNER_SETUP_CODE` 로 했다(코드 원문은 여전히 없어도 된다).

### T42 팀 로고 자르기 — **브라우저 확인 완료** (남아 있던 숙제)

900x300 가로로 긴 시험 그림(좌 빨강 L · 가운데 초록 C · 우 파랑 R, 위아래 검은 띠)으로
**1280px 와 390px 두 폭**에서 확인했다. 실제 계정으로 가입 → 운영자 등록 → 팀 등록 →
승인 → MY → 팀 정보 수정(톱니 단추) → 팀 로고 순서로 갔다.

- 그림을 고르면 **바로 올라가지 않고** 동그란 자르기 화면이 뜬다.
- 끌면 옮겨진다: 가로로 100px 끌면 정확히 100px 움직인다.
- **범위를 넘기면 한계에서 멈춘다**: 720px 그림 · 240px 틀 → 한계 240px 에서 멈췄다.
  세로는 딱 맞아 **아무리 끌어도 움직이지 않는다**(빈 곳이 생길 수 없다).
- 막대로 1~3배까지 커진다(720 → 2160px).
- 저장한 파일: **512x512 PNG, 투명한 픽셀 0개**. 화면에서 보던 왼쪽 빨강 부분이
  그대로 저장됐다(가운데·좌·우 표본 모두 `#e11d48`, 위아래는 원본의 검은 띠).
- 머리말 로고: 66x66(휴대폰 62x62), `border-radius:50%`, `clip-path:none`.
- 390px 에서 자르기 틀이 화면에 들어가고(양옆 75px) 가로 넘침이 없다.

### T44 알림 문구가 UTC 원문·영문 상태값을 그대로 보여줬다

위 확인 중에 알림함에서 발견했다. **사용자에게 보이던 글 그대로:**

- `난지천공원 인조잔디축구장 · 2026-09-22T01:00:00.000Z`
- `크롭시험FC · active`

- **원인**: 알림 본문을 만들 때 `lib/model.ts` 가 `g.start`(ISO 원문)와
  `team.status`(영문 상태값)를 문자열에 그대로 붙이고 있었다.
- **고침**: `seoulStamp` 를 만들어 `9월 22일 (화) 10:00` 모양으로 쓴다.
  **Intl 표준 시간대 자료에 기대지 않고** 9시간을 직접 더해 계산한다
  (이 컨테이너와 Workers 의 시간대가 UTC 라 날짜가 하루 밀릴 수 있다).
  상태는 한국어 표로 바꾸고, **정지·반려는 사유도 함께** 알려준다.
- **테스트**: 날짜가 넘어가는 자리(UTC 16:00 → KST 다음 날 01:00)와 해가 바뀌는 자리를
  고정했다. 예전 동작으로 되돌리는 변이 두 가지가 실제로 잡히는 것을 확인했다(100/101).
- **한계**: **이미 쌓인 알림의 글은 바뀌지 않는다.** 기록이라 소급해 고치지 않는다.
  배포 환경에도 예전 형식의 알림이 남아 있다가 보관 기간이 지나면 사라진다.

### T45 재방문마다 콘솔 오류가 하나씩 떴다 (hydration 불일치)

- **원인**: 첫 화면 스크립트가 React 보다 먼저 `<html>` 에 `data-splash="skip"` 를
  붙인다. 서버가 보낸 HTML 에는 그 표시가 없으니 React 가 불일치로 본다.
  **첫 방문은 오류 0건, 재방문은 1건**이 떴다(브라우저에서 세어 확인).
- **고침**: 표시를 서버에서 미리 붙일 수 없으므로(`sessionStorage` 는 브라우저에만 있다)
  `<html>` 의 속성 비교만 껐다(`suppressHydrationWarning`). `<html>` 에 걸릴 다른 속성은
  `lang` 뿐이고 자식 요소 검사는 그대로 살아 있다.
- **확인**: 고친 뒤 첫 방문 0건 · 재방문 0건. 첫 화면 동작도 그대로다
  (첫 방문에 2초 보이고 사라짐 → 재방문에 건너뜀 → `data-splash=skip`).

### T46 모달 부제가 전부 "우리 팀의 정보를 확인하고 관리하세요."

알림함·공지 상세·1:1 문의처럼 팀 정보와 상관없는 창에도 같은 문장이 붙어 있었다.
팀 정보와 무관한 창 12개에만 제 설명을 주고 나머지는 기존 문구 그대로 뒀다.
확인: 알림 → "읽지 않은 소식부터 최근 순으로 보여드려요.",
팀 정보 수정 → 기존 문구 유지.

### 인계 문서에 `UNVERIFIED` 로 남아 있던 것들 — 이번에 직접 확인했다

| 항목 | 결과 |
|---|---|
| 달력 | 경기 있는 날에 표시가 찍히고, 눌러서 고르면 그 날 경기가 나온다 |
| 주소 복사 | 클립보드에 `서울 마포구 한강로 111` 이 들어갔고 "복사했어요." 안내가 뜬다 |
| 지도 보기 | `https://map.naver.com/p/search/<주소>` 로 새 탭이 열린다(이 환경은 외부 접속이 막혀 목적지 화면까지는 확인 못 함) |
| 앱 안 알림함 | 공지·경기·팀 등록 상태 알림이 최근 순으로 쌓인다 |
| 팀 공지 | 홈에 뜨고 눌러 상세를 열고 삭제까지 된다 |
| 초대 링크 | 정상 링크 → 가입 신청 창이 뜬다 |
| 초대 링크 폐기 | 창이 뜨지 않고 앱은 정상 동작한다 |
| 초대 링크 만료 | 로컬 D1 에서 만료 시각을 과거로 바꿔 확인. 서버가 `invitedTeam:null` 을 주고 창이 뜨지 않는다 |
| 없는 초대 토큰 | 조용히 무시되고 앱은 정상 동작한다 |
| PWA 자산 | manifest 200(`application/manifest+json`), 아이콘 3개 200, `offline.html` 200, `sw.js` 200 |
| 서비스 워커 | 등록·활성화되고 페이지를 제어한다(`navigator.serviceWorker.controller` 있음) |
| 오프라인 | 연결을 끊고 새로 열면 "인터넷 연결을 확인해주세요" 화면이 뜨고, 연결을 되살리면 앱으로 돌아온다 |

**여전히 미검증**: 실기기(iPhone·갤럭시) 홈 화면 **설치**. 여기서는 설치에 필요한 자산과
서비스 워커까지만 확인했고, 헤드리스 브라우저는 `beforeinstallprompt` 를 띄우지 않는다.

### 2026-09-18 세션의 다음 행동

1. **배포 후 구글·네이버 로그인 실제 확인** → 되면 T20(이메일 가입 제거).
2. **T43 로고 원본 파일** — 사용자가 `public/` 에 올려주면 아이콘·첫 화면·머리말 교체.
3. **T11 출시 준비 정리** — 웹 실행·모바일 사용·네이티브 빌드·스토어 제출 구분.
4. 실기기 PWA 설치 확인(사용자 폰 필요).

---


### 팀 로고를 동그라미로, 올릴 때 앱 안에서 크기 맞추기 (T42)
**코드 작성 완료 / 타입·테스트 통과 / 브라우저 확인은 남았다**

사용자 요청: "팀마다 로고가 조금씩 다른데 지금은 깃발 모양에 들어간다.
동그랗게 하고 앱 안에서 이미지 크기를 조정할 수 있게 하면 좋겠다."

- `.club-crest` 의 `clip-path`(방패·깃발 모양)를 없애고 **정원(border-radius:50%)** 으로
  바꿨다. 66x66(휴대폰 62x62). 로고 없는 팀의 `FOOTBALL CLUB` 잔글씨는 원 안에서
  비좁아 지웠다. 미리보기 썸네일도 동그라미로 맞췄다.
- `ImageCropper`(`app/screens.tsx`) 를 새로 만들었다. 그림을 고르면 **바로 올리지 않고**
  동그란 틀 안에서 끌어 옮기고 막대로 크기를 맞춘 뒤 저장한다.
  보이는 그대로 512x512 정사각으로 잘라 올린다.
  - 짧은 쪽을 틀에 맞추는 것을 기본 배율로 삼아 **어떻게 옮겨도 빈 곳이 생기지 않는다.**
  - 옮길 수 있는 범위는 그릴 때마다 다시 계산해 가둔다(`pos=clamp(...)`).
    값을 고치는 `useEffect` 를 쓰지 않아 렌더가 겹치지 않는다.
  - 손가락·마우스 모두 포인터 이벤트 하나로 받는다(`touch-action:none`).

### 브라우저 확인이 남았다 (다음 세션에서 먼저 할 것)

로컬 개발 서버가 불안정해져(포트 점유, `kill` 이 셸까지 끊김) 끝내지 못했다.
**확인 절차** — 팀 로고 업로드는 **팀이 승인된 상태**여야 열린다:

1. `pnpm dev` 로 서버를 띄운다(이미 떠 있으면 그대로 쓴다).
2. 로컬 운영자가 이미 있으면 `setupOwner` 가 409 가 난다. 그때만
   로컬 시험 DB에서 운영자 기록을 지운다(운영 데이터 아님):
   `.wrangler/state/v3/d1/miniflare-D1DatabaseObject/*.sqlite` 에서
   `DELETE FROM entities WHERE kind='settings' AND id='settings:owner'`
   후 `UPDATE state_revision SET version=version+1 WHERE id=1`.
3. 가입 → `setupOwner`(코드는 `.dev.vars` 의 `OWNER_SETUP_CODE`) →
   `createTeam` → `approveTeam` → 화면에서 MY → 팀 정보 수정 → 팀 로고 선택.
4. 확인할 것: 자르기 화면이 뜨는지 / 끌어서 옮겨지는지 / 막대로 커지는지 /
   저장 후 동그란 로고로 보이는지 / 정사각형이 아닌 그림도 빈 곳 없이 채워지는지.
   시험용 그림은 `/tmp/claude-0/wide.png`(900x300) 같은 가로로 긴 것을 쓴다.

### 로고 원본 파일 — 사용자 결정: "글꼴까지 원본 그대로"

채팅으로 보낸 그림은 **파일로 받을 수 없다**(볼 수만 있다). 그래서 지금 로고는
코드로 다시 그린 것이다(Arial Black + skewX). 사용자가 원본 파일을 쓰기로 했으므로
**사용자가 GitHub `public/` 에 직접 올려주기를 기다리는 중이다.**
올라오면 할 일:
- `public/icon-512.png`·`icon-192.png` 교체
- 원본으로 `icon-512-maskable.png` 다시 생성(안전 영역 = 가운데 80% 원)
- `public/favicon.svg`, `SplashMark`, `BrandMark` 를 원본 파일 기준으로 교체
- 머리말·첫 화면은 SVG 가 아니라 이미지가 되므로 크기·선명도 재확인 필요

### 머리말 로고가 잘리던 것과 옆 글자 제거 (T40)

- **원인**: 로고 글자에는 `skewX(-13)` 이 걸려 있다. 기울이면 글자가 **왼쪽으로 밀리는데**,
  머리말 마크의 `viewBox` 를 글자에 적은 x 값(118)에 맞춰 `88 120 400 310` 으로
  잡아두어서 **왼쪽 58px 이 잘려 있었다**(TEAM 의 T, KICK 의 K).
- 브라우저에서 실제 범위를 쟀다: x 29.7~462.2, y 120.5~414.0.
  `viewBox` 를 `22 112 448 310` 으로 고쳤다(여백 8).
- 사용자 요청대로 **로고 옆 글자(TEAMKICK / 팀킥)를 지웠다.** 글자가 없어졌으니
  마크 자체가 브랜드 역할을 하도록 키웠다(데스크톱 116x80, 휴대폰 62x43).
  CSS 크기 비율은 `viewBox` 비율(1.445)과 맞췄다. 어긋나면 둘레에 빈 띠가 생긴다.
- 테스트로 고정했다. 초록 띠의 위치는 계산으로 정확히 구하고(변형 공식),
  글자까지 포함한 전체 범위는 브라우저에서 잰 값을 기준으로 둔다.
  예전 viewBox 로 되돌리는 변이가 실제로 잡힌다.

### 앱 아이콘 재확인 (T40)

- `icon-192.png`·`icon-512.png` 는 **보내준 그림과 맞다.** 잘린 곳 없다.
- 다만 **안드로이드는 홈 화면 아이콘을 원·둥근네모로 잘라낸다.** 지금 아이콘은
  글자가 폭의 85%를 차지해 그대로 쓰면 잘린다. 안전 영역(가운데 80% 원) 안에
  들어가는 `icon-512-maskable.png` 를 따로 만들어 manifest 에 `purpose:"maskable"`
  로 넣었다. 잰 값: 가장 먼 모서리가 중심에서 198.8px / 안전 반지름 204.8px.
- manifest 에 적힌 아이콘 파일이 실제로 있는지도 테스트가 확인한다.

### 이미지 업로드가 안 되는 경우 (조사만, 코드 변경 없음)

막힐 수 있는 곳이 **세 군데**고 각각 다른 메시지가 화면에 그대로 뜬다.

1. 저장소 미연결 → "이미지 저장소가 아직 연결되지 않아 사진을 올릴 수 없어요"
   (`GET /api/app` 의 `storageReady:false`. `.openai/hosting.json` 에 `"r2":"BUCKET"`
   이 있으므로 배포 환경에서 R2 바인딩이 붙었는지 확인해야 한다.
   운영자로 로그인한 상태의 `/api/health` 에 `storageReady` 가 나온다.)
2. **팀이 아직 운영자 승인 대기** → "현재 이용 가능한 팀이 아니에요"
   (`app/api/image/route.ts` 가 `teamOf(...)?.status==="active"` 를 요구한다.)
3. 주장이 아님 → "팀 로고는 주장만 바꿀 수 있어요"

**어느 쪽인지 화면 메시지로 구분된다.** 사용자에게 메시지를 물어봐야 한다.

### /api/health 에 version 과 소셜 준비 여부를 넣는다 (T39)

- **내 안내가 틀렸다.** 배포 확인 방법으로 `/api/health` 의 `version` 을 보라고 했는데
  **그 라우트는 `version` 을 준 적이 없다.** `googleReady`·`naverReady` 도 없었다
  (그 둘은 `GET /api/app` 에만 있었다). 배포한 쪽이 맞게 지적했다.
- 고침: `version` 은 누구나 보는 부분에 넣었다(앱 화면 아래에도 이미 적혀 있어
  비밀이 아니고, 이게 없으면 배포 확인 수단이 반쪽이다).
  `googleReady`·`naverReady` 는 **운영자에게만** 보이는 부분에 넣었다
  (무엇이 켜져 있는지는 공격자에게 쓸모 있는 지도가 된다).
- 로그인하지 않은 사람에게 보이는 것은 `build`·`version`·`database` 뿐이다.
  주장이어도 운영자가 아니면 같다. 테스트로 고정했고, 운영자 확인을 빼는 변이가
  실제로 잡히는 것을 확인했다.

### 2026-09-17 배포 확인됨

`/api/health` 가 `{"build":"2026-09-17-faster-saves","database":true}` 를 돌려줬다.
**구글·네이버 실제 로그인은 아직 확인하지 못했다.** 사용자 확인 대기.

### 저장할 때 서버를 두 번 다녀오던 것을 한 번으로 (T38)

사용자 제보: "웹 안에서 로딩이 뭔가 길다."

- **원인**: 화면이 저장(`POST /api/app`)을 마친 뒤 곧바로 `GET /api/app` 을 한 번 더
  불러 전체 상태를 다시 읽고 있었다. **그런데 저장 응답에는 이미 새 상태가 들어
  있었다.** 즉 왕복 한 번이 통째로 낭비였고, 그동안 버튼은 계속 잠겨 있었다.
- **고침**: 저장 응답을 그대로 화면에 반영한다(`applySaved`).
  응답에 없는 값(로그인한 사람, 외부 연동 준비 여부)은 저장으로 바뀌지 않으므로
  기존 값을 남긴다. 통째로 갈아끼우면 로그인 표시가 사라진다.
  계정·운영자 상태가 바뀌는 `setupOwner`·`closeAccount` 만 예전처럼 다시 읽는다.
- 명령에 팀이 없는 경우(알림 읽음 등) 보던 팀이 바뀌지 않도록 `viewTeam` 을 함께
  보낸다. **명령의 대상 팀이 우선**이고, `viewTeam` 은 보는 기준일 뿐 권한과 무관하다
  (속하지 않은 팀을 보내도 아무것도 열리지 않는 것을 테스트로 고정).
- **측정(로컬)**: 저장 한 번에 44ms → 22ms (**절반**).
  네트워크 지연이 붙는 휴대폰에서는 차이가 더 크다.
  왕복 150ms 기준 344ms → 172ms, 왕복 300ms 기준 644ms → 322ms.
- 브라우저에서도 확인: 팀 등록 신청 시 `/api/app` 호출이 **POST 한 번뿐**이고
  화면에 신청한 팀이 바로 나타난다.

### 팀 로고를 매번 다시 받아오던 것 (T38)

- 이미지 주소는 올릴 때마다 새로 만들어지고 같은 주소의 내용은 바뀌지 않는다.
  그런데 보관 기간이 5분이라 화면을 옮길 때마다 다시 받아왔다.
- **팀 로고**는 로그인한 사람이면 누구나 보는 것이라 7일 보관으로 바꿨다.
- **선수 사진**은 팀을 나가면 더 보이면 안 되므로 5분 그대로 두었다.
  (속도보다 이쪽이 중요하다. 바꾸려면 사용자 결정이 필요하다.)

### 첫 화면 2초를 되돌림 (T36 취소)

사용자 결정: **2초는 그대로 유지한다.** 나중에 이 자리에 광고가 들어간다.
데이터가 먼저 준비돼도 줄이지 않는다. 화면을 누르면 바로 넘어가는 것은 그대로.

그 과정에서 **진짜 버그를 하나 찾아 고쳤다**: 첫 화면은 React 의 Layout 안에도
들어 있어서, 눌러서 건너뛰면 지워졌다가 **React 가 붙는 순간 되살아나** 결국 2초를
채웠다. React 가 붙는 동안만 지켜보다가 되살아나면 다시 치우도록 했다.

### 아직 남은 느린 곳 (고치지 않음)

- `load()` 가 요청마다 `entities` 전체를 읽는다. 256행 5ms / 5천행 96ms /
  5만행 897ms / 20만행 4678ms. 지금 양에서는 문제가 아니지만 팀이 늘면 손봐야 한다.
- 이미지 한 장을 볼 때마다 권한 확인을 위해 `load()` 가 한 번 돈다.
  팀 로고는 위 캐시로 거의 사라지지만 선수 사진은 5분마다 다시 돈다.
- 화면 묶음이 356KB(압축 103KB)다. 나눠 싣는 것은 아직 손대지 않았다.

### 구글·네이버 로그인 (T35) — 코드 작성 완료 / 로컬 검증 완료 / **실제 로그인 미검증**

- 제공자 설정을 `lib/social.ts` 한 곳에 모았다(인가 주소·토큰 주소·프로필 주소·
  범위·응답 읽는 법). `lib/social-route.ts` 가 두 제공자의 콜백을 같은 흐름으로
  처리하고, `app/api/google/route.ts`·`app/api/naver/route.ts` 는 이를 부른다.
- 계정은 `(provider, provider_id)` 로 묶는다(`accounts_provider_unique`).
  제공자가 늘 때마다 열을 만들지 않기 위해서다. 카카오만 있던 시절의 `kakao_id`는
  `backfillAccounts()` 가 앱이 뜰 때 한 번 옮긴다(여러 번 돌아도 안전).
- 위조 요청 방지: 콜백 전에 무작위 `state` 를 쿠키에 넣고 돌아올 때 대조한다.
  어긋나면 로그인하지 않고 `/?social=...` 로 돌려보낸다.
- **구글에는 이메일을 요청하지 않는다**(`scope: openid profile`).
  받는 것은 회원 식별자와 이름뿐이다. 네이버도 같다.
- 소셜 계정은 비밀번호로 로그인할 수 없다(테스트로 고정).
- `GET /api/app` 이 `googleReady`·`naverReady` 를 돌려주고, 키가 없으면
  로그인 화면에 버튼을 띄우지 않는다. **눌러도 안 되는 버튼을 만들지 않는다.**
- **확인하지 못한 것**: 이 환경은 `accounts.google.com`·`nid.naver.com` 에 닿을 수
  없다(프록시가 CONNECT 거부). 실패 경로(키 없음·state 불일치·토큰 없음)만 확인했다.
  **성공 경로는 배포 환경에서 직접 로그인해봐야 한다.**

### 이메일 가입 제거(T20)를 아직 하지 않은 이유

사용자가 "이메일 가입은 없애자"고 했지만 **순서를 지킨다.**
① 구글·네이버 로그인 추가(완료) → ② 배포 환경에서 실제로 되는지 확인(대기) →
③ 확인된 뒤 이메일 가입 제거. ②를 건너뛰고 지우면 **아무도 가입하지 못한다.**

### 첫 화면 로딩 대기 단축 (T36)

- 로고 화면이 데이터 준비와 상관없이 **항상 2초를 기다리고 있었다.**
- 고친 방식: 최소 0.7초(`SPLASH_MIN_MS`)는 보여주되, 데이터가 준비되면
  (`teamkick-ready` 이벤트) 바로 넘어가고, 늦어도 2초(`SPLASH_MS`)에서 끊는다.
- 측정(로컬, 헤드리스 Chromium): 보통 1070ms(예전 2000ms 고정),
  데이터가 3초 걸릴 때 2454ms, 눌러서 건너뛰기도 그대로 된다.

### 체감 속도를 재본 결과 (2026-09-17)

**배포 환경이 아니라 로컬에서 잰 값이다.** 실제 사용자 체감은 다를 수 있다.

- 배포 빌드 크기: JS 740KB(압축 243KB), 그중 화면 묶음 356KB(압축 103KB),
  CSS 163KB(압축 25.5KB).
- `GET /api/app`: 처음 0.73초, 이후 14~17ms.
- 가장 큰 자초한 지연은 위의 스플래시 2초 고정이었고 고쳤다.
- **남은 구조적 문제**: `load()` 가 요청마다 `entities` 전체를 읽는다.
  이전 측정으로 256행 5ms / 5천행 96ms / 5만행 897ms / 20만행 4678ms.
  지금 데이터 양에서는 문제가 아니지만 팀이 늘면 손봐야 한다(후속 과제).

### 개인정보 보호책임자 성명·앱 안 로고 (T32-2, T37)

- 사용자 확인을 받아 처리방침 12항에 `박재연 (서비스 운영자)` 를 넣었다.
- 머리말의 글자 `K` 대신 팀킥 로고(`BrandMark`)를 쓴다.

### 처리방침에 구글·네이버를 반영

약관 제3조와 처리방침 1·3·4·5·10·11항에 넣었다.
`lib/social.ts` 의 제공자 목록이 늘면 처리방침도 늘도록 테스트가 잡는다.
변이 4건(위탁에서 네이버 삭제 / 국외 이전에서 구글 삭제 / 이메일 미수집 문구 삭제 /
제공자만 늘리고 문서 방치)이 모두 실패로 잡히는 것을 확인했다.

### 이번 검증 결과 (실제 실행)

- `node --test tests/core.test.mjs` **96/96 통과**
- `node node_modules/typescript/bin/tsc --noEmit` 통과
- `pnpm lint` **77 오류 / 18 경고** — 오류 77건은 **시작 시점부터 있던
  `no-explicit-any`** 이며 이번 작업이 늘리지 않았다.

### 사용자가 해야 할 일

1. 배포 후 `https://teamkick.co.kr/` 에서 **구글로 시작하기 / 네이버로 시작하기** 를
   실제로 눌러 로그인되는지 확인.
2. 네이버 개발자센터에서 제공 항목을 **이름만**으로 두었는지 확인
   (처리방침에 그렇게 적었다).
3. 확인되면 알려주면 이메일 가입을 제거한다(T20).


### 운영자 초기 설정 코드를 환경변수로 (2026-09-15)

- **문제**: 사용자가 원본 코드를 모른다. 저장소에는 SHA-256 해시만 있고 원문이 없다.
  게다가 **저장소가 공개**라 짧은 코드는 해시를 거꾸로 맞혀볼 수 있다.
- **변경**: `OWNER_SETUP_CODE` 환경변수를 쓰는 길을 열었다.
  환경변수가 있으면 **그것만 인정**하고(예전 해시 경로는 쓰지 않는다),
  없으면 기존 해시로 확인한다. 비교는 길이·내용 모두 일정한 시간으로 한다.
  `/api/health` 에 `ownerSetupCode: env|built-in` 을 넣어 어느 쪽인지 확인할 수 있다.
- **효과**: 사용자가 코드를 직접 정하고, 그 값은 **저장소에 어떤 형태로도 남지 않는다.**
  공개 저장소의 해시 노출 문제도 함께 사라진다.
- **검증**: `node --test tests/core.test.mjs` **44/44 통과**. 환경변수가 있을 때
  그 값만 통과하고 일부 일치·공백 추가·빈 값·기존 해시 문자열은 모두 거부하는 것을
  확인했다. 환경변수 경로를 무력화하면 실패한다(43/44).
  (`if(!code)return false` 는 빼도 다른 비교가 막아주는 이중 방어라 테스트가 갈리지 않는다.)
  `tsc` 통과, `lint` 오류 77건. — **로컬 검증 완료**
- **사용자 설정 필요**: 환경변수 `OWNER_SETUP_CODE` 에 원하는 코드를 넣는다.
  **그 값을 대화나 문서에 적지 않는다.**

### 카카오 로그인 실패 이유를 화면에 보여준다 (2026-09-15)

- **문제**: 카카오 로그인이 실패하면 `/?kakao=<이유>` 로 돌려보내기만 하고
  **화면에서는 그 값을 읽지 않았다.** 실패해도 그냥 홈으로 돌아온 것처럼 보여
  원인을 알 수 없었다. 조용히 실패하는 경로를 남겨둔 것은 잘못이다.
- **변경**: 서버가 주소에 실어 보낸 이유를 화면 위 안내 띠로 보여주고, 보여준 뒤
  주소는 정리한다(`app/page.tsx`, `app/teamkick.tsx`).
- **함께 확인된 것**: 사용자가 카카오 동의 후 오류 없이 돌아왔다면 콜백은 성공한
  것이다. 그런데도 샘플 화면이 보이는 이유는 **로그인해도 팀이 없으면 샘플이
  유지되던 문제**(PR #10에서 고쳤고 아직 배포 전) 때문이다. 두 증상이 겹쳐
  "로그인이 안 된다"처럼 보였다.
- **검증**: `node --test` 42/42, `tsc` 통과, `lint` 오류 77건. — **로컬 검증 완료**

### 로그인해도 샘플 화면에 머물던 문제 (2026-09-15)

- **증상**: 카카오로 로그인해도 계속 샘플 화면이 뜨고 우측 상단이 "샘플 주장"으로 보였다.
  `우리 팀 시작하기` 를 눌러야만 실제 화면으로 넘어갔다.
- **원인**: 샘플 모드가 기본값 켜짐이고 **"이미 소속 팀이 있을 때만"** 꺼지도록 되어 있었다
  (`if(r.teamId||r.invitedTeam)setDemo(false)`). 방금 가입한 사람은 팀이 없으므로
  로그인해도 샘플에 남는다. **로그인한 사람에게 샘플을 먼저 보여줄 이유가 없다.**
- **변경**: `if(r.user)setDemo(false)` — 로그인한 사람은 바로 실제 화면으로 간다.
  로그아웃 상태의 둘러보기는 그대로 유지된다.
- **함께 막은 빈틈**: 운영자 초기 설정 링크는 `setupNeeded`(운영자가 아직 없음)일 때만
  보였다. **예전 버전에서 운영자가 등록됐고 그 계정이 지금 없으면** 링크가 숨겨져
  아무도 운영자가 될 수 없다. 서버가 `ownerMissing` 을 함께 내려주고, 그때도 링크를 보인다.
  (`setupOwner` 명령 자체는 이미 `ownerReset` 으로 이 경우를 허용하고 있었다.)
- **검증(브라우저)**: 로컬 개발 서버 + Chromium.
  - 로그아웃 상태에서는 샘플 띠가 보이고, **가입 직후 샘플 띠가 사라지며** 팀 만들기 화면과
    실명("박재연")이 나오는 것을 확인했다.
  - 운영자 항목을 존재하지 않는 계정으로 바꾸자 `ownerMissing:true` 와 함께
    **운영자 초기 설정 링크가 나타나고**, 되돌리자 다시 숨겨지는 것을 확인했다.
  — **로컬 검증 완료**
- `node --test` 42/42, `tsc` 통과, `lint` 오류 77건.

### 샘플 화면에서 구장 검색이 로그인을 요구하던 문제 (2026-09-15)

- **증상**: 로그인한 상태인데 샘플 화면에서 경기 만들기 → 구장 검색을 하면
  **"먼저 로그인해주세요"** 가 떴다.
- **원인**: 샘플(데모) 화면은 로그인 없이도 보이게 만들었는데, 그 안의 구장 검색만
  **실제 서버를 불렀다.** `/api/places` 는 로그인을 요구하므로 401 이 나고,
  화면에는 그 문구가 그대로 노출됐다. 샘플과 실제가 섞인 자리였다.
- **로그인했는데도 샘플이 보이는 이유**: 소속 팀이 없으면 샘플 화면이 유지된다
  (`refresh()` 후 `teamId` 나 초대가 있을 때만 실제 화면으로 넘어간다).
  로그인만 하고 팀이 없는 상태에서는 계속 샘플을 보게 된다.
- **변경**: 샘플에서는 구장 검색 입력칸을 감추고
  **"샘플에서는 구장 검색을 쓸 수 없어요. 구장 이름과 주소는 아래에 직접 입력해보세요."**
  로 바꿨다. 서버를 부르지 않는다. 기존 팀 탈퇴·회원 탈퇴를 샘플에서 감추는 것과 같은 방식이다.
- **검증(브라우저)**: 로컬 개발 서버 + Chromium 으로 샘플 화면에서 경기 만들기를 열어
  검색 입력칸이 **0개**, 안내 문구가 보이고, **`/api/places` 호출이 0회**인 것을 확인했다.
  구장 이름·주소 입력칸은 그대로 있어 직접 입력은 된다. — **로컬 검증 완료**
- 단순 UI 조건 변경이라 테스트는 늘리지 않았다(`CLAUDE.md` 4-8).
  `node --test` 42/42, `tsc` 통과, `lint` 오류 77건.

### 카카오 로그인 실동작 확인 — **외부 연동 검증 완료** (2026-09-15)

- **확인된 사실**: 배포 환경에서 **카카오 로그인이 실제로 동작한다**(사용자 확인).
  인증 왕복, Redirect URI, 계정 생성, 세션 발급이 모두 정상이다.
  이로써 **로그인 수단 두 가지(이메일·카카오)가 실환경에서 확인됐다.**
- **의미**: 네이버 사용자는 카카오로 들어오면 메일이 전혀 개입하지 않는다.
  네이버 메일 문제의 영향 범위가 **이메일 가입자의 비밀번호 찾기**로 좁아졌다.
- **함께 처리한 것(법적 준수)**: 처리방침이 이메일 가입만 전제로 쓰여 있어
  카카오에서 정보를 받는데 고지가 없었다. 약관 제3조와 처리방침 1·3·4항을 고쳐
  **카카오에서 받는 항목은 회원번호와 닉네임뿐이고, 이메일·비밀번호는 받지 않으며,
  서비스가 카카오로 정보를 보내지 않는다**는 점을 명시했다.
- **남은 일**: 탈퇴 시 카카오 **연결 끊기(unlink)** 는 아직 하지 않는다.
  지금은 우리 기록만 지우고 카카오의 "연결된 서비스" 목록에는 남는다.
  처리방침에 이용자가 직접 정리하는 방법을 안내해 두었다.
- **검증**: `node --test tests/core.test.mjs` 42/42, `tsc` 통과, `lint` 오류 77건.

### 카카오 로그인 (2026-09-15)

- **왜**: 네이버가 확인 메일을 받지 않아 이메일 경로가 막혔다. 카카오 로그인은
  그 문제를 **우회하는 것이 아니라 메일 자체를 쓰지 않게** 한다. 비밀번호가 없어
  재설정 메일도 필요 없다. 카카오 계정은 대개 본인 확인을 거친 계정이라
  **제재 실효성과 대량 생성 방지 면에서 이메일보다 강하다.**
- **변경 파일**: `lib/kakao.ts`(신규), `app/api/kakao/route.ts`(신규), `lib/auth.ts`,
  `lib/schema.ts`, `db/schema.ts` + `drizzle/0006_aspiring_harpoon.sql`,
  `app/api/app/route.ts`, `app/screens.tsx`, `app/teamkick.tsx`, `app/globals.css`
- **동작**: 로그인 화면의 **카카오로 시작하기** → 카카오 동의 → 우리 사이트로 복귀 →
  세션 발급. 이메일 가입도 그대로 남겨 두었다(카카오를 안 쓰는 사람 대비).
- **계정 처리**: `provider="kakao"`, `kakao_id` 저장. 이메일 열은 비울 수 없어
  **배달되지 않는 주소**(`kakao-<id>@teamkick.invalid`)로 자리만 채우고,
  그 주소로는 **어떤 메일도 보내지 않는다**(재설정·확인 모두 막았다).
  비밀번호는 빈 값이라 어떤 입력과도 맞지 않는다.
  카카오가 본인을 확인했으므로 `verified_at` 을 채워 **팀 활동이 막히지 않는다.**
- **위조 방지**: 시작할 때 임의의 상태값을 쿠키에 담고 콜백에서 대조한다.
  값이 다르면 진행하지 않는다.
- **DB 변경**: migration `0006`(`accounts.provider`, `accounts.kakao_id`).
  배포 시 적용이 필요하지만 `ensureSchema()` 가 알아서 만든다.
- **검증**: `node --test tests/core.test.mjs` **42/42 통과**. 계정 생성 형태,
  같은 사람 재로그인 시 계정이 하나인 것, 비밀번호로는 들어올 수 없는 것,
  자리표시 주소로 메일을 보내지 않는 것, 인증 주소에 키·콜백·상태값이 담기는 것,
  닉네임 동의가 없어도 막히지 않는 것을 확인했다. 메일 차단과 상태값 전송을
  각각 지우면 실패한다(41/42). `tsc` 통과, `lint` 오류 77건. — **로컬 검증 완료**
- **미검증**: 실제 카카오 인증 왕복. 사이트가 비공개라 카카오가 되돌려보낼 때
  ChatGPT 로그인 화면이 끼어들 수 있다. 공개 전환 후 확인이 필요하다.
- **사용자 설정 필요**: 카카오 개발자 콘솔에서 **카카오 로그인 활성화**와
  **Redirect URI 등록**(`https://<사이트주소>/api/kakao`). 동의항목은 **닉네임만**
  받으면 되고 이메일은 받지 않으므로 비즈앱 전환이 필요 없다.

### 가입 429의 정체와 제한값 조정 (2026-09-15)

- **증상**: Gmail 주소로 가입이 "처리하지 못했어요"로 실패했다. 서버 로그에는
  `POST /api/auth` **429**, 스택 트레이스 없음.
- **원인**: **요청 횟수 제한**이다. 코드 결함이 아니라 값이 빡빡했다.
  하루 동안 실패한 시도까지 모두 세어 접속 주소 기준 10회를 넘겼다.
- **화면 문구가 어긋난 이유**: `limit()` 은 JSON 으로 이유를 돌려주는데,
  화면에는 기본 문구가 떴다. 응답 본문이 JSON 이 아닐 때(앞단에서 막히는 경우)
  화면이 이유를 잃는다. 상태 코드로 안내하도록 고쳤다.
- **변경**
  - 가입 제한 10회/시간 → **30회/시간**. 국내 통신사 CGNAT 과 같은 장소에서
    팀원이 한꺼번에 가입하는 상황을 고려했다. `LIMITS` 를 내보내 테스트가
    값을 따라가게 했다(값만 바꿔도 테스트가 함께 움직인다).
  - 로그인·비밀번호 찾기·확인 메일 제한은 그대로 둔다.
  - 화면: 본문이 JSON 이 아니어도 429 면 "요청이 너무 잦아요", 그 밖에는
    응답 코드를 함께 보여준다.
- **즉시 우회**: 제한은 접속 주소별이라 **휴대폰 데이터로 접속하면 바로 된다.**
- **검증**: `node --test tests/core.test.mjs` **39/39 통과**. 제한값을 0으로 바꾸면
  테스트가 실패해 값과 검증이 함께 움직이는 것을 확인했다(38/39).
  `tsc` 통과, `lint` 오류 77건. — **로컬 검증 완료**

### 비밀번호 확인란 (2026-09-15)

- **왜**: 가입할 때 비밀번호를 잘못 치면 원인을 모른 채 로그인이 막힌다.
  복구 수단이 메일뿐이라 더 곤란하다.
- **변경**: 가입과 비밀번호 재설정 화면에 **비밀번호 확인** 입력칸을 넣고,
  두 값이 다르면 서버로 보내기 전에 막는다(`app/screens.tsx`).
  서버는 확인란을 받지 않는다. 클라이언트 입력 실수를 막는 장치일 뿐이다.
- **검증(브라우저)**: 로컬 개발 서버 + Chromium 으로 실제 화면을 확인했다.
  입력칸 구성이 `이름 · 이메일 · 비밀번호 · 비밀번호 확인 · 동의 2개` 로 나오고,
  서로 다르게 넣고 제출하면 **"비밀번호가 서로 달라요. 확인란을 다시 입력해주세요."**
  가 뜨며, 같게 맞추면 경고가 사라지고 진행된다. — **로컬 검증 완료**
- **덤으로 확인된 것**: 그 가입이 로컬 D1 에 실제로 저장됐고, 저장된 해시가
  **`pbkdf2$100000x6$`** 형식이다. 연쇄 해싱이 실제 가입 경로에서 동작한다.
  예전 `pbkdf2$600000$` 계정도 같은 표에 남아 있고 그대로 읽힌다.
  `ensureSchema()` 가 로컬 D1 에도 표 8개를 만들어 두었다.
- 단순 UI 변경이라 테스트는 늘리지 않았다(`CLAUDE.md` 4-8).
  `node --test` 39/39, `tsc` 통과, `lint` 오류 77건.

### 가입 성공 — **외부 연동 검증 완료** / 확인 메일 미도착 (2026-09-15)

- **확인됨**: `/api/health` 의 `build` 갱신과 `passwordHashing:"ok"` 를 사용자가 확인했고,
  **실제 배포 환경에서 회원가입이 성공했다.** PBKDF2 10만×6회 연쇄가 Workers 의
  반복 상한과 CPU 제한 안에서 동작한다. 계정 생성·세션 발급 경로가 처음으로
  실환경에서 검증됐다.
- **남은 증상**: 확인 메일이 오지 않는다. 가입 흐름은 **메일 실패가 가입을 막지 않도록**
  되어 있어(`signUp` 의 try/catch) 조용히 실패한 것으로 보인다.
- **가장 의심되는 원인**: `MAIL_FROM` 이 아직 `jyp7296@naver.com` 일 가능성.
  도메인을 사기 전에 안내한 값이 그대로 남아 있으면 Brevo 가 거부한다.
- **진단 추가**
  - `/api/health` 에 `mailFromDomain`(보내는 주소의 도메인)과 `mailAccount`
    (메일을 보내지 않고 키만 조회한 결과)를 넣었다.
  - 발송 거절 시 사용자 메시지에 **발송 서비스 응답 상태 코드**를 남긴다.
    원인을 좁히기 위한 최소 정보이고 개인정보는 담지 않는다.
- **함께 고친 결함**: 확인 메일 발송에 실패해도 **토큰 행이 남아 있었다.** 그러면
  재발송 간격 제한(3분)에 걸려 "조금 전에 보낸 메일을 먼저 확인해주세요"가 뜨는데
  실제로는 아무것도 보내지 않은 상태가 된다. 발송이 실패하면 토큰을 지우도록 고쳤다.
- **검증**: `node --test tests/core.test.mjs` **39/39 통과**. 도메인 추출, 키 없음,
  계정 조회 성공/401, 메일을 보내지 않고 확인하는 것, 400 응답 시 상태 코드 노출을
  확인했다. 도메인 확인과 키 확인을 각각 무력화하면 실패한다(37/38).
  발송 실패 시 토큰이 남지 않고 곧바로 재발송되는 것도 확인했다(정리를 빼면 실패, 38/39).
  `tsc` 통과, `lint` 오류 77건. — **로컬 검증 완료**

### 가입 실패의 진짜 원인: PBKDF2 반복 상한 (2026-09-15)

- **증상**: 표가 모두 생성됐는데도 가입이 "처리하지 못했어요"로 실패했다.
- **원인**: **Cloudflare Workers 는 PBKDF2 반복을 10만 회로 제한**한다. 넘기면
  `NotSupportedError: Pbkdf2 failed: iteration counts above 100000 are not supported`
  가 난다. 우리 코드는 OWASP 권고를 따라 **60만 회**를 썼다.
- **왜 지금까지 못 잡았나**: **Node 에는 이 제한이 없다.** 로컬 테스트 37개가 전부
  통과하는데도 배포 환경에서만 실패한다. 테스트가 잡을 수 없는 종류의 차이였다.
- **변경**: 10만 회를 **6회 이어 붙여** 작업량 60만 회를 유지한다. 각 회차의 출력을
  다음 회차의 입력으로 넣으므로 병렬로 줄일 수 없다(작업량이 실제로 6배).
  저장 형식은 `pbkdf2$100000x6$<소금>$<해시>` 이고, 예전 단일 회차 형식도 계속 읽는다.
- **재발 방지**: `ROUND_ITERATIONS` 이 10만을 넘지 않는지, 전체 작업량이 60만 이상인지
  테스트로 고정했다. 상한을 올리면 테스트가 실패한다.
- **진단 추가**: `/api/health` 가 실제 런타임에서 해싱을 한 번 돌려보고
  `passwordHashing` 으로 `ok` 또는 오류 문구를 돌려준다. 다음부터는 가입을 시도하지
  않고도 이 값으로 알 수 있다.
- **검증**: `node --test tests/core.test.mjs` **37/37 통과**. 해시 생성·검증 왕복,
  원문 미저장, 상한 준수, 작업량 유지를 확인했다. 상한을 60만으로 되돌리면
  테스트가 실패하는 것까지 확인했다(35/37). `tsc` 통과, `lint` 오류 77건.
  — **로컬 검증 완료**
- **미검증**: 실제 Workers 에서 6회 연쇄가 CPU 제한 안에 드는지는 배포 후
  `/api/health` 의 `passwordHashing` 으로 확인해야 한다.

### 배포·스키마 실환경 확인 (2026-09-15) — **외부 연동 검증 완료**

`GET /api/health` 응답을 사용자가 확인해 보내주었다. 배포 환경에서 처음으로
사실이 확인된 항목들이다.

```
build: 2026-09-15-health   database: true   setupError: null
tables: entities, state_revision, write_guards, accounts, sessions,
        password_resets, rate_limits, email_verifications  — 모두 true
mailReady: true   storageReady: true   placeSearchReady: true
```

- **확인됨**: 최신 코드가 서빙 중이고, `ensureSchema()` 가 **실제 D1 에서 표 8개를
  만들었다.** migration 도구 없이 앱이 스키마를 준비하는 방식이 동작한다.
  R2 바인딩과 카카오·Brevo 환경변수도 들어가 있다.
- **아직 아닌 것**: `mailReady`·`storageReady`·`placeSearchReady` 는 **설정이 있다는
  뜻일 뿐** 실제 발송·업로드·검색 성공을 확인한 것이 아니다. 각각 실제 사용으로
  확인해야 한다.
- **여기까지 오는 데 걸린 것**: 코드 반영·빌드·게시가 각각 별개였고, 보고와 실제가
  두 번 달랐다. 진단 경로를 먼저 만들었어야 했다. 앞으로 배포 관련 판단은
  `/api/health` 응답으로만 한다.

### 진단 경로와 준비 실패 구분 (2026-09-15)

- **왜**: 반영 후에도 가입이 같은 문구("처리하지 못했어요")로 실패했다. 이 문구는
  `AppError` 가 아닌 예외의 기본값이라 **어디서 막혔는지 알 수 없다.**
  표가 없는 것인지, 표 생성이 실패한 것인지, 반영 자체가 안 된 것인지 구분해야 한다.
- **변경**
  - `ensureSchema()` 실패를 **"데이터베이스를 준비하지 못했어요"** 로 구분한다.
    SQL 원문과 오류는 서버 로그에만 남긴다.
  - `GET /api/health` 추가. `build` 표시, DB 바인딩 여부, **표 8개의 실제 존재 여부**,
    준비 중 오류, 메일·저장소·장소검색 설정 여부를 돌려준다.
    개인정보와 키는 담지 않는다.
  - `BUILD` 상수로 **어느 시점 코드가 배포됐는지** 눈으로 확인할 수 있게 했다.
- **쓰는 법**: 사이트 주소 뒤에 `/api/health` 를 붙여 연다.
  `build` 가 기대한 값이면 반영된 것이고, `tables` 로 표 생성 여부를 바로 알 수 있다.
- **검증**: `node --test tests/core.test.mjs` **36/36 통과**. 표를 모두 지운 뒤
  상태 확인을 호출해 8개가 다시 만들어지는 것을 확인했다. 문장을 깨뜨리면
  실패하는 것까지 확인했다(34/36). `tsc` 통과, `lint` 오류 77건. — **로컬 검증 완료**
- **미검증**: 실제 배포 환경에서의 응답은 확인하지 못했다.

### 표를 앱이 직접 만들도록 (2026-09-15)

- **왜**: 배포 환경(ChatGPT Sites)에 migration 을 실행할 도구가 없다. GitHub 연결도
  없어 수동 반영만 가능하고, D1 콘솔 접근 경로도 확인되지 않았다. 그 결과 코드는
  배포됐는데 표가 없어 가입이 실패했다.
- **변경**: `lib/schema.ts` 의 `ensureSchema()` 가 필요한 표·인덱스·열을 만든다.
  인증·앱 라우트 세 곳에서 요청 처리 전에 호출하며, 격리 인스턴스마다 한 번만 돈다.
  전부 `IF NOT EXISTS` 이고 `ADD COLUMN` 은 이미 있으면 나는 오류만 넘기므로
  여러 번 실행해도 안전하다.
- **드리프트 방지**: `drizzle/` 를 모두 적용한 DB 와 `STATEMENTS` 로 만든 DB 의
  표·열·인덱스·CHECK 를 비교하는 테스트를 넣었다. migration 을 추가하고
  `lib/schema.ts` 를 고치지 않으면 테스트가 실패한다.
- **검증**: `node --test tests/core.test.mjs` **35/35 통과**. 문장 하나를 빼면
  (표 누락·열 누락 각각) 비교 테스트가 실패하는 것을 확인했다(34/35).
  두 번 실행해도 스키마가 같은 것도 확인했다. `tsc` 통과, `lint` 오류 77건.
  — **로컬 검증 완료**
- **미검증**: 실제 D1 에서 동작하는지는 반영 후 확인해야 한다.
- `scripts/manual-migrate.sql` 은 그대로 둔다. 이 경로가 실패할 때의 대비책이다.

### 배포 1차 시도 결과와 준비 미완료 안내 (2026-09-15)

- **배포는 됐다.** 사이트에 새 회원가입 화면(팀킥 회원가입, 동의 2개)이 뜬다.
  ChatGPT Sites 는 GitHub 과 연결되어 있지 않아 수동 반영했다.
  (**2026-09-19 정정**: 여기 적힌 "`편집` 화면에서" 는 오해를 부른다. 사용자가 보는
  Sites 편집 화면에는 **환경 변수밖에 없고 코드 파일 목록이 없다**. 사용자 확인.
  아래 "배포 경로" 절을 볼 것.)
- **가입은 실패한다.** "처리하지 못했어요"가 뜬다. `AppError` 가 아닌 예외의
  기본 문구이며, 원인은 **migration 미적용**이다. 첫 DB 접근인 `limit()` 의
  `rate_limits` 조회부터 `no such table` 로 떨어진다.
- 앞선 안내가 부정확했다: "데이터 연결을 준비하고 있어요"는 **DB 바인딩 자체가 없을 때**
  나온다. 바인딩은 있고 **테이블만 없으면** 일반 실패 문구가 뜬다.
- **변경**: `setupIncomplete()` 로 `no such table`/`no such column` 을 구분해
  세 라우트 모두에서 **"데이터베이스 준비가 아직 끝나지 않았어요"** 로 안내한다.
  스키마 내용은 응답에 담지 않고 서버 로그에만 남긴다.
- **검증**: `node --test tests/core.test.mjs` **34/34 통과**. `entities` 테이블을
  지운 상태로 API 를 호출해 503 과 안내 문구를 확인했고, 판별 함수를 무력화하면
  실패하는 것까지 확인했다(33/34). `tsc` 통과, `lint` 오류 77건. — **로컬 검증 완료**

### T14 이메일 실재 확인 (2026-09-15)

- **변경 파일**: `lib/auth.ts`, `lib/model.ts`, `app/api/auth/route.ts`,
  `app/api/app/route.ts`, `app/page.tsx`, `app/teamkick.tsx`, `db/schema.ts`,
  `drizzle/0005_mixed_mach_iv.sql`(신규), `LEGAL.md`, `tests/core.test.mjs`
- **왜**: 지금까지는 존재하지 않는 주소로도 가입이 됐다. 비밀번호 재설정이 메일에
  의존하므로, 주소가 실재하지 않으면 계정을 영영 복구할 수 없다.
- **사용자에게 달라지는 동작**
  - 가입하면 **확인 메일**이 간다. 링크(`/?verify=...`)를 누르면 확인된다.
  - 확인 전에는 **팀 만들기와 팀 가입 신청이 막힌다.** 로그인·둘러보기는 된다.
  - 확인 전에는 화면 위에 안내 띠와 **확인 메일 다시 보내기** 버튼이 보인다.
- **안전 장치**: 토큰은 **서버에 해시만** 저장하고 메일로만 원문이 나간다.
  **24시간·1회용**이고, 재발송은 3분 간격을 둔다. 이미 확인한 계정에는 보내지 않는다.
  탈퇴 시 확인 기록도 함께 지운다.
- **메일 미설정 시**: 가입은 그대로 되고 확인 메일만 나가지 않으며, **확인을 요구하지도
  않는다**(`!mailReady()||verified`). 인증 수단이 없는데 기능을 막지 않기 위해서다.
  가입 응답의 `verificationSent` 로 실제 발송 여부를 그대로 돌려준다.
- **DB 변경**: migration `0005`(`email_verifications`, `accounts.verified_at`).
  **배포 시 적용이 필요하다.**
- **검증**: `node --test tests/core.test.mjs` **33/33 통과**. 가입 시 발송, 확인 전 차단,
  확인 후 통과, 1회용, 24시간 만료, 재발송 간격, 확인된 계정 재발송 안 함, 메일 미설정 시
  미발송을 확인했다. 확인 요구·1회용 표시·재발송 간격을 각각 지우면 테스트가 실패하는 것까지
  확인했다(32/33). `tsc --noEmit` 통과, `pnpm lint` 오류 77건(시작 시점과 동일).
  — **로컬 검증 완료**
- **미검증**: 실제 메일 도착과 링크 클릭 경로는 배포 환경에서 확인해야 한다(외부 API 차단).

### 가입·로그인 남용 제한 (2026-09-15)

- **변경 파일**: `lib/auth.ts`, `app/api/auth/route.ts`, `db/schema.ts`,
  `drizzle/0004_famous_vector.sql`(신규), `lib/legal.ts`, `LEGAL.md`, `tests/core.test.mjs`
- **왜**: 공개 전환 전에 필요한 항목이었다. 가입 한 번에 PBKDF2 60만 회가 돌아서
  제한이 없으면 가입 요청만으로 서버를 소모시킬 수 있고, 가짜 계정도 막지 못한다.
- **제한 값**(`lib/auth.ts` 의 `LIMITS` 한곳에 모음):
  가입 10회/시간, 로그인 20회/15분, 비밀번호 찾기 5회/시간 — 접속 주소별.
  계정 단위 보호(10회 실패 잠금, 재설정 3분 쿨다운)는 그대로 유지된다.
- **개인정보**: 접속 주소 원문을 저장하지 않고 **SHA-256 해시만** 둔다. 새 구간을
  열 때 만료된 기록을 함께 지운다. 처리방침 1항(수집 항목)·3항(보유 기간)에 고지했다.
- **접속 주소는 `cf-connecting-ip`** 로만 읽는다. 클라이언트가 보낸 헤더는 믿지 않는다.
  주소를 알 수 없으면 제한을 걸지 않고 계정 단위 보호에 맡긴다.
- **DB 변경**: migration `0004`(`rate_limits`). **배포 시 적용이 필요하다.**
- **검증**: `node --test tests/core.test.mjs` **31/31 통과**. 횟수 초과 차단,
  주소별·작업별 분리, 시간 경과 후 해제, 주소 미상 시 통과, 원문 미저장, 만료 기록 삭제를
  확인했다. 제한 검사와 만료 삭제를 각각 지우면 테스트가 실패하는 것까지 확인했다(30/31).
  `tsc --noEmit` 통과, `pnpm lint` 오류 77건(시작 시점과 동일). — **로컬 검증 완료**
- **미검증**: 실제 Cloudflare 환경에서 `cf-connecting-ip` 가 채워지는지는 배포 후 확인해야 한다.
- **알려진 한계**: 국내 통신사는 여러 사용자가 같은 IP 를 쓸 수 있다(CGNAT).
  같은 장소에서 팀원이 한꺼번에 가입하면 제한에 걸릴 수 있다. 값은 `LIMITS` 에서 바로 조정한다.

### 메일 회신 주소 분리 (2026-09-15)

- **변경 파일**: `lib/mail.ts`, `tests/core.test.mjs`
- **왜**: 발송 서비스는 **자기가 확인한 주소**로만 보낼 수 있어서 보내는 주소와
  문의처가 달라진다. 받는 사람이 그대로 답장하면 아무도 못 읽는 주소로 간다.
- **변경**: 모든 발송 메일에 회신 주소(`replyTo` / `reply_to`)를 `lib/legal.ts` 의
  문의처로 붙였다. Brevo·Resend 두 형식 모두 적용했다.
- **검증**: `node --test tests/core.test.mjs` 29/29 통과. 요청 본문을 가로채
  보내는 주소·받는 주소·회신 주소·HTML 이스케이프·발송 실패 처리를 확인했다.
  회신 주소를 빼면 그 테스트가 실패하는 것까지 확인했다(28/29). — **로컬 검증 완료**
- **미검증**: 실제 메일 도착 여부는 여기서 확인할 수 없다(외부 API 차단).

### 발신 도메인 제약 (확인 필요, 2026-09-15)

- 네이버는 **네이버 메일이 아닌 서비스에서 `naver.com` 을 발신 주소로 쓰는 것을
  막는 정책**을 두고 있다. 따라서 `MAIL_FROM` 을 `jyp7296@naver.com` 으로 두면
  받는 쪽에서 거부될 수 있다. 문의처(받는 주소)로 쓰는 것은 문제없다.
- Brevo 무료 플랜은 **주소 하나를 메일 링크 클릭으로 확인**해 쓸 수 있어 도메인이
  없어도 시작은 된다. 다만 gmail·naver 같은 무료 도메인은 SPF/DKIM 인증을 붙일 수
  없어 도착률이 떨어진다. — **미검증**(공식 문서 기준, 실제 발송으로 확인 못 함)
- 실사용자 발송 전에 **도메인 확보**가 필요하다. 카카오 앱 도메인과 같이 쓸 수 있다.

### 메일 서비스 정리와 문의처 반영 (2026-09-15)

- 메일은 **Brevo 기본**(도메인 불필요, 무료 하루 300건), Resend 도 선택 가능하게 했다.
  키가 들어 있는 쪽을 자동으로 쓴다(`lib/mail.ts`).
- 문의처 `jyp7296@naver.com` 을 약관·개인정보처리방침에 넣고, 메일 발송 위탁 고지를 추가했다.
- **확인된 제약**: 이 개발 환경은 외부 API 접속이 막혀 있어(카카오·메일 모두 CONNECT 403)
  외부 연동의 성공 경로를 여기서 검증할 수 없다. 앞선 세션에서 "카카오가 403을 줬다"고
  적은 것은 프록시 차단이었고 키 문제가 아니었다.

### T13 비밀번호 재설정 (2026-09-15)

- **변경 파일**: `lib/mail.ts`(신규), `lib/auth.ts`, `db/schema.ts` + `drizzle/0003`,
  `app/api/auth/route.ts`, `app/api/app/route.ts`, `app/page.tsx`,
  `app/teamkick.tsx`, `app/screens.tsx`, `tests/core.test.mjs`
- **사용자에게 달라지는 동작**: 로그인 화면의 **비밀번호를 잊으셨나요?** 에서
  이메일을 넣으면 재설정 링크를 보낸다. 링크로 들어가면 바로 새 비밀번호를 정하고
  로그인된다.
- **안전 장치**: 토큰은 **서버에 해시만** 저장하고 메일로만 원문이 나간다.
  **1시간·1회용**이고, 재설정하면 **다른 기기 세션을 모두 끊는다.**
  가입 여부를 응답으로 알려주지 않고, 3분 안에는 다시 보내지 않는다.
- **메일 미설정 시**: 로그인 화면에서 링크 자체를 감추고 요청도 분명히 거절한다.
  토큰도 남기지 않는다.
- **메일 서비스**: Resend(무료 월 3,000건·하루 100건). `lib/mail.ts` 한 겹으로
  감싸 다른 서비스로 바꾸기 쉽게 했다. 필요한 환경변수는
  `RESEND_API_KEY`, `MAIL_FROM`.
- **DB 변경**: migration `0003`(`password_resets`). 배포 시 적용이 필요하다.
- **미검증**: 실제 키가 없어 **메일이 실제로 도착하는지는 확인하지 못했다.**
  토큰 발급·검증·만료·재사용 차단·세션 무효화는 테스트와 브라우저로 확인했다.

### T12 법적 준수 기반 (2026-09-15)

- **변경 파일**: `lib/legal.ts`(신규), `LEGAL.md`(신규), `db/schema.ts` +
  `drizzle/0002`(동의 시각), `lib/auth.ts`, `lib/model.ts`,
  `app/api/app/route.ts`, `app/screens.tsx`, `tests/core.test.mjs`
- **사용자에게 달라지는 동작**
  - 가입할 때 **약관·개인정보 수집·이용 동의**와 **만 14세 이상** 확인을 받는다.
    체크하지 않으면 가입되지 않고 동의 시각을 저장한다.
  - 가입 화면에서 **약관과 개인정보처리방침 전문**을 열어볼 수 있다.
  - 설정에서 **회원 탈퇴**를 할 수 있다. 계정·비밀번호·세션·알림이 삭제되고
    팀에서 나간 상태가 되며 선수 사진 연결이 끊긴다.
    과거 경기·출석·기록은 팀의 공동 기록으로 남는다.
  - 주장을 맡은 팀이 있으면 인계 전에는 탈퇴할 수 없다.
- **고친 결함**: 데모 모드에서 회원 탈퇴를 누르면 샘플 데이터에만 반영되고
  실제 계정은 남아 있었다. 기존 팀 탈퇴와 같이 데모에서는 버튼을 숨긴다.
- **DB 변경**: migration `0002`(`accounts.agreed_at`). 배포 시 적용이 필요하다.
- **남은 일**: `LEGAL.md` 3절 — 약관 변호사 검토, 문의처 이메일 기입,
  개인정보 보호책임자 지정, 위탁 업체 명시, 탈퇴 후 이름 처리 방침 확정.

### T03 구장명 검색 (2026-09-15)

- **변경 파일**: `lib/places.ts`(신규), `app/api/places/route.ts`(신규),
  `lib/model.ts`(좌표 저장), `app/screens.tsx`(VenuePicker),
  `app/api/app/route.ts`, `.gitignore`
- **사용자에게 달라지는 동작**: 경기 만들기와 일정 변경에서 구장명을 검색해
  후보를 고르면 **구장 이름·주소·좌표가 자동으로 채워진다.** 같은 이름은 도로명과
  지번 주소로 구분한다. 그 팀이 최근에 쓴 구장은 **자주 쓰는 구장**으로 바로 고른다.
- **검색이 안 될 때**: 키가 없거나 카카오가 응답하지 않아도 일정 등록은 막히지 않는다.
  이유를 보여주고 구장 이름·주소는 계속 직접 입력할 수 있다.
- **키 관리**: `KAKAO_REST_KEY` 환경변수로 받고 소스에 넣지 않는다.
  로컬은 `.dev.vars`(git 제외). 검색 결과에 없는 주소는 만들어내지 않는다.
- **미검증**: 실제 키가 없어 **검색 성공 응답은 확인하지 못했다.**
  키 없음(503)·잘못된 키(카카오 403 → 안내 메시지)·비로그인(401) 경로는 확인했다.

### T04 투표 마감 지정과 참여 알림 발송 (2026-09-15)

- **변경 파일**: `lib/model.ts`, `app/screens.tsx`, `tests/core.test.mjs`
- **사용자에게 달라지는 동작**
  - 경기를 만들 때 **투표 마감**을 직접 정할 수 있다. 비우면 경기 시작 시각이다.
  - 우리 팀 경기 설정에서도 마감을 바꿀 수 있고, 범위를 벗어나면 이유를 알려준다.
  - 참여 현황에서 **미응답자에게 알림 보내기**로 직접 알림을 보낸다.
    (이전 계획이던 24시간 전 자동 알림은 사용자 결정으로 대체)
- **고친 결함 2건**
  - 팀 설정에서 지난 시각을 고르면 "저장했어요"가 뜨는데 마감은 그대로였다
    (클라이언트가 값을 조용히 버림). 이제 서버가 판단해 이유를 알려준다.
  - 경기 만들기에서 마감 검증이 `s.games.push` 뒤에 있어, 거부돼도 경기가
    남을 수 있었다(테스트가 이 문제를 잡았다). 검증을 상태 변경 앞으로 옮겼다.
- **스케줄러**: 출석 초안은 읽을 때 계산되고 마감·완료는 저장 시점에 처리되므로
  배경에서 돌릴 예약 작업이 없어졌다. 실제 푸시를 붙일 때 다시 검토한다.

### T09 팀 로고·선수 사진 업로드 (2026-09-15)

- **변경 파일**: `lib/images.ts`(신규), `app/api/image/route.ts`(신규),
  `lib/model.ts`(setTeamLogo·setMemberPhoto), `app/screens.tsx`(ImageField),
  `app/teamkick.tsx`(Crest 로고·PlayerPhoto), `app/globals.css`,
  `.openai/hosting.json`(`r2: "BUCKET"` 선언)
- **사용자에게 달라지는 동작**: 팀 정보 수정에서 **팀 로고**를, 내 선수 정보와
  주장의 선수 정보 수정에서 **선수 사진**을 올릴 수 있다. 올린 이미지는 팀 배지와
  선수 명단·기록 순위에 바로 보이고 새로고침·다른 기기에서도 유지된다.
- **저장·검증**: 파일은 R2에, 앱 상태에는 키만 저장한다. 선언한 종류를 믿지 않고
  실제 바이트로 PNG·JPEG·WEBP를 확인하고 2MB로 제한한다. 브라우저에서 긴 변
  512px로 줄여 보낸다. 같은 자리에 새로 올리면 이전 파일을 지운다.
- **권한**: 팀 로고는 주장만 올리고 로그인 이용자에게 보인다. 선수 사진은 본인 또는
  주장만 올리고 **그 팀의 활동 팀원에게만** 보인다. 명령은 키가 그 팀·그 팀원 경로
  아래인지 확인해 남의 이미지를 가리킬 수 없다.
- **저장소 미연결 시**: 앱은 그대로 동작하고 화면은 업로드 대신 안내를 보여주며
  서버는 업로드를 분명히 거절한다. 성공한 것처럼 보이지 않는다.
- **`<img>` 사용**: lint가 `next/image` 사용을 권하지만(경고 3건), 이 이미지는
  권한 검사를 거치는 자체 API로 내려주는 비공개·소용량 파일이라 최적화 로더를
  거치지 않는 편이 맞다고 판단했다.

### T01 자체 회원가입 (2026-09-15)

- **변경 파일**: `lib/auth.ts`(신규), `app/api/auth/route.ts`(신규),
  `db/schema.ts` + `drizzle/0001_dizzy_slayback.sql`(accounts·sessions 테이블),
  `app/api/app/route.ts`(신원 교체·운영자 재설정 판정),
  `app/screens.tsx`(AuthPanel·로그아웃), `app/teamkick.tsx`(로그인 화면 연결),
  `lib/model.ts`(운영자 재설정), `tests/core.test.mjs`(테스트 5건)
- **사용자에게 달라지는 동작**: ChatGPT 계정 없이 **이메일과 비밀번호로 가입·로그인**한다.
  ChatGPT 로그인 버튼은 화면에서 사라졌다.
- **보안 처리**: 비밀번호는 PBKDF2-HMAC-SHA256 600,000회(OWASP 권고)로만 저장하고
  원문은 남기지 않는다. 세션 토큰은 HttpOnly·Secure·SameSite=Lax 쿠키로 주고
  서버에는 **토큰의 SHA-256만** 저장한다. 30일 만료, 로그아웃 시 삭제,
  로그인 10회 실패 시 15분 잠금, 실패 응답은 가입 여부를 구분해주지 않는다.
  계정·세션은 `entities` 문서 저장소가 아니라 별도 테이블에 둔다.
- **DB 변경**: migration `0001` 추가. 배포 시 적용이 필요하다.
- **주의(운영자)**: 기존 운영자 설정이 ChatGPT 계정 ID에 묶여 있으면 새 계정이
  운영자가 될 수 없다. 저장된 운영자 ID의 계정이 없을 때에 한해 설정 코드를 다시
  제시하면 재설정되도록 했다. 살아 있는 운영자 계정은 코드를 알아도 가져갈 수 없다.

### T00 기준 확보 · T02-1 모바일 명단 표 수정

- **변경 파일**: `app/globals.css` 1줄 (`.gap-grid>*,.form-grid>*{min-width:0}`)
- **사용자에게 달라지는 동작**: 선수 이름이 긴 팀원이 있으면 우리팀 선수 명단 표가
  화면 폭을 밀어내 휴대폰에서 페이지 전체가 옆으로 벌어지고 마지막 '관리' 열이
  화면 밖에 남았다. 이제 페이지는 화면 폭을 유지하고 넘치는 표만 가로로 스크롤된다.

### T06 용병 모집·마감 (신규 기능)

- **변경 파일**
  - `lib/model.ts` — `guests` 컬렉션 추가, 명령 7종
    (`openGuests`/`closeGuests`/`applyGuest`/`withdrawGuest`/
    `approveGuest`/`rejectGuest`/`cancelGuest`), `visibleState`에
    `guests`·`myGuests`·`guestListings` 노출, 경기 취소 시 모집 종료
  - `app/screens.tsx` — 매칭 화면 용병 탭, 경기 상세의 모집 현황·신청자 승인,
    용병 모집·신청 모달
  - `app/teamkick.tsx` — `GuestBadge`("용병 모집 중"/"용병 마감"), 경기 목록 표시,
    소속 팀 없는 사용자의 매칭 화면 접근 허용
  - `tests/core.test.mjs` — 테스트 4건 추가
- **사용자에게 달라지는 동작**
  주장·운영진이 예정 경기에 용병 모집 인원을 열 수 있고, 다른 사용자가
  매칭 > 용병에서 신청한다. 승인 인원이 정원에 도달하면 **서버가 자동으로 마감**하고
  남은 대기자를 종료 처리하며, 일정 목록·경기 상세에 "용병 마감"이 함께 표시된다.
  용병은 팀원이 되지 않고 팀 내부 정보도 보이지 않는다.
- **DB 변경 없음**: `entities` 단일 문서 테이블 구조를 그대로 쓰므로
  migration이 필요하지 않다.

---

## 검증

- **실행 명령**
  - `pnpm install --frozen-lockfile` → exit 0
  - `node --test tests/core.test.mjs` → **20 pass / 0 fail**
  - `node node_modules/typescript/bin/tsc --noEmit` → 통과
  - `pnpm dev` + 실제 브라우저(Chromium) 조작
- **결과**: 위 전부 통과. 수정 후 재실행해도 동일.
- **수동 확인 (390×844 모바일 에뮬레이션)**
  - 수정 전: 문서 폭 **456px**(화면 390px) — 레이아웃이 옆으로 벌어짐,
    표 컨테이너 `scrollWidth == clientWidth`라 가로 스크롤 불가
  - 수정 후: 문서 폭 **390px** 유지, 표 컨테이너 **316/353** 으로 가로 스크롤 동작
  - 홈·일정·매칭·기록·우리팀 5개 화면 모두 가로 넘침 없음
  - 1440 / 768 / 390 / 360 폭 확인 — 데스크톱·태블릿 레이아웃 변화 없음
  - 팀 등록 신청 → 운영자 승인 → 주장 권한 → 팀원 관리 모달 → 중첩 확인창
    '확인' → 팀원 제외가 서버에 반영됨
  - 내 선수 정보에서 포지션 변경·저장 시 **역할은 그대로 유지**
    (주장·일반 팀원 두 시점 모두)
  - 일반 팀원 화면에서 팀원 초대·관리·공지 작성 버튼이 **노출되지 않음**
- **용병 기능 검증 (브라우저 + 테스트)**
  - 모집 개설(정원 2) → 다른 사용자 3명 신청 → 승인 2건 → **서버가 자동 마감**,
    남은 대기자 모집 종료, 목록에서 사라짐
  - 일정 목록과 경기 상세에 **"용병 마감"** 동일 표시, 상세에 "승인 2명 / 모집 2명"
  - 마감 후 승인 요청 → **HTTP 409 거부**, 승인 인원 2명 유지
  - 소속 팀 없는 사용자로 용병 목록 확인·신청 성공, 신청 후에도 팀원이 되지 않음,
    팀 명단·경기·공지 미노출
  - **변이 테스트**로 테스트가 실제 실패를 잡는지 확인:
    자동 마감 제거 → 3건 실패 / 팀원 중복 신청 차단 제거 → 1건 실패 /
    권한 검사 제거 → 1건 실패. (마감 상태 검사만 제거하면 정원 검사가 막아
    통과하는데, 두 검사가 서로를 커버하는 의도된 이중 방어다.)
  - `pnpm lint` 오류 수가 기준과 동일한 **77건** 유지(새로 늘리지 않음)
- **자체 회원가입 검증 (브라우저 + 테스트)**
  - 가입 API 응답 **335ms**(PBKDF2 600,000회 포함)로 실제 Workers 런타임에서 동작
  - 로그인 화면 표시 → 가입 → 세션 쿠키(HttpOnly·SameSite=Lax, JS로 읽기 불가) →
    새로고침·새 탭에서 로그인 유지 → 팀 등록 신청 성공 → 로그아웃(쿠키 제거) →
    잘못된 비밀번호 거부 → 재로그인 후 이전 데이터 그대로 확인
  - 비밀번호 원문·세션 토큰 원문이 DB에 저장되지 않음을 테스트로 확인
  - 운영자 재설정: 계정이 없을 때만 열리고, 살아 있는 계정은 뺏기지 않음을
    로컬 API 요청으로 양방향 확인
  - 변이 테스트로 재설정 조건·설정 코드 검사가 실제 회귀를 잡는 것을 확인
- **미실행 항목과 이유**
  - 실기기 검증: 기기가 없다 → **실기기 미검증**
  - 실기기 푸시 수신: 기능 자체가 없다 → **미구현**
  - 운영자 초기 설정: 설정 코드 원문이 저장소에 없어 실제 등록은 **미검증**
  - 비밀번호 재설정(메일 발송): 외부 메일 서비스가 필요해 **미구현**
  - 배포 검증: 배포 권한·환경 밖이다 → 미실행

---

## 막힌 작업

### 1. 사용자 제보가 재현되지 않음 (T02-2)

- **원인**: "우리팀 팀원관리의 내 선수 정보 수정에서 주장만 선택되고 다른 선택이
  되지 않는다"를 재현하지 못했다.
- **시도한 해결**
  1. `내 선수 정보` 모달의 포지션 선택(GK/DF/MF/FW)을 실제 클릭 → 4개 모두
     선택되고 저장까지 정상.
  2. **주장 시점**과 **일반 팀원 시점** 두 계정 상태로 각각 확인 → 둘 다 정상.
  3. 화면 전체에서 선택 UI를 조사한 결과 "주장"이 **선택지로 들어간 곳은 없다.**
     주장/운영진/팀원은 명단 표의 **읽기 전용 역할 표시**이고,
     역할 변경은 주장이 '관리'에서 버튼으로 한다(선택 목록이 아님).
  4. 다만 긴 이름이 있을 때 **'관리' 열이 화면 밖으로 밀리는 결함**은 실재했고
     이번에 수정했다. 제보가 이 증상일 가능성이 있다.
- **필요한 사용자 결정·정보**: 아래 "사용자 확인 필요" 참고.
- **다른 작업에 미치는 영향**: 없음. 다른 작업은 계속 진행 가능.

### 2. 일반 회원 직접 가입 (T01)

- **원인**: 요구사항 B는 "박재연은 일반 회원으로 직접 가입"이지만,
  현재 로그인은 **ChatGPT Sites 계정 전용**이다(`app/chatgpt-auth.ts`).
  앱에는 자체 회원가입·비밀번호 저장이 전혀 없다.
- **영향 범위**: 이 결정이 배포 방식, 서버 비용, 앱스토어 제출 가능성,
  실제 팀원 초대 방식에 모두 영향을 준다. 임의로 정하면 되돌리기 어렵다.
- **다른 작업에 미치는 영향**: 용병 모집·푸시·구장 검색은 이 결정과 무관하게
  진행할 수 있다.

### 3. 이미지 업로드 (T09)

- **원인**: `hosting.json`의 `r2: null` — 파일 저장소가 연결되어 있지 않다.
- **필요한 설정**: R2 바인딩. 없으면 요구사항 J를 구현할 수 없다.

---

## 공개 전환 전에 처리해야 할 것 (자체 회원가입의 알려진 공백)

- ~~가입 요청 속도 제한이 없다.~~ **해결됨** — 접속 주소별 제한을 넣었다
  (가입 10회/시간, 로그인 20회/15분, 비밀번호 찾기 5회/시간).
- ~~비밀번호 재설정이 없다.~~ **해결됨**(T13) — 메일로 재설정 링크를 보낸다.
  `BREVO_API_KEY`·`MAIL_FROM` 이 있어야 동작하고, 없으면 화면에서 감춰진다.
- ~~이메일 실재 확인(인증 메일)이 없다.~~ **해결됨**(T14) — 가입 시 확인 메일을 보내고
  확인 전에는 팀 생성·가입 신청을 막는다. 메일 키가 없는 환경에서는 확인을 요구하지 않는다.

## 다음 행동

1. **T14 이메일 인증** — 가입한 주소가 실제인지 확인한다. 같은 메일 키를 쓴다.
2. **T08 실제 휴대폰 푸시** — 앱 내 알림함은 동작한다. 기기 푸시는 미구현.
3. **T11 출시 준비 정리** — 웹 실행·모바일 사용·네이티브 빌드·스토어 제출 구분.

**광고 영역(T10)은 사용자 결정으로 보류한다.**


---

## 결정과 가정

- **새로 결정한 사항**
  - 모바일 표 결함은 컴포넌트가 아니라 `globals.css` 한 줄로 고쳤다.
    `components/ui/table.tsx`(shadcn 원본)를 건드리지 않아 다른 화면의
    표 4곳에 같은 효과가 적용되고 업스트림과의 차이도 생기지 않는다.
  - `no-explicit-any` 77건은 고치지 않았다. 기존 코드 전반의 스타일이라
    수정 시 변경 범위가 과도해지고, 요구사항과 무관하다.
- **확인이 필요한 가정** (`PROJECT_CONTEXT.md` 4절 표에 누적)
  - ASM-01: 용병 승인 취소로 빈자리가 생기면 모집을 다시 연다. **구현함.**
  - ASM-02: 투표 마감 24시간 전 알림은 일정 변경 시 재계산한다. (T04에서)
  - ASM-03: 용병 모집·승인 권한을 주장뿐 아니라 **운영진에게도** 부여했다.
  - ASM-04: 용병은 팀원 명단·출석·선수 통계에 넣지 않는다.
  - ASM-05: 소속 팀이 없는 사용자도 매칭 화면의 용병 탭에 들어갈 수 있게 했다.

---

## 네이버 메일 도달

**확인된 사실**: 같은 발송 경로로 **Gmail 에는 도착하고 네이버에는 오지 않는다**
(받은편지함·스팸함 모두). 앱 → Brevo 구간은 정상이다.

**영향**: 네이버 사용자는 확인 메일을 못 받아 **팀 생성·가입 신청을 할 수 없고**,
비밀번호를 잊어도 되찾을 수 없다. 이용자 상당수가 네이버를 쓰므로 공개 전 필수 과제다.

**진행 결과(2026-09-15)**: SPF 를 추가한 뒤에도 네이버는 여전히 받지 않았다.
재발송은 성공(화면에 "다시 보냈어요")했지만 도착하지 않았다.
**SPF 만으로는 부족하다. 2단계가 필요하다.**
단, SPF 가 실제로 퍼졌는지는 확인하지 못했다(이 개발 환경은 DNS 조회가 막혀 있다).
`https://dnschecker.org/#TXT/teamkick.co.kr` 로 먼저 확인할 것. — **미검증**

**중요도 조정**: 카카오 로그인이 들어오면서 네이버 메일의 급한 쓰임이 줄었다.
네이버 사용자는 카카오로 들어오면 메일이 전혀 개입하지 않는다. 메일이 필요한 곳은
**이메일로 가입한 사람의 비밀번호 찾기**뿐이다. 여전히 고쳐야 하지만 최우선은 아니다.

### 1단계 — SPF 레코드 추가 (가비아 DNS)

```
타입 TXT / 호스트 @ / 값 v=spf1 include:spf.brevo.com ~all / TTL 3600
```

- 기존 `@` TXT(brevo-code)를 지우지 말고 **추가**한다. TXT 는 여러 개 공존한다.
- Brevo 는 기술적으로 SPF 가 필요 없다(봉투 발신이 Brevo 도메인). 그러나
  **네이버 기준과 2단계 신청 요건 때문에 필요하다.**
- 반영 후 네이버 계정으로 확인 메일 재발송을 눌러 도착 여부를 본다.
  **이것만으로 해결될 수 있다.**

### 2단계 — ~~KISA 화이트도메인 등록~~ **이 제도는 없어졌다**

**KISA 화이트도메인 서비스는 2024년 6월 14일 신규 접수를 끝으로 종료됐다.**
자체 메일서버 대신 상용 메일을 쓰는 추세라 실효성이 떨어졌다는 것이 종료 사유다.
사이트 전체 메뉴에도 해당 항목이 없다(사용자가 확인).
**앞선 세션에서 이 제도를 대안으로 안내한 것은 낡은 정보였다.**

남은 방법은 **네이버에 직접 발송 도메인을 신청**하는 것이다(네이버 고객센터 경로).
정확한 절차는 아직 확인하지 못했다. — **미검증**

**우선순위**: 카카오 로그인이 들어와 네이버 사용자는 메일 없이 쓸 수 있다.
이 항목은 **이메일로 가입한 사람의 비밀번호 찾기**에만 영향을 준다. 급하지 않다.

### 건드리지 말 것

- **DMARC 는 `p=none` 그대로 둔다.** 섣불리 올리면 메일이 전부 사라질 수 있다.
- 브랜딩 기록의 서브도메인은 `mail` 이다. `www` 는 웹 주소용으로 비워 두었다.

---

## 배포 설정 체크리스트 (환경변수·도메인)

이 표가 "무엇을 어디에 넣는가"의 단일 기준이다. 값 자체는 여기 적지 않는다.

| 이름 | 쓰이는 곳 | 없으면 | 넣는 곳 |
|---|---|---|---|
| `KAKAO_REST_KEY` | 구장명 검색, 카카오 로그인 | 검색·카카오 로그인 모두 막힘 | 배포: Site 설정 / 로컬: `.dev.vars` |
| `KAKAO_CLIENT_SECRET` | 카카오 로그인 토큰 발급 | **토큰 발급이 401 로 실패**(기본 활성화) | 같음 |
| `OWNER_SETUP_CODE` | 운영자 초기 설정 | 저장소의 기존 해시로만 확인(원문을 모르면 등록 불가) | 같음 |
| `BREVO_API_KEY` | 비밀번호 재설정 메일(`lib/mail.ts`) | 비밀번호 찾기 숨김, 토큰도 안 만듦 | 같음 |
| `MAIL_FROM` | 보내는 사람 표시 | 위와 같음(키만으론 동작 안 함) | 같음 |

- `MAIL_FROM` 형식: `팀킥 <no-reply@teamkick.co.kr>`
  **`naver.com`·`gmail.com` 금지** — 위 "발신 도메인 제약" 참고.
- `.dev.vars` 는 `.gitignore` 에 있어 저장소에 올라가지 않는다. 키를 코드 파일에
  넣지 않는다.
- 배포 쪽 환경변수 3개는 **ChatGPT Sites 프로젝트 설정 → 환경 변수**에 넣었다(사용자 확인).
  키 두 개는 시크릿으로, `MAIL_FROM` 은 비밀값이 아니다.
- 같은 화면에 `VAPID_*` 3개가 들어 있으나 **이 저장소에는 이를 읽는 코드가 없다.**
  기기 푸시(T08)는 미구현이므로 지금은 아무 동작도 하지 않는다.

## 2026-09-16 MY 화면 — 내 기록·문의·공지 (T23)

`우리팀` 탭을 **`MY`** 로 바꾸고, 팀 화면 아래에 팀을 넘어선 "내 것"들을 붙였다.

- **내 전체 기록** — `myTotals(s,userId)`. `summaries` 와 달리 **팀 경계를 넘어**
  합산한다. 팀을 옮기거나 여러 팀에서 뛰어도 내가 쌓은 것은 하나로 보여야 한다.
  출석이 확정된 경기만 출석률에, 기록이 확정된 것만 골·도움에 넣는다.
- **프로필 관리** — 기존 `profile` 모달을 연다(사진·이름·등번호·포지션).
- **1:1 문의** — 새 컬렉션 `inquiries`. 팀이 아니라 **운영자에게 직접** 보낸다.
  본인과 운영자만 본다. 운영자만 답변하고, 답변하면 문의자에게 알림이 간다.
  답변을 기다리는 문의가 3건 쌓이면 더 받지 않는다(한 사람이 창구를 막지 않게).
- **공지사항** — 새 컬렉션 `announcements`. **팀 공지(`notices`)와 다르다.**
  운영자만 올리고 지우며 모두에게 보인다. 버전을 함께 남길 수 있다.
- **버전 표기** — `lib/version.ts` 의 `APP_VERSION`. 화면 맨 아래에 `팀킥 v1.1.0`.

**팀이 없어도 MY 는 보여야 한다.** 팀이 없으면 온보딩 화면이 모든 뷰를 가로채는데,
`view==="team"` 일 때는 온보딩 아래에 `MyHub` 를 함께 붙였다. 안 그러면 팀에 들어가기
전에는 문의·공지·버전을 볼 길이 없다. (처음 구현에서 이걸 놓쳐 화면 확인 때 드러났다.)

**백업 호환**: 컬렉션이 두 개 늘었다. `readBackup` 이 옛 파일을 거절하지 않도록,
**나중에 생긴 표가 없으면 비어 있던 것으로 본다.** 다만 뼈대(`users`·`teams`·
`members`·`games`·`sides`·`settings`)가 빠진 파일은 그대로 거절한다. 손상된 파일로
전부 지우면 안 된다.

검증(로컬): 테스트 **67/67**(6개 추가). 변이 검사 8종 모두 테스트가 잡았다 —
문의 답변 권한 / 공지 작성 권한 / 공지 삭제 권한 / 남의 문의 노출 /
확정 안 된 기록 합산 / 남의 기록을 내 것으로 / 문의 도배 제한 / 뼈대 없는 백업.
Playwright 11단계: 탭 이름, 다섯 구역 표시, 버전 문자열, 문의 등록 → 운영자 답변 →
상태 변경, 공지 올리기 → 건수 반영 → 내용·버전 표시, 모바일 가로 넘침 없음.

## 2026-09-17 처리방침 보완 · 팀 찾기 · 경기 날짜 (T32-T34)

### 처리방침 (T32)

공식 작성지침 2025.4 와 대조해 **빠져 있던 6개를 채웠다**. `lib/legal.ts`.

9 파기 절차와 방법 / 10 국외 이전 / 11 쿠키 등 자동 수집 장치 /
12 개인정보 보호책임자 / 13 권익침해 구제방법 / 14 처리방침의 변경.
5항 위탁 업체를 **실명**(OpenAI·Cloudflare·Brevo·카카오)으로 바꾸고,
1항에 1:1 문의 내용과 기기 알림 구독을, 3항에 보관 기간 숫자를 넣었다.

**테스트가 항목 누락을 잡는다.** 제목 줄을 정규식으로 확인하므로 본문 어딘가에
같은 낱말이 있다고 통과하지 않는다. 위탁 절과 국외 이전 절을 따로 떼어
사업자 이름이 양쪽에 다 있는지 본다. 보관 기간 숫자가 `KEEP` 과 어긋나도 잡는다.
**초안 표기를 지우면 실패한다** — 아직 변호사 검토 전이다.

**보호책임자 성명은 비워 두었다**(`(서비스 운영자)`). 실명을 넣을지 사용자 결정.

### 팀 찾기 (T33)

`oz` 로 `FCOZ` 를 찾을 수 없었다. 온보딩 화면의 검색이 `.includes(query)` 로
**대소문자를 가리고 있었다.** `teamMatches()` 로 규칙을 한곳에 모으고
소문자로 맞춰 비교한다. 로고(`Crest`)와 지역·형식·실력을 함께 보여준다.
이름을 정확히 몰라도 알아볼 수 있어야 한다.
모달(`findTeam`)에도 같은 검색을 붙였다.

### 경기 날짜 (T34)

- 시작·종료 아래에 **`9월 20일 (일) 10:00 – 12:00`** 처럼 사람이 읽는 말로 보여준다.
  `datetime-local` 만으로는 무슨 요일인지 알 수 없다.
- **시작을 정하면 종료가 2시간 뒤로 따라온다.** 미리 채워둔 기본값도 자동으로
  취급해 따라 움직인다(처음 구현에서 이걸 놓쳐 화면 확인에서 드러났다).
  **사용자가 종료를 직접 바꾸면 그 값을 지킨다.**
- 일정 변경(`changeGame`)은 요일 표시만 붙였다. 기존 경기의 길이를 2시간으로
  덮어쓰면 안 되기 때문이다.

검증(로컬): 테스트 **87/87**(4개 추가). 변이 검사 12종 모두 테스트가 잡았다.
Playwright 9단계: 검색(대소문자·이름 가운데 글자·지역·0건), 로고·지역 표시,
종료 자동 계산, 요일 표시, 직접 바꾼 종료 유지.

## 2026-09-16 기기 푸시 알림 (T31)

`VAPID_*` 환경변수만 있고 읽는 코드가 0건이었다. 이제 실제로 보낸다.

**내용을 싣지 않는 푸시**를 쓴다. 두 가지 이유다.
1. 내용을 실으려면 기기별 키로 암호화(RFC 8291)해야 하는데, **이 개발 환경에서는
   실제 푸시 서버로 보내볼 수 없어 암호화가 맞는지 확인할 방법이 없다.**
2. 알림 내용을 구글·애플 푸시 서버에 맡기지 않는 편이 개인정보에도 낫다.
서비스 워커가 깨어나 `/api/app` 에서 최신 알림을 읽어 보여준다.

- `lib/push.ts` — VAPID 토큰(ES256) 서명, 구독 저장·삭제, `wakeDevices()`.
  환경변수 이름은 `VAPID_PUBLIC_KEY`/`VAPID_PRIVATE_KEY`/`VAPID_SUBJECT` 를
  우선 보고, `VAPID_PUBLIC`·`VAPID_PRIVATE` 도 받는다(사이트마다 다를 수 있다).
  개인키는 **32바이트 원본과 PKCS#8 둘 다** 받는다.
- `public/sw.js` — `push`·`notificationclick` 핸들러. 이미 열린 창이 있으면
  새 창을 띄우지 않는다. 읽기에 실패해도 일반 문구라도 띄운다
  (브라우저가 푸시마다 알림 하나를 요구한다).
- `app/api/push/route.ts` — 공개키 알려주기, 구독 등록·해제.
- `app/notify.tsx` — MY 화면의 `기기 알림` 켜기/끄기.
  **안 되는 이유를 화면에 적는다**(아이폰 홈 화면 미추가, 브라우저 차단,
  키 미설정). 눌러도 아무 일이 없는 것처럼 보이면 안 된다.
- 새 표 `push_subs` (+ `drizzle/0007_push_subs.sql`). `entities` 가 아니라 따로 둔다 —
  매 요청마다 읽히면 안 되고, 기기 자격증명이라 백업에 담기면 안 된다.
- 보내는 시점: `POST /api/app` 의 **커밋이 끝난 뒤**. 이번 저장으로 새로 생긴
  알림을 받은 사람만, **본인은 빼고** 깨운다. 한 번에 20기기, 각 4초 제한.
  실패해도 저장을 되돌리지 않는다.
- 탈퇴하면 `closeAccount` 가 구독도 지운다.

검증(로컬): 테스트 **84/84**(5개 추가). **VAPID 서명을 WebCrypto 로 실제 검증**한다
(받는 주소가 토큰에 들어가는지, 만료가 24시간 이내인지, 서명이 공개키로 맞는지).
구독 넣기·빼기·같은 기기 중복 방지·주인 바뀜, 죽은 구독(410) 정리,
키가 없으면 아무 데도 보내지 않기, 본인 제외.
변이 검사 6종 모두 테스트가 잡았다.

**확인하지 못한 것 — 중요**
- **실제 푸시 전달.** 이 환경은 푸시 서버(FCM 등)에 닿을 수 없다.
  게다가 **크롬은 시크릿 모드에서 Push API 를 아예 막는다**(Playwright 가 시크릿이다).
  그래서 브라우저에서 진짜 구독을 만들 수 없었다. 대신 눌렀을 때 조용히 넘어가지
  않고 이유를 보여주는 것과, 서버 쪽 등록·해제·검증이 도는 것을 확인했다.
- **실기기 확인이 반드시 필요하다.** 배포 후 갤럭시·아이폰에서 직접 켜 보고
  다른 계정으로 경기를 만들어 알림이 오는지 봐야 한다.
- 환경변수 이름이 맞는지. `/api/health` 의 `pushReady` 로 확인할 수 있다(운영자만).

## 2026-09-16 팀 정보(형식·실력·활동 때) 수정 (T26)

**사용자 제보**: "MY 에 11인제, 일요일 오전은 선택하는 것도 없어."

**확인한 문제**: `팀 만들기` 에는 `주 경기 형식`·`활동 요일` 칸이 있는데
**`팀 정보 수정` 에는 그 칸이 없었고 `editTeam` 서버 코드도 받지 않았다.**
한 번 정하면 영영 고칠 수 없었다. `level`(팀 실력)은 더 심해서 매칭 화면에
`t?.level` 로 보여주면서 **어디에서도 정할 수 없었다**(항상 기본값).

- `editTeam` 이 `format`·`days`·`level` 을 받는다.
- 형식과 실력은 `FORMATS`·`LEVELS` 목록으로 **서버에서 검증**한다.
  매칭 화면에 그대로 보이는 값이라 목록 밖 값이 들어오면 화면이 깨진다.
- **값이 안 오면 지금 값을 지킨다.** 배포 중에 예전 화면을 열어둔 사람이
  이름만 보낼 수 있는데, 그때 형식·실력이 빈 값으로 덮이면 안 된다.
- `LEVELS` 를 만들기 전 저장된 값("중" 등)이 있다. `levelOf()` 가 화면에서
  다듬어 보여준다(하→초급, 중→중급, 상→상급). 저장된 값은 건드리지 않고
  **수정할 때 비로소 새 값으로 바뀐다.**
- 팀 패널에 실력도 함께 보여준다. 고칠 수 있으면 보여야 한다.

검증(로컬): 테스트 **74/74**(5개 추가). 변이 검사 5종 모두 테스트가 잡았다 —
형식 검증 제거 / 실력 검증 제거 / 값 없을 때 지금 값 지키기 제거 /
주장 확인 제거 / 예전 값 다듬기 제거.
Playwright 5단계: 만들기·수정 양쪽에 세 칸이 있고, 팀 패널에 표시되며,
고친 값이 반영되고 예전 값이 사라지는 것까지 확인했다.

**아직 하지 않은 것**: 팀원이 주장에게 "팀 정보가 틀렸어요" 라고 알리는 기능.
지금은 주장·운영진만 고칠 수 있다. 필요하면 `correctRequest`(기록 정정 요청)와
같은 방식으로 붙일 수 있다.

## 2026-09-16 첫 화면이 번쩍이던 것 고침 (T25)

**증상**: 앱을 열면 팀킥 UI 가 잠깐 보였다가 그 위로 검은 첫 화면이 덮였다.

**원인**: 첫 화면을 React 컴포넌트(`useEffect`)로 띄우고 있었다. React 가 붙기
전까지는 첫 화면이 존재하지 않으므로, 브라우저가 먼저 앱 화면을 한 번 그린다.

**고침**: 첫 화면을 **HTML 에 그대로 넣었다**(`app/layout.tsx` 의 `<body>` 첫 요소).
서버가 보내는 HTML 에 이미 들어 있어 **첫 페인트부터 보인다.**

- 이미 본 사람은 `<head>` 의 작은 스크립트가 `<html data-splash="skip">` 을 붙이고,
  CSS 가 즉시 숨긴다. 화면을 칠하기 전에 정해지므로 두 번째 방문에도 번쩍임이 없다.
- 치우는 일도 그 스크립트가 한다(2초 뒤, 또는 누르면 바로).
- 자바스크립트가 막혀 있으면 치울 방법이 없으므로 `<noscript>` 로 아예 숨긴다.
- `app/splash.tsx` 에는 **모양과 값만** 남겼다(`SplashMark`, `SPLASH_MS`, `SPLASH_ID`,
  `SPLASH_KEY`). 띄우고 치우는 일은 layout 이 맡는다.

검증(Playwright 8단계): 자바스크립트 묶음을 900ms 늦춰 "React 붙기 전" 순간을
길게 만든 뒤 확인했다 — 그 시점에 이미 첫 화면이 있고(①), 화면 전체를 덮으며(②),
배경이 검정이고(④), 2초 뒤 사라지고(⑤), 두 번째 방문에는 표시되지 않으며(⑦),
누르면 바로 넘어간다(⑧). 그 시점 화면을 찍어 로고만 보이는 것을 확인했다.

## 2026-09-16 로고 교체 + 첫 화면(스플래시) (T22)

### 로고

사용자가 로고 이미지를 보내주었으나 **이 세션은 그림 파일을 저장소로 받을 수 없다.**
그래서 보내준 그림을 보고 **SVG 로 다시 그렸다**(검은 바탕, 기울인 굵은 흰 글씨
TEAM/KICK, 초록 평행사변형 밑줄). `public/icon-192.png`·`icon-512.png`·`favicon.svg`
를 교체했고 `manifest.webmanifest` 의 `background_color` 를 `#000000` 으로 맞췄다.

**원본과 정확히 같지는 않다.** 정확한 파일이 필요하면 사용자가 GitHub 웹에서
`public/` 에 직접 올리면 된다(파일 이름만 맞추면 코드는 그대로 동작한다).

### 첫 화면(스플래시)

`app/splash.tsx`. 앱을 열면 검은 바탕에 로고를 2초 보여준다.

- **나중에 광고가 들어갈 자리를 지금부터 잡아 두었다.** `.splash-ad` 가 위에 있고
  `data-ad="true"` 를 주면 광고 자리가 펼쳐지면서 로고가 아래로 내려간다
  (사용자가 보낸 세 번째 그림 배치). 그때 화면을 갈아엎지 않기 위해서다.
- 로고는 파일로 불러오지 않고 **SVG 로 직접 그린다.** 첫 화면인데 파일을 받는
  동안 빈 화면이 보이면 안 된다.
- 한 번 본 사람에게는 `sessionStorage` 로 다시 띄우지 않는다. 새로고침에는 안 뜨고
  탭을 새로 열면 다시 뜬다. 매번 2초를 쓰게 하지 않기 위해서다.
- 화면을 누르면 바로 넘어간다.

검증(Playwright 10단계): 첫 방문 표시, 로고 250×250(화면 390px 의 64%),
배경 검정, 가운데 정렬, 약 2초 뒤 자동으로 사라지고 앱이 보임,
새로고침 때는 안 뜸, 새 탭에서는 다시 뜸, 광고 모드에서 정렬이 바뀌고
광고 자리가 펼쳐짐. 두 배치 모두 화면을 찍어 사용자가 보낸 그림과 대조했다.

## 2026-09-16 설치 안내 고침 + 확정 매칭 배치 (T21)

### 설치 안내가 갤럭시에서 안 떴다 — 원인 두 가지

1. **설치 창을 놓치고 있었다.** 브라우저는 화면을 그리기 **전에**
   `beforeinstallprompt` 를 띄운다. React 가 붙은 뒤(`useEffect`)에 듣기 시작하면
   이미 지나간 뒤라 영영 못 받는다.
   → `app/layout.tsx` 에서 **React 보다 먼저** 받아 `window.__teamkickInstall` 에
   담아 두고, 컴포넌트가 그것을 읽는다. 늦게 오는 경우를 위해
   `teamkick-install-ready` 이벤트로도 알린다.
2. **창을 안 주는 브라우저에서는 아무것도 안 보여줬다.** 삼성 인터넷은
   `beforeinstallprompt` 를 주지 않는다. 갤럭시 기본 브라우저가 이것이다.
   → 이제 **설치되지 않은 상태면 항상 안내를 띄운다.** 창을 받았으면 `앱 설치`
   버튼, 못 받았으면 **브라우저별 손 동작 안내**(아이폰 / 삼성 인터넷 / 그 밖).

검증(Playwright 6단계): 아이폰·삼성인터넷·크롬 각각 알맞은 문구가 뜨는지,
창이 늦게 와도 버튼으로 바뀌는지, 버튼이 실제 설치 창을 부르는지,
**화면을 그리기 전에 온 창도 잡는지**(선捕獲 스크립트).

### 확정 매칭 배치

매칭 화면의 `확정 매칭` 만 **일정 화면용 줄 컴포넌트(`Fixture`)** 를 그대로 가져다
써서, 같은 화면의 다른 탭(카드)과 생김새가 따로 놀았다. 모바일에서는
`.fixture-row{flex-wrap:wrap}` 때문에 배지와 화살표가 줄바꿈되어 더 어긋났다.

→ `모집 중` 탭과 같은 `.match-list-grid` + `article.listing` 카드로 맞췄다.
경기 형식·상태 배지, 팀 대 팀, 날짜·시간, 구장, 확보 여부와 비용, 확정된 결과가
있으면 최종 스코어, `경기 상세 보기` 버튼. 시작 시각 순으로 정렬한다.

검증(Playwright): 데스크톱 1280px·모바일 390px 양쪽에서 카드가 뜨고,
가로 스크롤이 생기지 않고(카드 358px / 화면 390px), 상세가 열린다.

## 2026-09-16 탈퇴자 표시 이름 가리기 (T19)

탈퇴해도 과거 기록의 표시 이름(`members[].name`)은 남는다. 팀의 공동 기록이므로
**그대로 두는 것이 기본**이다(사용자 결정). 다만 개인정보보호법 제36조의 정정·삭제
요구권은 처리방침 고지만으로 배제되지 않는다. 요구가 왔을 때 **방법이 없는 것**이
문제가 되므로 수단을 만들어 두었다.

- `lib/model.ts` — `ANON_NAME="탈퇴한 선수"`, `anonymizeMember` 명령,
  `retiredPeople(s)` 도우미.
- **운영자만** 실행할 수 있고(`ensure(owner,...)`), **이미 탈퇴한 계정만** 대상이다.
  아직 쓰고 있는 계정은 가릴 수 없다.
- 여러 팀에 걸친 기록이 한 번에 바뀐다. 사진(`photo`)도 함께 지운다.
- **경기·출석·골 기록은 그대로 남는다.** `games[].goals[].userId` 도 남는다.
  바뀌는 것은 표시 이름뿐이다.
- 탈퇴자 목록(`visibleState().retired`)은 **운영자에게만** 보낸다.
- 화면: 서비스 관리 → `탈퇴한 분의 기록`. 확인 대화상자를 거쳐야 한다.

검증(로컬): 테스트 **61/61**(4개 추가). 변이 검사 4종 모두 테스트가 잡았다 —
운영자 확인 제거 / 탈퇴 확인 제거 / 목록을 모두에게 노출 / 가릴 때 골 기록도 지움.
Playwright 8단계: 패널 표시, 대상 행 지정, 확인 문구, 가린 뒤 그 사람 실명만
사라지고 **다른 탈퇴자 기록은 그대로**, 가림 완료 표시, 버튼 사라짐.

## 2026-09-16 홈 화면 추가 안내 (T18)

팀킥은 이미 PWA(`public/manifest.webmanifest`, `display: standalone`)라 홈 화면에
추가하면 앱처럼 열린다. 그런데 **아이폰은 그 방법을 스스로 알려주지 않아서**,
안내가 없으면 아이폰 사용자는 영영 모른다.

- `app/install.tsx` 의 `InstallGuide` — `app/teamkick.tsx` 상단 띠 자리에 붙였다.
- **아이폰**: 설치 창이 없으므로 "공유 → 홈 화면에 추가" 문구를 바로 보여준다.
- **안드로이드**: `beforeinstallprompt` 를 가로채 두었다가 `앱 설치` 버튼으로 부른다.
  브라우저가 설치 창을 주지 않으면 안내도 띄우지 않는다.
- 이미 홈 화면 앱으로 열렸으면(`navigator.standalone` 또는
  `display-mode: standalone`) 아무것도 띄우지 않는다.
- 닫으면 `localStorage` 에 기억해 다시 띄우지 않는다. 저장소를 막아둔 브라우저에서도
  화면은 그대로 뜨도록 읽기·쓰기를 모두 `try/catch` 로 감쌌다.

검증(로컬, Playwright 11단계 전부 통과): 아이폰 UA 로 안내 표시·문구·설치 버튼 없음,
닫으면 사라지고 새로고침해도 안 뜸, 안드로이드 UA 로 설치 창 전/후 동작,
`앱 설치` 가 실제로 브라우저 설치 창을 부르는지, 이미 설치된 상태에서 안 뜨는지.

**실기기 확인은 못 했다.** 헤드리스 Chromium 의 UA 흉내다.
실제 아이폰 Safari·안드로이드 Chrome 확인이 필요하다.

## 2026-09-16 공개 전환 준비 — 정보 노출 막기 (T17)

공개하면 누구나 접근한다. `LEGAL.md` 의 "공개 전에 해야 할 것" 중 코드로 할 수 있는
두 가지를 처리했다.

- **`errorHint` 제거** (`lib/model.ts`, `app/api/app/route.ts`, `app/api/auth/route.ts`)
  저장 실패 시 오류 원문 160자를 사용자 화면에 붙이고 있었다
  (`...[Error: no such column: kakao_id]`). DB 구조를 남에게 알려주는 꼴이었다.
  두 라우트 모두 이미 `console.error` 로 원문을 남기므로 진단에 잃는 것이 없다.
- **`/api/health` 노출 제한** (`app/api/health/route.ts`)
  - 누구나: `build`, `database` 둘뿐. 배포 확인 수단은 남겨야 한다.
  - 운영자만: `tables`, `mailReady`, `kakaoSecret`, `ownerSetupCode` 등 전부.
  - **운영자가 아직 없으면 전부 보여준다.** 처음 설치 때는 감출 것이 없고,
    운영자 등록에 필요한 정보를 볼 방법이 이것뿐이다.
  - 표가 없어 `load()` 가 실패해도 막지 않는다. 그 상태를 확인하려고 쓰는 경로다.

검증(로컬): 테스트 **57/57**. 3개 추가. 변이 검사 3종 모두 테스트가 잡았다 —
health 권한 확인 제거 / 운영자 아니어도 통과 / 오류 원문을 응답에 다시 넣음.

**아직 남은 공개 전 항목(코드로 못 하는 것)**: 약관·처리방침 변호사 검토,
개인정보 보호책임자 지정·표기, 위탁 업체 실명 기재, 백업 보관 항목 처리방침 추가.

## 2026-09-16 운영 데이터 백업·복원 (T15)

D1 이 사라지면 복구할 다른 수단이 없었다. 백업·내보내기 코드가 0건이었다.

- `lib/backup.ts` — `exportAll()` / `readBackup()` / `restoreAll()`
- `app/api/backup/route.ts` — `GET` 내려받기, `POST` 복원.
  **둘 다 서비스 운영자만**(`settings:owner` 와 대조). 화면에서 숨기는 것으로는
  부족하므로 서버에서 확인한다. `POST` 는 출처 확인 + 확인 문구("복원합니다") 필요.
- `app/screens.tsx` 의 `BackupPanel` — 서비스 관리 화면 아래쪽.

**백업 파일에 담지 않는 것**: 비밀번호 해시, 세션 토큰, 비밀번호 재설정 토큰,
이메일 확인 토큰, 접속 제한 기록. 파일이 새어 나가도 남의 계정으로 로그인할 수
없어야 하기 때문이다. 그래서 **복원해도 비밀번호는 살아나지 않고** 각자
"비밀번호 찾기"로 다시 정해야 한다. 카카오 가입자는 원래 비밀번호가 없어 바로 된다.

**복원은 계정을 덮어쓰지 않는다**(`INSERT OR IGNORE`). 백업 이후에 바꾼 비밀번호와
이메일 확인 상태를 되돌리면 안 되기 때문이다. 없는 계정만 되살린다.

검증(로컬):
- `node --test tests/core.test.mjs` **50/50**. 백업 테스트 5개 추가.
  변이 검사 6종 모두 테스트가 잡았다 — 운영자 확인 제거 / 비밀번호를 백업에 포함 /
  형식 검사 제거 / 계정 덮어쓰기로 변경 / 확인 문구 검사 제거 / 행 검증 제거.
- `tsc --noEmit` 통과, `pnpm lint` 오류 **77건 유지**(기준값).
- Playwright 14단계: 가입 → 운영자 등록 → 팀 신청·승인 → 백업 패널 표시 →
  실제 파일 내려받기(파일명·내용 확인, 해시 없음) → 문구 없이 누르면 거부 →
  팀 이용 정지로 상태 변경 → 백업으로 복원 → 정지가 풀리고 팀은 그대로.

**아직 하지 않은 것**
- 배포본 확인. 이 개발 환경은 외부 접속이 막혀 직접 못 한다.
- 정기 자동 백업. 지금은 운영자가 직접 눌러야 한다.
- 이미지(R2)는 백업에 포함되지 않는다. 사진 주소만 들어간다.
- 데이터가 커지면 한 번에 내려받는 방식이 한계에 온다(아래 "규모 한계" 참고).

## 2026-09-16 오래된 기록 정리 (T16)

`audit`(운영 이력)과 `notifications`(알림)에 **지우는 코드가 없어 영원히 쌓였다.**
`receipts` 만 7일 기준으로 정리되고 있었다. `load()` 가 매 요청마다 전체를 읽으므로
가장 빨리 부푸는 이 둘이 서비스 전체를 함께 느리게 만든다.

- `lib/model.ts` 의 `prune(state, now)` — `POST /api/app` 의 저장 직전에 돈다.
- 보관 기간 `KEEP`: 영수증 7일 / 운영 이력 90일 / **읽은** 알림 30일 /
  **안 읽은** 알림 180일 / **문의 1년**. 아직 보지 못한 소식은 오래 남긴다.
  문의는 분쟁 기록이라 길게 두고, **답변 대기 중인 문의는 지우지 않는다**.
  탈퇴하면 그 사람 문의는 `closeAccount` 에서 함께 지운다.
- `PRUNE_LIMIT=200` — 한 요청에서 지우는 양의 상한. 오래 쌓인 상태로 배포했을 때
  첫 요청이 수만 건을 한꺼번에 지우다 저장이 실패하는 것을 막는다. 여러 요청에 나뉜다.
- 날짜를 읽을 수 없는 줄은 **남긴다.** 판단할 수 없는 것을 지우지 않는다.

**절대 지우지 않는 것**: `games`(경기·골·어시스트), `sides`(출석·명단), `teams`,
`members`, `users`, `notices`, `requests`, `guests`, `invites`, `settings`.
경기 기록은 팀킥의 존재 이유다. 테스트가 이것을 직접 확인한다.

검증(로컬): 테스트 **54/54**. 정리 테스트 4개 추가.
변이 검사 5종 중 **4종을 테스트가 잡았다** — 경기도 같이 지우게 만듦 / 상한 제거 /
안 읽은 알림도 30일로 / 저장 경로에서 정리 제거.
나머지 1종(`Number.isFinite` 제거)은 `Date.parse` 가 NaN 을 주면 비교가 항상 거짓이라
동작이 같다. **구조상 구분할 수 없는 중복 방어**이며, 비교를 부정형으로 바꾸는
수정이 들어오면 의미가 생기므로 남겨 두었다.

### 측정값 — 왜 이 작업을 했나

메모리 SQLite 에서 `SELECT kind, body FROM entities` 후 `JSON.parse` 까지 잰 값이다.
실제 D1 은 네트워크를 타므로 이보다 느리다.

| 행 수 | 요청 1건 |
|---|---|
| 256 (현재 로컬) | 5ms |
| 5,000 | 96ms |
| 50,000 | 897ms |
| 200,000 | 4,678ms |

## 규모 한계 — 지금은 문제없지만 알아둘 것

`lib/store.ts` 의 `load()` 는 `SELECT kind, body FROM entities` 로 **매 요청마다
전체를 읽는다.** 용량(D1 무료 500MB / 유료 10GB)보다 이쪽이 먼저 걸린다.

- 팀 수십 개 수준까지는 문제없다.
- 팀이 수백 개가 되고 몇 년 누적되면 요청마다 수십만 행을 읽게 되어
  Workers 실행 시간·메모리에 걸린다.
- 바꿀 때가 오면 `entities.scope` 열을 쓰면 된다. 이미 `teamId` 가 들어가 있어서
  팀 단위로 나눠 읽도록 고치는 길이 열려 있다. **지금 미리 고치지 않는다.**

### 배포 상태 (2026-09-15, 사용자 확인)

- `main` `62c251c` 배포 완료. `/api/health` 의 `build` 는 `2026-09-15-region-venue`.
- `OWNER_SETUP_CODE` 환경변수 적용 완료(`ownerSetupCode` 가 `env`).
  **코드 원문은 사용자만 안다. 저장소·문서에 남기지 않는다.**
- 운영자 등록 → 활동 지역 목록 → 구장 검색까지 배포본에서 확인했다(**사용자 확인**).
  개발 환경은 외부 접속이 막혀 있어 내가 직접 확인한 것이 아니다.
- **카카오 구장 검색이 실제 응답을 주는지는 아직 기록하지 못했다.** 다음 세션에서 확인할 것.

### 도메인 — `teamkick.co.kr` (2026-09-15, 가비아 등록 / Brevo 인증 완료)

- Brevo 상태 **Authenticated + Branded**, 브랜드 서브도메인 `mail`.
  사용자가 화면에서 확인했다(개발 환경에서는 외부 조회가 막혀 직접 확인하지 못했다).
- 브랜드 서브도메인을 `www` 가 아닌 `mail` 로 잡아 `www.teamkick.co.kr` 은 비어 있다.
  나중에 웹 주소로 쓸 수 있다.
- 가비아 DNS 에 들어간 것: 인증 4개(brevo-code TXT, DKIM CNAME 2개, `_dmarc` TXT),
  브랜딩 3개(`mail`, `r.mail`, `img.mail` CNAME). 값은 공개 DNS 라 문서에 옮기지 않는다.
- `MAIL_FROM` 은 `팀킥 <no-reply@teamkick.co.kr>` 로 쓴다.
- **아직 실제 발송으로 확인하지 않았다.** 배포 후 가입 → 확인 메일 수신까지 봐야 한다.
- 아래는 고를 때 참고한 조사 기록이다.
- 용도 세 가지: Brevo 발신 인증, 카카오 앱 도메인, 이후 앱 배포. `.com` 하나로 충분하다.
- 가격(조사 시점, 연): Cloudflare Registrar 약 $10.5(갱신도 원가 고정),
  카페24 약 2.2만원, 가비아 약 2.3만원, 후이즈 약 2.8만원. `.kr` 은 국내 등록기관만 가능.
- 비용 차이가 연 8천원 수준이라 **관리 편의로 고르면 된다**(한국어 상담·국내 결제가
  필요하면 가비아, 장기 비용이 우선이면 Cloudflare).
- 구입 후: Brevo → Domains → Add a domain → 표시되는 TXT/CNAME(DKIM·brevo-code·DMARC)을
  도메인 구입처 DNS 에 추가 → Verify. — **미검증**(공식 문서 기준, 실행 확인 못 함)
- 도메인 가용성 조회는 이 환경에서 불가(외부 조회 차단).

#### 웹 주소 연결 — 진행 중 (2026-09-15)

- ChatGPT Sites 설정 → 맞춤 도메인에 `teamkick.co.kr`(apex) 를 등록했다.
  Sites 가 요구한 레코드는 **A 2개(`@`) + TXT 2개**
  (`_openai-site-verification`, `_cf-custom-hostname`).
  값은 공개 DNS 라 문서에 옮기지 않는다.
- apex 에 A 레코드를 주므로 `www` 없이 `teamkick.co.kr` 를 그대로 쓸 수 있다.
- 사용자가 가비아에 4줄을 넣었다(2026-09-15). **연결 확인은 아직이다.**
  가비아 총 11줄 = Brevo 7 + Sites 4.
- 화면의 `DNS 제공업체에 레코드를 추가했습니다` 버튼은 **신고 버튼**이고
  레코드를 대신 넣어주지 않는다. "24시간"은 최대 대기 시간이다.
- 가비아에서 호스트 이름은 **도메인을 뺀 앞부분만** 넣어야 한다
  (`_openai-site-verification`, `_cf-custom-hostname`). 전체 도메인을 넣으면
  `..teamkick.co.kr.teamkick.co.kr` 이 되어 영원히 확인되지 않는다.
- `@` 의 기존 A 레코드(가비아 파킹)는 지워야 하지만,
  **TXT·CNAME 은 하나도 지우면 안 된다** — 메일 발송이 죽는다.

#### 주소가 바뀌면 함께 해야 하는 것

- 코드에는 주소가 하드코딩되어 있지 않다. 카카오 redirect(`lib/kakao.ts:19`)도
  메일 링크(`lib/auth.ts:197,276`)도 **요청이 들어온 origin 을 그대로 따라간다.**
  따라서 앱 코드는 고칠 것이 없다.
- 다만 **카카오 개발자 콘솔에 새 주소를 등록해야** 한다. 안 하면 새 주소에서
  카카오 로그인만 실패한다.
  - 플랫폼 → Web → 사이트 도메인: `https://teamkick.co.kr`
  - 카카오 로그인 → Redirect URI: `https://teamkick.co.kr/api/kakao`
  - 기존 `chatgpt.site` 주소는 **지우지 말고 함께 둔다**(확인 전 백업).
- 연결 후 확인할 것: `/api/health` 응답, 카카오 로그인, 가입 확인 메일 수신과
  메일 속 링크 주소.

---

## 사용자 확인 필요

지금 당장 필요한 것은 없다. 아래는 나중에 필요해지는 시점에 요청한다.

0. ~~배포와 migration~~ **해결됨** — 배포 확인, 표 8개 생성 확인(`/api/health`).
   남은 것은 기능별 실사용 확인(가입 → 확인 메일 → 링크)이다.
   - 저장소에는 migration 을 돌리는 스크립트가 없다. 원본 앱이 `0000` 이 적용된
     상태로 동작했으므로 **배포 과정이 `drizzle/` 를 자동 적용할 가능성이 높지만
     확인하지 못했다** — **미검증**.
   - 자동 적용되지 않으면 `scripts/manual-migrate.sql` 을 운영 D1 에 한 문장씩
     실행한다. 0000 만 적용된 DB 에서 실제로 돌려 결과 스키마를 확인했다.
1. **R2 켜기** — 이미지 업로드를 실제 서비스에 올릴 때 필요하다.
   ChatGPT Sites는 공개 베타 동안 저장소가 플랜 한도 안에서 포함되므로 별도 결제
   없이 켤 수 있을 가능성이 높지만, **한도는 제품 화면에서 확인**해야 한다.
   지금 켜지 않아도 다른 작업은 계속 진행할 수 있다.
2. **사이트 공개 전환** — 자체 회원가입과 실기기 확인이 끝난 뒤에 한다.
3. **용병 권한 범위(ASM-03)** — 모집·승인을 운영진에게도 열었다.
   주장만 하도록 좁히려면 알려주면 바로 바꾼다.
4. **운영자 초기 설정 코드** — 원문이 저장소에 없다(SHA-256만 있음).
   실제 운영자 등록을 검증할 때 필요하다.
5. **`BREVO_API_KEY`·`MAIL_FROM`** — 비밀번호 재설정 메일 발송에 필요하다.
   Brevo는 **보유 도메인 없이** 주소 하나를 확인해 시작할 수 있고 무료 하루 300건이다.
   (`RESEND_API_KEY` 를 넣으면 Resend 로 보내지만 도메인 확인이 필요하다.)
   없으면 비밀번호 찾기만 감춰지고 나머지는 그대로 동작한다.
   **`MAIL_FROM` 에 `naver.com` 주소를 넣지 않는다** — 위 "발신 도메인 제약" 참고.
   회신은 코드가 문의처로 돌려주므로 발신 주소와 문의처가 달라도 된다.
6. **`KAKAO_REST_KEY`** — 구장명 검색을 실제로 쓰려면 카카오 개발자에서
   앱을 만들고 REST API 키를 받아 환경변수로 넣어야 한다. 무료로 쓸 수 있고
   한도는 카카오 개발자 콘솔에서 확인한다. 없어도 수기 입력은 그대로 된다.

---

## 2026-09-15 활동 지역 목록화 · 구장 선택 UX

- **활동 지역**을 자유 입력에서 18개 목록 선택으로 바꿨다.
  목록은 `lib/model.ts`의 `REGIONS`("서울 / 경기 남부 / 경기 북부 / 인천 /
  강원 / 대전 / 세종 / 충북 / 충남 / 광주 / 전북 / 전남 / 대구 / 경북 /
  부산 / 울산 / 경남 / 제주")이고, `regionValue()`가 **서버에서** 값을 검증한다.
  `createTeam`·`editTeam`·`createGame` 세 곳에 적용했다.
  화면(`app/screens.tsx`)은 `Picker`로 바뀌어 목록 밖 값을 보낼 수 없다.
- **구장 검색**에서 후보를 고르면 후보 목록·검색어가 사라지고
  "고른 구장: <이름> · <주소> [다시 검색]" 만 남는다(`VenuePicker`).
- 검증(로컬):
  - `node --test tests/core.test.mjs` **45/45 통과**
    (추가 테스트 "활동 지역은 목록에 있는 값만 받는다" 포함.
    `ensure`를 지우면 44/45로 떨어지는 것까지 확인했다.)
  - `node node_modules/typescript/bin/tsc --noEmit` 통과.
  - `pnpm lint` **오류 77건 유지**(시작 시점 기준값과 동일).
  - Playwright 로 가입 → 운영자 등록 → 팀 신청 → 승인 → 경기 만들기 →
    구장 검색까지 한 번에 확인했다. 구장 검색은 외부 API 라 응답만 흉내 냈고
    (`/api/places` 라우트 가로채기), **화면 동작만** 확인한 것이다.
    실제 카카오 응답 확인은 배포 환경에서 해야 한다.
- 기존 데이터: 목록에 없는 지역("용인" 등)이 들어간 팀이 로컬에 남아 있다.
  읽기는 그대로 되고, **수정할 때만** 목록에서 다시 고르게 된다.

---

## Astra 검토

- **불필요**
- 이유: 이번 변경은 CSS 1줄이고 실행 결과로 전후 차이를 측정했다.
  남은 막힌 항목은 기술 판단이 아니라 사용자 결정이 필요한 사안이다.
- 검토할 구체적인 질문: 없음.

> 기록: Astra는 소스와 기능을 검증하지 않았으며,
> 프로젝트·저장 버전·배포 상태만 확인했다.
