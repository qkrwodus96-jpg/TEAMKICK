# TEAMKICK 작업 인계

최종 갱신: 2026-09-15
작업 폴더: `/home/user/TEAMKICK`
브랜치: `claude/teamkick-setup-baseline-lwntca`
현재 커밋: 이미지 업로드 구현 커밋 (직전 `6149469`)
미커밋 변경: 문서 갱신분 (이 커밋에 포함 예정)
현재 작업 ID: T01·T09 완료 / 다음은 T04 예약 작업

---

## 확인된 상태

### 실제로 확인한 사실

- 원본 소스가 `main`에 들어와 병합했다. **118개 파일, 실제 동작하는 앱이다.**
  이전 세션의 "소스 없음" 기준은 더 이상 유효하지 않다.
- 스택: vinext(Vite 기반 Next 호환) + React 19 + TypeScript,
  Cloudflare Workers + **D1**, Drizzle migration, shadcn/Radix, PWA.
- 인증은 **ChatGPT Sites 로그인 전용**이다. 자체 회원가입은 없다.
- 모든 변경은 `POST /api/app` 하나로 들어가고 `lib/model.ts`가 서버에서
  팀 소속·역할·승인 상태를 검증한다. 클라이언트 역할 값을 신뢰하지 않는다.
- 동시성은 `state_revision` 버전 + `write_guards` CHECK 제약 + 최대 4회
  재시도로 처리한다. 요청 ID를 7일 보관해 재전송 중복을 막는다.
- **기준 실행 결과**: `pnpm install --frozen-lockfile` 성공,
  `node --test tests/core.test.mjs` **10/10 통과**, `tsc --noEmit` 통과.
- `pnpm lint`는 `no-explicit-any` **77건 오류**. **시작 시점부터 있던 문제**이며
  이번 작업이 만든 것이 아니다. 기존 코드 스타일이라 손대지 않았다.
- 로컬 D1에는 테이블이 없어 첫 실행 시 `/api/app`이 503이다.
  `drizzle/0000_bitter_cardiac.sql`을 로컬 D1 파일에 적용하면 정상 동작한다.
- 요구사항 대비 **없는 기능을 코드 검색으로 확인**: 용병 모집 0건,
  실제 푸시 0건(`sw.js`에 push 핸들러 없음), 구장 검색 API 0건, 광고 영역 0건,
  이미지 업로드 0건(`hosting.json`의 `r2: null`).

### 아직 확인하지 못한 사항

- 실기기(iPhone·갤럭시) 확인. 이번엔 **헤드리스 Chromium 에뮬레이션만** 사용했다.
- 배포 환경의 실제 로그인·D1·접근 범위. 로컬 mock 로그인만 사용했다.
- 다중 사용자 동시 조작의 브라우저 검증. 로컬 mock 사용자는 1명뿐이라
  권한·동시성은 `tests/core.test.mjs`로만 확인했다.
- 달력·주소 복사·지도, 앱 내 알림·초대 링크, PWA 설치·오프라인 동작.
- 운영자 초기 설정 코드의 원문(저장소에 SHA-256만 있음).

---

## 이번에 완료한 작업

### T09 팀 로고·선수 사진 업로드 (2026-09-15)

- **변경 파일**: `lib/images.ts`(신규), `app/api/image/route.ts`(신규),
  `lib/model.ts`(setTeamLogo·setMemberPhoto), `app/screens.tsx`(ImageField),
  `app/teamkick.tsx`(Crest 로고·PlayerPhoto), `app/globals.css`,
  `.openai/hosting.json`(`r2: "BUCKET"` 선언)
- **사용자에게 달라지는 동작**: 팀 정보 수정에서 **팀 로고**를, 내 선수 정보와
  주장의 선수 정보 수정에서 **선수 사진**을 올릴 수 있다. 올린 이미지는 팀 배지와
  선수 명단·기록 순위에 바로 보이고 새로고침·다른 기기에서도 유지된다.
- **저장·검증**: 파일은 R2에, 앱 상태에는 키만 저장한다. 선언한 종류를 믿지 않고
  실제 바이트로 PNG·JPEG·WEBP를 확인하고 2MB로 제한한다. 브라우저에서 긴 변
  512px로 줄여 보낸다. 같은 자리에 새로 올리면 이전 파일을 지운다.
- **권한**: 팀 로고는 주장만 올리고 로그인 이용자에게 보인다. 선수 사진은 본인 또는
  주장만 올리고 **그 팀의 활동 팀원에게만** 보인다. 명령은 키가 그 팀·그 팀원 경로
  아래인지 확인해 남의 이미지를 가리킬 수 없다.
- **저장소 미연결 시**: 앱은 그대로 동작하고 화면은 업로드 대신 안내를 보여주며
  서버는 업로드를 분명히 거절한다. 성공한 것처럼 보이지 않는다.
- **`<img>` 사용**: lint가 `next/image` 사용을 권하지만(경고 3건), 이 이미지는
  권한 검사를 거치는 자체 API로 내려주는 비공개·소용량 파일이라 최적화 로더를
  거치지 않는 편이 맞다고 판단했다.

### T01 자체 회원가입 (2026-09-15)

- **변경 파일**: `lib/auth.ts`(신규), `app/api/auth/route.ts`(신규),
  `db/schema.ts` + `drizzle/0001_dizzy_slayback.sql`(accounts·sessions 테이블),
  `app/api/app/route.ts`(신원 교체·운영자 재설정 판정),
  `app/screens.tsx`(AuthPanel·로그아웃), `app/teamkick.tsx`(로그인 화면 연결),
  `lib/model.ts`(운영자 재설정), `tests/core.test.mjs`(테스트 5건)
- **사용자에게 달라지는 동작**: ChatGPT 계정 없이 **이메일과 비밀번호로 가입·로그인**한다.
  ChatGPT 로그인 버튼은 화면에서 사라졌다.
- **보안 처리**: 비밀번호는 PBKDF2-HMAC-SHA256 600,000회(OWASP 권고)로만 저장하고
  원문은 남기지 않는다. 세션 토큰은 HttpOnly·Secure·SameSite=Lax 쿠키로 주고
  서버에는 **토큰의 SHA-256만** 저장한다. 30일 만료, 로그아웃 시 삭제,
  로그인 10회 실패 시 15분 잠금, 실패 응답은 가입 여부를 구분해주지 않는다.
  계정·세션은 `entities` 문서 저장소가 아니라 별도 테이블에 둔다.
- **DB 변경**: migration `0001` 추가. 배포 시 적용이 필요하다.
- **주의(운영자)**: 기존 운영자 설정이 ChatGPT 계정 ID에 묶여 있으면 새 계정이
  운영자가 될 수 없다. 저장된 운영자 ID의 계정이 없을 때에 한해 설정 코드를 다시
  제시하면 재설정되도록 했다. 살아 있는 운영자 계정은 코드를 알아도 가져갈 수 없다.

### T00 기준 확보 · T02-1 모바일 명단 표 수정

- **변경 파일**: `app/globals.css` 1줄 (`.gap-grid>*,.form-grid>*{min-width:0}`)
- **사용자에게 달라지는 동작**: 선수 이름이 긴 팀원이 있으면 우리팀 선수 명단 표가
  화면 폭을 밀어내 휴대폰에서 페이지 전체가 옆으로 벌어지고 마지막 '관리' 열이
  화면 밖에 남았다. 이제 페이지는 화면 폭을 유지하고 넘치는 표만 가로로 스크롤된다.

### T06 용병 모집·마감 (신규 기능)

- **변경 파일**
  - `lib/model.ts` — `guests` 컬렉션 추가, 명령 7종
    (`openGuests`/`closeGuests`/`applyGuest`/`withdrawGuest`/
    `approveGuest`/`rejectGuest`/`cancelGuest`), `visibleState`에
    `guests`·`myGuests`·`guestListings` 노출, 경기 취소 시 모집 종료
  - `app/screens.tsx` — 매칭 화면 용병 탭, 경기 상세의 모집 현황·신청자 승인,
    용병 모집·신청 모달
  - `app/teamkick.tsx` — `GuestBadge`("용병 모집 중"/"용병 마감"), 경기 목록 표시,
    소속 팀 없는 사용자의 매칭 화면 접근 허용
  - `tests/core.test.mjs` — 테스트 4건 추가
- **사용자에게 달라지는 동작**
  주장·운영진이 예정 경기에 용병 모집 인원을 열 수 있고, 다른 사용자가
  매칭 > 용병에서 신청한다. 승인 인원이 정원에 도달하면 **서버가 자동으로 마감**하고
  남은 대기자를 종료 처리하며, 일정 목록·경기 상세에 "용병 마감"이 함께 표시된다.
  용병은 팀원이 되지 않고 팀 내부 정보도 보이지 않는다.
- **DB 변경 없음**: `entities` 단일 문서 테이블 구조를 그대로 쓰므로
  migration이 필요하지 않다.

---

## 검증

- **실행 명령**
  - `pnpm install --frozen-lockfile` → exit 0
  - `node --test tests/core.test.mjs` → **20 pass / 0 fail**
  - `node node_modules/typescript/bin/tsc --noEmit` → 통과
  - `pnpm dev` + 실제 브라우저(Chromium) 조작
- **결과**: 위 전부 통과. 수정 후 재실행해도 동일.
- **수동 확인 (390×844 모바일 에뮬레이션)**
  - 수정 전: 문서 폭 **456px**(화면 390px) — 레이아웃이 옆으로 벌어짐,
    표 컨테이너 `scrollWidth == clientWidth`라 가로 스크롤 불가
  - 수정 후: 문서 폭 **390px** 유지, 표 컨테이너 **316/353** 으로 가로 스크롤 동작
  - 홈·일정·매칭·기록·우리팀 5개 화면 모두 가로 넘침 없음
  - 1440 / 768 / 390 / 360 폭 확인 — 데스크톱·태블릿 레이아웃 변화 없음
  - 팀 등록 신청 → 운영자 승인 → 주장 권한 → 팀원 관리 모달 → 중첩 확인창
    '확인' → 팀원 제외가 서버에 반영됨
  - 내 선수 정보에서 포지션 변경·저장 시 **역할은 그대로 유지**
    (주장·일반 팀원 두 시점 모두)
  - 일반 팀원 화면에서 팀원 초대·관리·공지 작성 버튼이 **노출되지 않음**
- **용병 기능 검증 (브라우저 + 테스트)**
  - 모집 개설(정원 2) → 다른 사용자 3명 신청 → 승인 2건 → **서버가 자동 마감**,
    남은 대기자 모집 종료, 목록에서 사라짐
  - 일정 목록과 경기 상세에 **"용병 마감"** 동일 표시, 상세에 "승인 2명 / 모집 2명"
  - 마감 후 승인 요청 → **HTTP 409 거부**, 승인 인원 2명 유지
  - 소속 팀 없는 사용자로 용병 목록 확인·신청 성공, 신청 후에도 팀원이 되지 않음,
    팀 명단·경기·공지 미노출
  - **변이 테스트**로 테스트가 실제 실패를 잡는지 확인:
    자동 마감 제거 → 3건 실패 / 팀원 중복 신청 차단 제거 → 1건 실패 /
    권한 검사 제거 → 1건 실패. (마감 상태 검사만 제거하면 정원 검사가 막아
    통과하는데, 두 검사가 서로를 커버하는 의도된 이중 방어다.)
  - `pnpm lint` 오류 수가 기준과 동일한 **77건** 유지(새로 늘리지 않음)
- **자체 회원가입 검증 (브라우저 + 테스트)**
  - 가입 API 응답 **335ms**(PBKDF2 600,000회 포함)로 실제 Workers 런타임에서 동작
  - 로그인 화면 표시 → 가입 → 세션 쿠키(HttpOnly·SameSite=Lax, JS로 읽기 불가) →
    새로고침·새 탭에서 로그인 유지 → 팀 등록 신청 성공 → 로그아웃(쿠키 제거) →
    잘못된 비밀번호 거부 → 재로그인 후 이전 데이터 그대로 확인
  - 비밀번호 원문·세션 토큰 원문이 DB에 저장되지 않음을 테스트로 확인
  - 운영자 재설정: 계정이 없을 때만 열리고, 살아 있는 계정은 뺏기지 않음을
    로컬 API 요청으로 양방향 확인
  - 변이 테스트로 재설정 조건·설정 코드 검사가 실제 회귀를 잡는 것을 확인
- **미실행 항목과 이유**
  - 실기기 검증: 기기가 없다 → **실기기 미검증**
  - 실기기 푸시 수신: 기능 자체가 없다 → **미구현**
  - 운영자 초기 설정: 설정 코드 원문이 저장소에 없어 실제 등록은 **미검증**
  - 비밀번호 재설정(메일 발송): 외부 메일 서비스가 필요해 **미구현**
  - 배포 검증: 배포 권한·환경 밖이다 → 미실행

---

## 막힌 작업

### 1. 사용자 제보가 재현되지 않음 (T02-2)

- **원인**: "우리팀 팀원관리의 내 선수 정보 수정에서 주장만 선택되고 다른 선택이
  되지 않는다"를 재현하지 못했다.
- **시도한 해결**
  1. `내 선수 정보` 모달의 포지션 선택(GK/DF/MF/FW)을 실제 클릭 → 4개 모두
     선택되고 저장까지 정상.
  2. **주장 시점**과 **일반 팀원 시점** 두 계정 상태로 각각 확인 → 둘 다 정상.
  3. 화면 전체에서 선택 UI를 조사한 결과 "주장"이 **선택지로 들어간 곳은 없다.**
     주장/운영진/팀원은 명단 표의 **읽기 전용 역할 표시**이고,
     역할 변경은 주장이 '관리'에서 버튼으로 한다(선택 목록이 아님).
  4. 다만 긴 이름이 있을 때 **'관리' 열이 화면 밖으로 밀리는 결함**은 실재했고
     이번에 수정했다. 제보가 이 증상일 가능성이 있다.
- **필요한 사용자 결정·정보**: 아래 "사용자 확인 필요" 참고.
- **다른 작업에 미치는 영향**: 없음. 다른 작업은 계속 진행 가능.

### 2. 일반 회원 직접 가입 (T01)

- **원인**: 요구사항 B는 "박재연은 일반 회원으로 직접 가입"이지만,
  현재 로그인은 **ChatGPT Sites 계정 전용**이다(`app/chatgpt-auth.ts`).
  앱에는 자체 회원가입·비밀번호 저장이 전혀 없다.
- **영향 범위**: 이 결정이 배포 방식, 서버 비용, 앱스토어 제출 가능성,
  실제 팀원 초대 방식에 모두 영향을 준다. 임의로 정하면 되돌리기 어렵다.
- **다른 작업에 미치는 영향**: 용병 모집·푸시·구장 검색은 이 결정과 무관하게
  진행할 수 있다.

### 3. 이미지 업로드 (T09)

- **원인**: `hosting.json`의 `r2: null` — 파일 저장소가 연결되어 있지 않다.
- **필요한 설정**: R2 바인딩. 없으면 요구사항 J를 구현할 수 없다.

---

## 공개 전환 전에 처리해야 할 것 (자체 회원가입의 알려진 공백)

- **가입 요청 속도 제한이 없다.** 지금은 가입 한 번에 비밀번호 해시 계산이
  들어가 자연스러운 제동이 걸리고 팀 생성에도 운영자 승인이 필요하지만,
  사이트를 전체 공개하기 전에는 IP 기준 제한이나 가입 확인 절차가 필요하다.
- **비밀번호 재설정이 없다.** 비밀번호를 잊으면 복구할 수 없다.
  메일 발송 서비스가 필요하므로 별도 작업으로 다룬다.
- **이메일 실재 확인(인증 메일)이 없다.** 같은 이유로 미구현이다.

## 다음 행동

1. **T04 예약 작업** — 출석 초안과 투표 마감 24시간 전 알림을 서버 스케줄로 옮긴다.
   지금은 브라우저를 켜둬야만 처리된다. 사용자 결정에 묶이지 않는다.
2. **T03 구장명 검색** — 검색 API 미설정·장애에서도 수기 입력이 유지되도록 설계한다.
   외부 API 키가 필요하면 그 시점에 요청한다.
3. **T10 광고 영역 준비** — 공통 영역과 설정 on/off. SDK는 도입하지 않는다.

---

## 결정과 가정

- **새로 결정한 사항**
  - 모바일 표 결함은 컴포넌트가 아니라 `globals.css` 한 줄로 고쳤다.
    `components/ui/table.tsx`(shadcn 원본)를 건드리지 않아 다른 화면의
    표 4곳에 같은 효과가 적용되고 업스트림과의 차이도 생기지 않는다.
  - `no-explicit-any` 77건은 고치지 않았다. 기존 코드 전반의 스타일이라
    수정 시 변경 범위가 과도해지고, 요구사항과 무관하다.
- **확인이 필요한 가정** (`PROJECT_CONTEXT.md` 4절 표에 누적)
  - ASM-01: 용병 승인 취소로 빈자리가 생기면 모집을 다시 연다. **구현함.**
  - ASM-02: 투표 마감 24시간 전 알림은 일정 변경 시 재계산한다. (T04에서)
  - ASM-03: 용병 모집·승인 권한을 주장뿐 아니라 **운영진에게도** 부여했다.
  - ASM-04: 용병은 팀원 명단·출석·선수 통계에 넣지 않는다.
  - ASM-05: 소속 팀이 없는 사용자도 매칭 화면의 용병 탭에 들어갈 수 있게 했다.

---

## 사용자 확인 필요

지금 당장 필요한 것은 없다. 아래는 나중에 필요해지는 시점에 요청한다.

1. **R2 켜기** — 이미지 업로드를 실제 서비스에 올릴 때 필요하다.
   ChatGPT Sites는 공개 베타 동안 저장소가 플랜 한도 안에서 포함되므로 별도 결제
   없이 켤 수 있을 가능성이 높지만, **한도는 제품 화면에서 확인**해야 한다.
   지금 켜지 않아도 다른 작업은 계속 진행할 수 있다.
2. **사이트 공개 전환** — 자체 회원가입과 실기기 확인이 끝난 뒤에 한다.
3. **용병 권한 범위(ASM-03)** — 모집·승인을 운영진에게도 열었다.
   주장만 하도록 좁히려면 알려주면 바로 바꾼다.
4. **운영자 초기 설정 코드** — 원문이 저장소에 없다(SHA-256만 있음).
   실제 운영자 등록을 검증할 때 필요하다.

---

## Astra 검토

- **불필요**
- 이유: 이번 변경은 CSS 1줄이고 실행 결과로 전후 차이를 측정했다.
  남은 막힌 항목은 기술 판단이 아니라 사용자 결정이 필요한 사안이다.
- 검토할 구체적인 질문: 없음.

> 기록: Astra는 소스와 기능을 검증하지 않았으며,
> 프로젝트·저장 버전·배포 상태만 확인했다.
