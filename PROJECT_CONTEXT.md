# PROJECT_CONTEXT.md — TEAMKICK 제품 요구사항

최종 갱신: 2026-09-14
이 문서는 **확정 제품 요구사항의 단일 출처**다.
개발 규칙은 `CLAUDE.md`, 구조는 `ARCHITECTURE.md`, 작업은 `TASKS.md`를 본다.

---

## 0. 서비스 개요와 식별 정보

| 항목 | 값 |
|---|---|
| 서비스명 | TEAMKICK |
| 모바일 표기 | 팀킥 |
| 서비스 내용 | 아마추어 축구팀의 가입 승인, 일정, 참여 투표, 출석, 팀 매칭, 용병 모집, 선수 기록 관리 |
| 운영 목표 | 초기 무료 배포로 사용자 확보 → 이후 광고 지면으로 수익화 확장 가능한 구조 |
| 운영 조건 | 1인 운영, 초기 서버비 최소화, 유지보수 단순화 |
| 기본 언어 | 한국어 |
| 통계·화면 기준 시간대 | Asia/Seoul |

### 기존 ChatGPT Sites 프로젝트 (참고 정보)

| 항목 | 값 |
|---|---|
| 프로젝트 ID | `appgprj_6aa6c615232c8191833b52f2e9f2cf49` |
| 제목 | 팀킥 · 우리 팀의 모든 경기 |
| 기존 URL | https://teamkick-jaeyeon.qkrwodus96.chatgpt.site |
| 저장 버전 | v1 (73개 파일 소스 보관본) |
| 참고 커밋 | `2f899fe373d2d7ee447739b1f650dd52e386aa4d` |

**주의**

- 위 커밋은 2026-09-14 Astra가 확인한 시점의 참고 정보다.
- Astra가 확인한 범위는 **프로젝트 조회·소유자 권한, 저장 버전 v1과 73개 파일
  보관본의 존재, 해당 버전의 배포 성공**뿐이다.
  Astra는 **소스 내용과 기능을 검증하지 않았다.**
- **배포 성공을 기능 구현 완료의 근거로 사용하지 않는다.**
- 프로젝트 ID만으로 Claude Code가 원본에 접근할 수 있다고 가정하지 않는다.
- 현재 폴더에 더 최신 작업이 있다면 위 과거 버전으로 되돌리지 않는다.

---

## 1. 코드에서 확인한 사실 (2026-09-14, 원본 소스 확인 후)

원본 소스가 `main`에 들어와 병합했다. 아래는 **코드를 읽고 실제로 실행해
확인한 사실**이다. 추정은 4절 가정 표에 둔다.

### 확인된 기술 스택

| 항목 | 확인된 내용 |
|---|---|
| 프레임워크 | vinext 1.0.0-beta.5 (Vite 8 기반 Next 16 호환), React 19.2 |
| 실행 | Cloudflare Workers (wrangler 4.92, @cloudflare/vite-plugin) |
| DB | **Cloudflare D1**, Drizzle ORM + migration (`drizzle/0000_bitter_cardiac.sql`) |
| UI | shadcn/Radix, Tailwind 4, 녹색 테마, PWA(manifest·sw.js·오프라인 안내) |
| 인증 | **자체 회원가입**(이메일+비밀번호, PBKDF2 600,000회, 세션 쿠키) — `lib/auth.ts` |
| 진입점 | `app/teamkick.tsx`(화면·달력·요약), `app/screens.tsx`(팀·매칭·입력) |
| 도메인 | `lib/model.ts` — 역할·상태 전이·출석·통계 전부 서버에서 검증 |
| 저장 | `lib/store.ts` — `entities` 문서 테이블 + `state_revision` + `write_guards` CHECK |
| API | `POST /api/app` 단일 엔드포인트, 요청 ID 7일 보관(중복 방지), 분당 40건 제한 |

### 실행으로 확인한 사실

- `pnpm install --frozen-lockfile` 성공, `node --test tests/core.test.mjs` **10/10 통과**,
  `tsc --noEmit` 통과. `pnpm lint`는 `no-explicit-any` **77건 오류(시작 시점부터 존재)**.
- 개발 서버 실행 후 실제 브라우저로 팀 등록 신청 → 운영자 승인 → 주장 권한 →
  팀원 관리 → 내 선수 정보 수정 흐름이 **실제로 동작**함을 확인했다.
- 일반 팀원 계정에서는 팀원 초대·관리·공지 작성 버튼이 노출되지 않고,
  본인 프로필 수정으로 역할이 바뀌지 않음을 확인했다.

### 요구사항 대비 현재 구현 상태

| 요구사항 | 현재 코드 상태 | 근거 |
|---|---|---|
| A 5개 탭·모바일 | 구현됨. 모바일 레이아웃 결함 1건 수정 | 실행 확인 |
| B 운영자·팀 권한 | 구현됨(운영자 일회용 코드 SHA-256 검증, 첫 가입자 자동 운영자 아님) | `model.ts` `setupOwner` |
| B 일반 회원 직접 가입 | **구현 완료.** 이메일+비밀번호 회원가입·로그인·로그아웃 | 테스트 16~19, 브라우저 검증 |
| C 팀원 관리·내 선수 정보 | 구현됨. **제보 원인 규명·수정 완료**(Picker 값 초기화 + 주장의 선수 정보 수정 기능 추가) | 브라우저 검증 |
| D 일정 | 구현됨(달력·목록·자정 넘는 경기·상태 3종) | `model.ts` |
| D 구장명 검색·주소 자동 입력 | **구현 완료.** 카카오 로컬 검색 · 후보 선택 · 좌표 저장 · 자주 쓰는 구장 (**키 설정 필요**) | 브라우저 검증 |
| E 투표·출석 | 구현됨(4상태·마감·초안·확정·멱등·출석률 분모) | 테스트 통과 |
| E 브라우저 없이 자동 처리 | **스케줄러가 필요 없게 정리됨**(아래 4-2절) | 코드 확인 |
| F 팀 매칭 | 구현됨(공통 경기 ID·동시 수락 1건·시간 충돌·재동의) | 테스트 통과 |
| G 용병 모집 | **구현 완료.** 모집·신청·승인·자동 마감·동시 승인 방어 | 테스트 11~14, 브라우저 검증 |
| H 결과·기록·통계 | 구현됨(제안·확인·이견·검증식·기간 집계) | `model.ts`, 테스트 |
| I 앱 내 공지·알림 | 구현됨(주장만 발송, 팀별 분리) | `model.ts` |
| I 실제 휴대폰 푸시 | **없음.** `public/sw.js`에 push·notificationclick 핸들러 없음 | 코드 확인 |
| J 이미지 업로드 | **구현 완료.** 팀 로고·선수 사진 업로드·표시·삭제, 종류/크기 검증, 접근 권한 | 테스트 21, 브라우저 검증 |
| K 운영자 화면 | 구현됨(승인·반려·정지·복구, 감사 이력) | `model.ts` |
| K 광고 영역 | **보류.** 사용자 결정으로 나중에 진행 | 2026-09-15 |

## 2. 확정 요구사항

전부 사용자가 결정한 사항이다. 구현 가정은 3절에 별도 표기한다.

### A. 화면과 모바일

- 기본 탭: **홈 / 일정 / 매칭 / 기록 / 우리팀**
- 기존 디자인을 우선 유지하고 모바일 사용성을 개선한다.
- iPhone·갤럭시에서 글자, 버튼, 키보드, 스크롤, 화면 하단 여백이
  자연스럽게 동작해야 한다.
- 로딩 / 빈 상태 / 승인 대기 / 오류 / 권한 없음 상태를 제공한다.
- iOS·Android 대응 및 향후 App Store·Google Play 배포가 가능한 구조를
  검토하고 필요한 작업을 문서화한다.
- 현재가 웹앱/PWA라면 네이티브 앱과의 기능 차이를 기록한다.
  **웹 배포 성공만으로 양대 앱스토어 대응 완료라고 보고하지 않는다.**

### B. 운영자와 팀 권한

**서비스 운영자**

- 서비스 운영자 계정은 일반 회원과 **별도로** 존재한다.
- 운영자 아이디·비밀번호는 안전한 관리자 생성 절차로 설정한다.
- 비밀번호·관리자 비밀값을 코드에 하드코딩하지 않는다.
- 일반 가입, 첫 가입자, 브라우저 값, 프로필 수정으로
  서비스 운영자 권한을 얻을 수 없어야 한다.
- 사용자 **박재연은 일반 회원으로 직접 가입**한다.
  자동으로 회원·주장·운영자 계정을 만들거나 연결하지 않는다.

**팀 생성과 가입**

- 팀 생성 신청 → 서비스 운영자 승인·반려
- 승인 시 **팀 활성화와 최초 주장 지정은 함께 성공**해야 한다(원자적 처리).
- 팀 가입 신청 → 해당 팀 주장 승인·반려
- 초대 링크도 로그인과 가입 승인 절차를 거친다.
- 대기자는 팀 내부 일정, 선수 명단, 투표자 명단, 개인 기록을 볼 수 없다.
- 한 계정이 여러 팀에 가입할 수 있고, **역할은 팀마다 다르다.**
- 팀 전환 시 이전 팀 데이터·내부 메모·캐시가 섞이지 않는다.

**운영진과 주장 인계**

- 팀 운영진은 **위임된 기능만** 관리한다.
- 주장단 공지 권한을 추가하더라도 **가입 승인·매칭 결정·주장 인계 권한을
  모든 운영진에게 자동으로 부여하지 않는다.**
- 주장 인계: 기존 주장 지정 → 새 주장 수락
- 인계가 끝나기 전에 기존 주장이 탈퇴해 **무주장 팀이 되지 않도록** 한다.
- 예외적인 운영자 개입은 사유와 이력을 남긴다.
- 팀 이용 정지 시 외부 검색과 새 활동을 제한하고,
  기존 승인 팀원에게는 기록을 **읽기 전용**으로 제공한다.
- 정지된 팀 때문에 **동일 계정의 다른 정상 팀 권한이 막히지 않는다.**

### C. 팀원 관리와 내 선수 정보

**사용자 제보 (재현·수정 대상)**
> "우리팀 팀원관리의 내 선수 정보 수정에서 주장만 선택되고 다른 선택이 되지 않는다."

- 실제 화면과 코드를 확인해 문제를 재현하고 수정한다.
- **선수 포지션·프로필과 팀 운영 역할을 구분**한다.
- 본인 프로필 수정으로 주장이나 운영자 권한을 얻을 수 없어야 한다.
- 승인된 팀원이 우리팀의 전체 선수 명단을 확인할 수 있게 한다.
- 현재 팀원 / 가입 대기 / 과거 선수를 필요한 범위에서 구분한다.
- 본인은 허용된 선수 정보와 프로필 이미지를 수정할 수 있다.
- 권한 있는 관리자는 팀원 제외·관리를 수행할 수 있다.
- **팀원 제외와 서비스 계정 전체 삭제를 구분**한다.
- 팀에서 제외돼도 과거 경기·출석·득점 기록은 **보존**한다.
- 제외된 사람의 현재 계정 정보나 다른 팀 활동을 추가 공개하지 않는다.

### D. 일정과 구장 주소

- 월간 달력, 날짜별 목록, 오늘 이동, 같은 날 여러 경기 지원
- 저장 항목: 경기일, 시작·종료 시각, 경기 형식, 상대팀, 장소, 투표 마감
- 자정을 넘는 경기와 팀별 집합 시간·내부 메모 지원
- 경기 상태: **예정 / 완료 / 취소**
- **시간이 지났다는 이유만으로 경기 완료를 자동 확정하지 않는다.**

**구장 주소**

- 구장명 검색 시 실제 장소 검색 결과와 전체 주소를 제시한다.
  예: "수지체육공원 축구장" 검색 → 후보 선택 → 주소 자동 입력
- 동명 장소가 있으면 지역·주소로 구분해 선택할 수 있게 한다.
- **검증하지 않은 주소를 임의로 생성하지 않는다.**
- 선택한 구장명, 주소, 필요하면 좌표를 함께 저장한다.
- 세부 코트, 주차·집합 안내, 주소 복사, 지도 보기, 자주 쓰는 구장 저장 제공
- 검색 API 장애·미설정 시 **수기 입력이 가능**해야 한다.
- 구장 확보 여부는 관리자가 입력하는 정보이며,
  **이 앱이 실제 예약을 완료한 것처럼 표시하지 않는다.**

### E. 참여 투표와 출석

**투표**

- 응답: **참여 / 미참여 / 미정**, 그리고 **미응답은 미정과 별도 상태**
- 마감 전 본인 응답 변경 가능, 타인 응답 변경 불가
- 기본 마감은 경기 시작 시각, 주장이 앞당길 수 있음
- 필요한 인원 대비 부족 인원 표시

**출석**

- 참여 투표와 실제 출석을 **분리**한다.
- 경기 시작 시점의 출석 대상자와 유효 응답을 보존한다.
- 참여 응답을 기반으로 출석 초안을 만들되,
  담당자가 실제 참석 여부를 수정한 뒤 확정한다.
- 서버 작업이 지연되거나 재실행돼도 같은 기준으로 복원한다.
- **수동 수정한 출석을 자동 작업이 덮어쓰지 않는다.**
- 경기+팀+선수 기준 중복을 막는다.
- 확정 버튼을 여러 번 눌러도 출석이 중복 집계되지 않는다(멱등).
- 출석 초안·예약 알림 등은 **브라우저를 켜두지 않아도** 처리돼야 한다.

**출석률 정의**

- 분모: 해당 선수가 당시 **승인된 활동 선수**였으며,
  **팀 출석 확인이 완료된 실제 완료 경기**
- 분자: 그중 **출석 확정** 경기
- 팀 출석 미확정 경기는 분모·분자에서 **함께 제외하고 별도 표시**
- 가입 전 · 탈퇴 후 · 취소 경기 제외
- **미참여 투표를 했다고 대상 경기의 분모에서 제외하지 않는다.**
- 분모가 0이면 `—` 표시
- 한 경기의 여러 쿼터는 **출석 1회**

### F. 팀 매칭과 외부 상대팀

- 승인된 활성 팀 소속 이용자에게 매칭 목록을 제공한다.
- 신청·수락·거절·취소는 **해당 팀 주장 권한**으로 처리한다.
- 날짜·지역·경기 형식·모집 상태 필터 제공
- **경기 하나에 활성 모집글 하나**
- 수락 시 양쪽 팀이 **동일한 공통 경기 ID**에 연결된다.
- 양쪽 달력에 상대팀과 경기 정보가 표시된다.
- 각 팀의 투표·출석·내부 메모는 분리한다.
- 한 상대팀 확정 후 나머지 신청은 모집 종료 처리한다.

**서버 검증 (필수)**

- 동시 수락, 자기 팀 신청, 중복 신청, 철회·만료 신청, 정지 팀, 시간 충돌
- 시간 충돌은 **취소되지 않은 경기 구간의 겹침**으로 판단한다.
  **종료와 다음 시작이 맞닿는 경우는 충돌로 보지 않는다.**
- 수락 직전에도 양 팀의 시간 충돌을 다시 확인한다.
- 모집 조건이 바뀌면 기존 신청자의 **재동의**를 받아야 한다.

**확정 후 변경**

- 확정 경기의 공통 시간·장소·비용 변경은 **상대 주장 수락 후** 반영한다.
- 시간·장소 변경 시 **이전 투표 이력을 보존하고 재투표를 요청**한다.
- 팀별 내부 메모·집합 안내는 자기 팀이 관리한다.
- 경기 취소 시 사유와 이력을 보존하고 양쪽에 표시·알림 처리한다.
- 상대팀의 연락처·참여자 명단·비공개 기록을 노출하지 않는다.

**외부(미등록) 팀**

- 미등록 외부팀은 이름을 수기로 입력할 수 있다.
- **외부팀을 위해 가짜 계정이나 승인 팀을 만들지 않는다.**
- 외부팀 경기에서도 일정·투표·출석·결과·선수 기록을 관리한다.
- 같은 이름의 팀이 나중에 가입해도 **과거 기록을 자동 연결하지 않는다.**

### G. 용병 모집과 마감

필수 범위에 포함(최신 요청).

- 모집 인원, 신청, 승인·거절, 승인 인원, 모집 상태를 관리한다.
- **승인 대기자는 모집 완료 인원에 포함하지 않는다.**
- 승인 인원이 모집 정원에 도달하면 **서버에서** 모집을 마감한다.
- 목록·상세·관련 경기 화면에 **"용병 마감"을 동일하게** 표시한다.
- 마감 후 추가 신청·정원 초과 승인을 **서버에서 차단**한다.
- **마지막 한 자리를 동시에 승인해도 정원을 초과하지 않아야 한다.**
- 새로고침·다른 계정·다른 기기에도 같은 상태가 표시돼야 한다.
- **용병 승인을 정식 팀 가입 승인과 구분**한다.
- 용병에게 필요한 경기 정보만 제공하고 팀 내부 정보는 보호한다.
- 용병 기록과 기존 팀원 기록을 중복 집계하지 않는다.
- 취소 후 재모집 정책이 기존 코드·문서에 없으면
  기본 동작과 선택 이유를 이 문서 3절에 가정으로 기록한다.

### H. 경기 결과와 선수 기록

**입력 권한과 검증**

- 선수 기록은 **주장 또는 기록 관리 권한을 받은 운영진**이 입력한다.
- 일반 팀원은 정정 요청을 할 수 있지만 확정 기록을 직접 바꾸지 않는다.
- **출석 확정 선수 기준**으로 골·어시스트를 입력한다.
- **미입력 결과와 실제 0:0을 구분**한다.
- 스코어·골·어시스트는 **0 이상의 정수**로 검증한다.
- `선수 골 합계 + 상대 자책골 + 득점자 미상 = 우리팀 스코어`
- 어시스트 없는 득점을 허용한다.
- **어시스트 합계는 상대 자책골을 제외한 우리팀 득점을 넘지 않는다.**

**결과 확정**

- 앱 내 매칭 결과는 **공통 결과 하나**를 사용한다.
- 한쪽 주장 제안 → 상대 주장 확인 → 확정
- 서로 다른 결과를 제출하면 **이견 상태로 두고 덮어쓰지 않는다.**
- 결과 확인은 **해당 결과 버전에 연결**한다.
- 외부팀 결과는 우리팀 주장이 확정하고 **"팀 자체 기록"**으로 표시한다.
- **출석 확정은 상대팀의 결과 확인을 기다리지 않는다.**
- 개인 기록 순위 반영 기준: 실제 완료 + 결과 확정 + 우리팀 개인 기록 확정
- 결과 정정 시 불일치하는 개인 기록과 집계를 재검증한다.
- 수정자·시각·사유·변경 내용을 남긴다.

**집계**

- 월간·연간·전체·기간 선택 조회
- 골·어시스트·공격포인트·출석수·출석률 정렬
- **공격포인트 = 골 + 어시스트**
- 동률은 공동 순위와 안정적인 정렬 사용
- 팀 전적은 **결과가 확정된 완료 경기** 기준
- **승률 = 승리 수 / 결과 확정 완료 경기 수**
- **한국 시간의 실제 경기 시작일**로 기간을 구분한다.
- 취소·미확정 경기를 무승부나 패배로 넣지 않는다.
- 탈퇴·이름 변경 후에도 **안정적인 선수 ID**와 과거 표시 정보를 보존한다.

### I. 공지와 실제 푸시

푸시를 필수 범위에 포함(최신 요청).

**공지**

- 팀 공지를 작성·조회·수정·고정할 수 있다.
- 팀 공지 발송은 **해당 팀 주장단만** 가능하다.
- 주장단 구성과 권한은 **서버에서** 관리하고,
  일반 팀원이 본인 프로필 수정으로 획득할 수 없게 한다.
- **다른 팀 주장단이 우리팀에 발송할 수 없어야 한다.**
- 서비스 운영자 공지는 팀 공지 권한과 별도로 관리한다.

**푸시**

- **앱 내 알림함과 휴대폰 푸시를 구분해** 구현한다.
- 공지 DB 저장과 푸시 전달 결과를 **각각** 확인할 수 있게 한다.
- 수신 동의, 기기 토큰 등록·갱신·삭제, 로그아웃·팀 탈퇴 후 처리,
  실패·재시도를 구현한다.
- 중복 요청과 재시도로 **같은 공지가 중복 발송되지 않게** 한다.
- **발송 시점에도** 수신자의 팀 권한을 확인한다.
- 푸시 클릭 후 해당 공지로 이동하되 **현재 권한을 다시 검사**한다.
- 푸시 미동의·미지원이어도 앱 내 공지는 확인할 수 있게 한다.
- **토큰·인증정보·개인정보를 로그나 인계 문서에 남기지 않는다.**
- iOS·Android의 실제 지원 경로와 필요한 계정을 확인한다.
- 기기 수신을 검증하지 못했다면 **"실기기 수신 미검증"**으로 기록한다.
- 화면 알림만 구현하고 실제 푸시 완료라고 보고하지 않는다.

**앱 내 알림 대상 이벤트**

- 새 팀·팀원 신청, 승인 결과, 일정 변경·취소, 매칭 결정,
  출석·기록 확인 요청
- ~~투표 마감 전 미응답자 알림은 **기본 24시간 전 1회**~~
  → **2026-09-15 변경: 자동 예약 대신 주장·운영진이 직접 보내는 방식으로 한다**(4-2절)
- 취소 경기·이미 응답한 사람·중복 알림 제외
- 일정이 바뀌면 **예약 알림도 갱신**한다.

### J. 이미지와 저장

- 팀 로고·선수 프로필 이미지 **실제 업로드**
- 새로고침·재로그인·다른 기기에서도 확인 가능
- 파일 종류·크기 검증과 접근 권한 적용
- 인증·업무 DB·파일 저장이 **실제로 연결**돼야 한다.
- 개인 데이터가 공용 캐시에 남지 않게 한다.
- **네트워크 저장 실패를 성공으로 표시하지 않는다.**

### K. 운영과 광고 확장

**운영**

- 서비스 운영자용 팀 승인·반려·정지·복구 화면
- 팀·회원 현황, 처리 사유, 중요한 변경 이력
- 계정 탈퇴와 주장 인계, 과거 경기 기록 처리를 일관되게 설계
- 오류 확인, 백업·복구, DB 변경·배포 방법 문서화
- **테스트 데이터와 실제 운영 데이터 분리**

**광고 (준비만)**

- 초기 서비스는 **무료 이용** 기준.
- 향후 광고를 넣을 위치와 공통 광고 영역 구성요소를 준비한다.
- 광고 활성 여부를 **설정으로 제어**할 수 있게 한다.
- 광고가 없거나 비활성이면 **빈 공간을 남기지 않는다.**
- 일정·투표·팀 관리의 주요 행동을 광고가 방해하지 않게 한다.
- **이번 작업에서 광고 SDK·추적 기능·유료 상품을 임의로 도입하지 않는다.**

---

## 3. 보류 기능과 초기 범위 밖

- 회비 결제·정산, 구장 실시간 예약
- 채팅, 영상, 프로리그 데이터, xG·히트맵
- 포메이션, 다크 모드 등 별도 요청 없는 확장

**기존에 이미 작동하는 기능은 이 목록을 이유로 삭제하지 않는다.**

---

## 4. 구현 가정 (확인 필요)

사용자 결정이 아니라 **Claude Code가 정한 가정**이다.
원본 소스 확인 후 기존 코드·문서에 다른 정책이 있으면 그쪽을 따른다.

| ID | 가정 | 이유 | 상태 |
|---|---|---|---|
| ASM-01 | 용병 승인 취소로 빈자리가 생기면 모집 상태를 자동으로 "모집 중"으로 되돌리고, 마감 시각이 지났거나 경기가 취소/완료면 되돌리지 않는다. | 요구사항 G에 재모집 정책이 없고 기존 코드·문서도 확인 불가. 정원 미달 상태를 "마감"으로 남기면 실제 운영에서 인원을 못 채운다. | 사용자 확인 필요 |
| ASM-03 | 용병 모집 열기·마감과 신청 승인·거절은 **주장과 운영진(manager)** 권한으로 한다. | 요구사항 G가 용병 승인을 팀 가입 승인과 명시적으로 구분하고, 원본 PRD에서 운영진 권한이 "자기 팀 일정 생성, 안내·출석 관리"이므로 일정 운영에 가깝다고 판단했다. 주장 전용으로 좁히는 것은 쉬우므로 되돌릴 수 있다. | **2026-09-15 사용자 확정(운영진도 가능)** |
| ASM-04 | 용병은 팀원 명단·출석·선수 통계에 넣지 않는다. | 요구사항 E의 출석률 분모는 "승인된 활동 선수"이고 용병은 여기에 해당하지 않는다. 넣으면 출석률이 왜곡된다. 요구사항 G의 "중복 집계하지 않는다"도 이 방식으로 충족된다. 용병의 경기 기록이 필요하면 별도 작업으로 다룬다. | 사용자 확인 필요 |
| ASM-05 | 소속 팀이 없는 사용자도 매칭 화면의 용병 탭에 들어갈 수 있게 했다(기존에는 팀이 없으면 온보딩 화면만 나왔다). | 용병은 보통 소속 팀이 없는 사람이라 접근을 막으면 기능이 성립하지 않는다. 이 화면에서 팀 내부 정보는 노출되지 않는다. | 구현 시 확정 |
| ASM-02 | ~~투표 마감 24시간 전 알림을 예약한다~~ → 사용자 결정으로 **수동 발송**으로 대체했다(4-2절). | — | 해소됨 |
| ASM-07 | 메일 발송은 Resend를 붙였다. 무료로 월 3,000건·하루 100건을 보낼 수 있고 REST 호출이라 Workers에서 바로 쓸 수 있다. 다른 서비스로 바꿀 수 있게 `lib/mail.ts` 한 겹으로 감쌌다. | 문자 인증은 건당 요금과 사업자 확인 절차가 있어 후순위로 두었다. | 구현 시 확정 |
| ASM-06 | 같은 경기에 참여 알림을 다시 보내려면 6시간을 기다려야 한다. | 요구사항 I의 "중복 알림 제외"를 지키면서도 마감이 다가올 때 한 번 더 부를 수 있게 하는 절충이다. | **2026-09-15 사용자 확정** |

원본 소스 확인 후 가정이 추가되면 이 표에 계속 누적한다.

---

## 4-1. 확정된 운영 결정 (2026-09-14)

### 로그인: 자체 회원가입으로 간다 (2026-09-15 구현 완료)

사용자 결정. 앱으로 배포하려면 ChatGPT 계정 전용 로그인으로는 안 된다.
공식 문서 확인 결과 **현재 호스팅(ChatGPT Sites)을 그대로 두고 가능하다.**
사이트 공개 범위를 "Anyone on the Internet"으로 두면 ChatGPT 로그인 없이 접속되고,
사이트 안에서 자체 로그인을 따로 둘 수 있다. 서버를 옮길 필요가 없다.

### 사이트 공개 전환은 개발이 끝난 뒤에 한다

공개 범위는 **보기 권한만** 정한다. 공개해도 방문자가 사이트 내용이나 코드를
수정할 수 없다. 편집은 ChatGPT 워크스페이스의 소유자·편집 권한이 있어야 한다.

다만 지금 공개할 이유가 없고, 공개하면 다음이 생긴다.

- 개발 중인 미완성 화면이 외부에 보인다
- 아무나 가입해 팀 등록을 신청할 수 있다(운영자 승인이 있어 팀 활성화는 막히지만
  가입·신청 자체는 들어온다)
- 검색엔진에 노출될 수 있다
- 실제 사용자 데이터가 테스트 데이터와 섞이기 시작한다

**따라서 자체 회원가입 구현과 실기기 확인이 끝난 뒤 마지막에 공개로 바꾼다.**

### 이미지 저장소: R2를 쓴다 (2026-09-15 구현 완료)

팀 로고·선수 프로필 사진을 올리려면 파일 보관 장소가 필요하다.

| 방법 | 판단 |
| --- | --- |
| **R2 (선택)** | 지금 쓰는 호스팅에 D1처럼 붙는다. 별도 계정·계약이 필요 없고 다운로드 트래픽 비용이 없다. |
| D1에 이미지 직접 저장 | 쓰지 않는다. DB가 커지고 느려지며 용량 한도에 금방 걸린다. |
| 외부 이미지 호스팅 | 쓰지 않는다. 접근 권한을 걸 수 없어 선수 사진이 사실상 공개된다. |
| 다른 클라우드(S3 등) | 쓰지 않는다. 별도 계정·결제와 다운로드 트래픽 요금이 붙는다. |

규모 감: 사진을 한 장 200KB로 줄여 저장하면 10GB에 약 5만 장이 들어간다.
20명 팀이면 팀당 21장이므로 2,000팀이 넘어도 여유가 있다. 초기 무료 배포 목표에 충분하다.

비용: ChatGPT Sites는 공개 베타 동안 사이트·저장소·트래픽 사용량이
**플랜 한도 안에서 포함**되며, 정확한 한도는 공개 가격표가 아니라 제품 화면에 표시된다.
따라서 별도 결제 없이 켤 수 있을 가능성이 높지만 **한도는 제품 화면에서 확인해야 한다.**

코드 쪽 준비 상태: `vite.config.ts`가 `.openai/hosting.json`의 `r2` 값으로
버킷 바인딩을 이미 배선한다(`d1: "DB"`와 같은 방식). 즉 **설정 값만 채우면 연결된다.**
업로드 기능 자체는 아직 없다.

## 4-2. 알림 방식 변경과 스케줄러 (2026-09-15)

### 24시간 전 자동 알림 → 주장·운영진의 수동 발송

사용자 결정. 정해진 시각에 자동으로 나가는 알림 대신, 참여 현황 화면에서
**미응답자에게 알림 보내기** 버튼으로 필요할 때 보낸다.

- 보내는 사람: 주장·운영진
- 받는 사람: 그 경기의 **미응답자만**. 이미 응답한 사람은 제외한다.
- 마감 뒤, 취소된 경기에는 보낼 수 없다.
- 같은 경기는 **6시간에 한 번**만 보낼 수 있다(ASM-06).

### 결과: 예약 작업(스케줄러)이 필요 없어졌다

요구사항 E의 "출석 초안·예약 알림 등은 브라우저를 켜두지 않아도 처리돼야 한다"를
다시 점검한 결과, 배경 작업이 필요한 항목이 남지 않았다.

| 항목 | 처리 방식 | 스케줄러 필요 |
| --- | --- | --- |
| 출석 초안 | 저장하지 않고 읽을 때 투표에서 계산한다(`attendanceDraft`) | 필요 없음 |
| 투표 마감 | 저장 시점에 서버 시간과 비교해 거부한다 | 필요 없음 |
| 경기 완료 | 요구사항대로 주장이 직접 확정한다 | 필요 없음 |
| 미응답자 알림 | 주장·운영진이 직접 보낸다 | 필요 없음 |

1인 운영·서버비 최소화 목표에도 맞는다. 나중에 실제 푸시(요구사항 I)를 붙일 때
예약 발송이 필요해지면 그때 다시 검토한다.

### 투표 마감 시각은 직접 정한다

경기를 만들 때와 우리 팀 경기 설정에서 모두 정할 수 있다.
지금 이후부터 경기 시작 시각까지 고를 수 있고, 비워두면 경기 시작 시각에 마감한다.
범위를 벗어나면 **조용히 무시하지 않고 이유를 알려준다.**

## 5. 최신 변경사항

| 날짜 | 변경 | 출처 |
|---|---|---|
| 2026-09-15 | 자체 회원가입 구현, ChatGPT 로그인 사용 중단 | 사용자 결정 |
| 2026-09-15 | 운영자 계정이 사라진 경우의 재설정 경로 추가 | 인증 전환의 영향 |
| 2026-09-15 | 팀 로고·선수 사진 업로드 구현, `hosting.json`에 r2 선언 | 사용자 결정 |
| 2026-09-15 | 24시간 전 자동 알림 대신 수동 발송, 투표 마감 직접 지정 | 사용자 결정 |
| 2026-09-15 | 구장명 검색(카카오 로컬) 구현, `KAKAO_REST_KEY` 필요 | 요구사항 D |
| 2026-09-15 | 가입 동의·만 14세 확인·회원 탈퇴 구현, `LEGAL.md` 추가 | 사용자 지시(법적 준수) |
| 2026-09-15 | 광고 영역 보류, 용병 권한은 운영진도 가능으로 확정 | 사용자 결정 |
| 2026-09-15 | 비밀번호 재설정·인증은 **메일 발송 서비스**로 진행하기로 결정 | 사용자 결정 |
| 2026-09-15 | 비밀번호 재설정 구현(Resend), `RESEND_API_KEY`·`MAIL_FROM` 필요 | 사용자 결정 |
| 2026-09-14 | 용병 모집·마감(G)을 필수 범위에 포함 | 사용자 최신 요청 |
| 2026-09-14 | 실제 휴대폰 푸시(I)를 필수 범위에 포함 | 사용자 최신 요청 |
| 2026-09-14 | 저장소가 비어 있음을 확인, 코드 작업 BLOCKED 처리 | 코드 확인 |

---

## 부록. REVIEW_PACKET.md 양식

Astra 검토가 필요해진 시점에만 `REVIEW_PACKET.md`를 아래 양식으로 만든다.
전체 프로젝트를 반복 전달하지 말고 관련 범위를 정확히 제시한다.
**비밀키·토큰·실사용자 개인정보는 제거한다.**
요약만으로 코드의 정상 작동이나 안전성이 입증됐다고 주장하지 않는다.

```markdown
# TEAMKICK 선택 검토 요청

## 이번 검토에서 판단할 질문
- 구체적인 질문 최대 3개

## 기준
- 작업 ID:
- 관련 요구사항:
- 기준 커밋과 변경 커밋:
- 미커밋 변경 포함 여부:

## 실제 변경
- 변경 파일과 목적:
- 판단에 필요한 실제 diff:
- 관련 서버 권한·DB 정책·마이그레이션:
- 기존 코드 중 함께 봐야 하는 부분:

## 검증 근거
- 실행 명령:
- 통과·실패 결과:
- 재현 절차:
- 기대 동작과 실제 동작:
- 미검증 범위:

## 현재 판단과 미해결 사항
- 원인 후보와 근거:
- 이미 시도한 해결:
- 검토 결과에 따라 달라질 결정:
```
