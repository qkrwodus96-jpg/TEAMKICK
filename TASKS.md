# 팀킥 작업 상태

최종 갱신: 2026-09-14

상태 표기: `UNVERIFIED`(구현 여부 미확인) / `TODO` / `IN_PROGRESS` /
`BLOCKED` / `DONE`(실제 실행 결과로 확인). 요구사항 본문은
`PROJECT_CONTEXT.md`, 구조는 `ARCHITECTURE.md`를 본다.

## 이번 세션에서 실제로 검증한 것

- [x] `T00` 원본 확보·기준 실행 — `DONE`
  - 원본 118개 파일 병합, `pnpm install` 성공
  - `node --test tests/core.test.mjs` **10/10 통과**, `tsc --noEmit` 통과
  - `pnpm lint` `no-explicit-any` **77건 오류 — 시작 시점부터 존재**(이번 작업과 무관)
  - 로컬 D1에 migration 적용 후 개발 서버·API 정상 동작 확인
- [x] `T02-1` 모바일 명단 표 레이아웃 결함 수정 — `DONE`
  - 변경 파일: `app/globals.css` (`.gap-grid>*,.form-grid>*{min-width:0}`)
  - 근거: 390px 화면에서 문서 폭이 456px로 벌어지고 표 가로 스크롤 불가 →
    수정 후 390px 유지, 표 컨테이너 316/353 스크롤 동작
  - 1440/768/390/360 폭 확인, 데스크톱·태블릿 레이아웃 변화 없음

## 원본 문서가 구현 완료로 기록한 항목 (이번 세션 재확인 결과)

아래는 원본 `TASKS.md`의 완료 목록이다. 코드와 테스트로 뒷받침되는 것은
`DONE`, 코드는 있으나 이번에 직접 실행하지 못한 것은 `UNVERIFIED`로 둔다.

| 항목 | 상태 | 근거 |
| --- | --- | --- |
| 반응형 5개 탭, 경기 상세·입력 | `DONE` | 4개 폭에서 실행 확인 |
| 서비스 운영자 팀 승인·반려·정지·복구 | `DONE` | 승인 흐름 실제 실행 |
| 주장 팀원 승인·역할 지정·인계, 다중 팀 소속 | `DONE` | 팀원 관리·제외 실행 확인 |
| 달력·목록, 주소 복사·지도 | `UNVERIFIED` | 코드 존재, 직접 조작 미실시 |
| 투표·마감, 출석 초안·확정·정정 | `DONE` | `tests/core.test.mjs` 통과 |
| 모집·신청·철회·수락, 양쪽 달력 연결 | `DONE` | 동시 수락 테스트 통과 |
| 외부 상대 수기 입력, 결과 확인·일정 변경 | `DONE` | 테스트 통과 |
| 선수 골·어시스트·출석, 기간별 요약 | `DONE` | 테스트 통과 |
| 앱 내 알림·공지·만료 초대 링크 | `UNVERIFIED` | 코드 존재, 직접 조작 미실시 |
| D1 저장, 서버 권한·동시성·중복 요청 방어 | `DONE` | 테스트 + 실제 API 확인 |
| PWA 자산과 오프라인 안내 | `UNVERIFIED` | 자산 존재, 설치·오프라인 미검증 |

## 이번 세션에서 구현한 것

- [x] `T13` **비밀번호 재설정** — `DONE` (**메일 키 설정 필요**)
  - 변경 파일: `lib/mail.ts`(신규), `lib/auth.ts`, `db/schema.ts`+`drizzle/0003`,
    `app/api/auth/route.ts`, `app/api/app/route.ts`, `app/page.tsx`,
    `app/teamkick.tsx`, `app/screens.tsx`
  - 로그인 화면에서 비밀번호 찾기 → 메일의 링크로 새 비밀번호 설정
  - 토큰은 서버에 해시만 저장, 1시간·1회용, 재설정하면 다른 기기 세션을 모두 끊는다
  - 가입 여부를 응답으로 알려주지 않고, 3분 안에 연달아 보내지 않는다
  - 메일이 설정되지 않으면 링크를 감추고 요청도 분명히 거절한다
  - 이메일 인증(실재 확인)은 아직 없다

- [x] `T12` **법적 준수 기반** — `DONE` (약관 전문가 검토는 남음)
  - 변경 파일: `lib/legal.ts`(신규), `LEGAL.md`(신규),
    `db/schema.ts`+`drizzle/0002`(동의 시각), `lib/auth.ts`, `lib/model.ts`,
    `app/api/app/route.ts`, `app/screens.tsx`
  - 가입 시 약관·개인정보 동의와 만 14세 확인을 필수로 받고 동의 시각을 저장
  - 앱에서 약관·개인정보처리방침 전문 열람
  - 회원 탈퇴: 계정·비밀번호·세션·알림 삭제, 팀에서 나간 상태로 정리,
    선수 사진 연결 해제. 과거 경기·출석 기록은 팀 공동 기록으로 보존
  - **고친 결함**: 데모 모드에서 회원 탈퇴가 샘플 데이터에만 적용되고 실제로는
    삭제되지 않았다. 기존 팀 탈퇴와 같이 데모에서는 숨긴다
  - **남은 일**: `LEGAL.md` 3절 체크리스트(변호사 검토, 문의처 기입 등)

- [x] `T03` **구장명 검색 → 주소 자동 입력** — `DONE` (**키 설정 필요**)
  - 변경 파일: `lib/places.ts`(신규), `app/api/places/route.ts`(신규),
    `lib/model.ts`(좌표 저장), `app/screens.tsx`(VenuePicker),
    `app/api/app/route.ts`(검색 사용 가능 여부), `.gitignore`(`.dev.vars`)
  - 경기 만들기·일정 변경에서 구장명으로 검색 → 후보 선택 → 이름·주소·좌표 채움
  - 같은 이름은 도로명·지번 주소로 구분, 검색 결과에 없는 주소는 만들지 않음
  - 자주 쓰는 구장: 그 팀의 지난 경기에서 최근 구장을 뽑아 바로 선택
  - 키가 없거나 검색이 실패해도 수기 입력으로 일정 등록 가능
  - **사용자 확인 필요**: `KAKAO_REST_KEY` 발급·설정

- [x] `T04` **투표 마감 지정과 참여 알림 발송** — `DONE`
  - 변경 파일: `lib/model.ts`(createGame 마감·remindVote), `app/screens.tsx`
    (경기 만들기 마감 입력, 팀 설정 마감 수정, 참여 현황 알림 버튼),
    `tests/core.test.mjs`(테스트 2건)
  - **24시간 전 자동 알림 대신 주장·운영진이 직접 보내는 방식**으로 바꿨다(사용자 결정).
    미응답자만 받고, 마감·취소된 경기는 제외, 같은 경기는 6시간에 한 번.
  - 투표 마감을 경기 만들 때와 팀 설정에서 모두 지정할 수 있다.
    범위를 벗어나면 조용히 무시하지 않고 이유를 알려준다.
  - **고친 결함**: 팀 설정에서 지난 시각을 고르면 화면은 저장됐다고 하면서
    마감이 그대로였다(클라이언트가 값을 조용히 버림). 이제 서버가 판단해 알려준다.
  - **고친 결함**: 경기 만들기에서 마감 검증이 상태 변경 뒤에 있어,
    거부돼도 경기가 남을 수 있었다. 검증을 앞으로 옮겼다.
  - 결과적으로 **예약 작업(스케줄러)이 필요 없어졌다**(`PROJECT_CONTEXT.md` 4-2절)

- [x] `T09` **팀 로고·선수 사진 업로드** — `DONE`
  - 변경 파일: `lib/images.ts`(신규), `app/api/image/route.ts`(신규),
    `lib/model.ts`(setTeamLogo·setMemberPhoto), `app/screens.tsx`(ImageField·연결),
    `app/teamkick.tsx`(Crest 로고·PlayerPhoto), `app/globals.css`,
    `.openai/hosting.json`(r2 선언)
  - 실제 업로드·표시·삭제, 종류(바이트 확인)·2MB 제한, 브라우저에서 512px로 축소,
    이전 파일 자동 정리, 팀·팀원 경로를 벗어난 키 거부
  - 접근 권한: 팀 로고는 로그인 이용자, 선수 사진은 그 팀 활동 팀원만.
    비로그인 401, 외부 계정 403 확인
  - 저장소 미연결 시에도 앱은 정상 동작하고 업로드만 분명히 거절
  - **사용자 확인 필요**: 배포 환경에서 R2가 실제로 붙는지

- [x] `T01` **자체 회원가입** — `DONE`
  - 변경 파일: `lib/auth.ts`(신규), `app/api/auth/route.ts`(신규),
    `db/schema.ts`+`drizzle/0001_dizzy_slayback.sql`(accounts·sessions),
    `app/api/app/route.ts`(신원 교체), `app/screens.tsx`(AuthPanel·로그아웃),
    `app/teamkick.tsx`(로그인 화면 연결), `lib/model.ts`(운영자 재설정)
  - 이메일+비밀번호 가입·로그인·로그아웃, PBKDF2-HMAC-SHA256 600,000회,
    HttpOnly·Secure·SameSite=Lax 세션 쿠키(서버에는 토큰 해시만 저장),
    30일 만료, 로그인 10회 실패 시 15분 잠금
  - ChatGPT 로그인은 앱에서 사용하지 않는다(`app/chatgpt-auth.ts` 미사용 표시)
  - 운영자 재설정: 저장된 운영자 ID의 계정이 없을 때만, 설정 코드를 다시 제시하면
    재설정된다. 살아 있는 운영자 계정은 코드를 알아도 가져갈 수 없다(양방향 검증)
  - 검증: 테스트 20/20, 브라우저에서 가입→세션 유지→팀 등록→로그아웃→
    잘못된 비밀번호 거부→재로그인까지 확인
  - **미검증**: 실제 운영자 초기 설정(설정 코드 원문이 없어 확인 불가)

- [x] `T02-2` **사용자 제보 원인 규명·수정** — `DONE`
  - 원인 1: `Picker`(Radix Select)가 **이미 열린 모달 안에서 마운트될 때
    `onValueChange("")`를 스스로 호출해 저장된 포지션 값을 지웠다.**
    화면에는 현재 포지션 대신 "선택"이 뜨고, 그대로 저장하면 서버가
    길이 검증으로 거부한다. `app/teamkick.tsx`의 Picker에서 빈 값을 무시하도록 수정.
  - 원인 2: **주장이 팀원의 포지션·등번호를 바꾸는 기능이 아예 없었다.**
    팀원 관리에는 운영진 지정 / 주장 인계 / 내보내기만 있었다.
    `editMember` 명령과 "선수 정보 수정" 화면 추가(주장 권한).
  - 가입 시 포지션 입력은 원래부터 동작했다(`joinTeam`).

- [x] `T06` **용병 모집·마감** — `DONE`
  - 변경 파일: `lib/model.ts`(guests 컬렉션·명령 7종·노출), `app/screens.tsx`(용병 탭·경기 상세·신청 모달), `app/teamkick.tsx`(GuestBadge·경기 목록 표시), `tests/core.test.mjs`(테스트 4건)
  - 검증: 테스트 14/14 통과, 변이 테스트로 자동 마감·중복 신청 차단·권한 검사가
    실제로 실패를 잡는 것을 확인. 브라우저에서 모집 개설 → 신청 3건 → 승인 2건 →
    자동 마감 → 목록·상세의 "용병 마감" 표시 → 마감 후 승인 요청 409 거부 확인
  - 소속 팀 없는 사용자도 용병 탭에서 신청 가능하도록 라우팅 수정(ASM-05)

## 새 요구사항 — 현재 코드에 없음 (구현 대상)

전체 검색으로 **관련 코드가 0건**임을 확인했다.


- [ ] `T08` **실제 휴대폰 푸시** — `TODO`
      `public/sw.js`에 push·notificationclick 핸들러 없음. 수신 동의·토큰
      등록/갱신/삭제·중복 발송 방지·발송 시점 권한 재확인 필요.
      iOS 지원 경로와 필요한 계정은 구현 시점 공식 문서로 확인
- [x] `T03` 구장명 검색 — **완료**(위 참고, 키 설정 필요)
- [x] `T09` 팀 로고·프로필 이미지 업로드 — **완료**(위 참고)
- [x] `T04` 예약 작업 — **필요 없음으로 정리됨**(위 참고)
- [ ] `T10` 광고 영역 준비 — **보류**(2026-09-15 사용자 결정, 나중에 진행)
- [x] `T15` **운영 데이터 백업·복원** — **완료** (2026-09-16)
      D1 이 사라지면 복구할 수단이 없어 만들었다.
      `GET /api/backup` 내려받기, `POST /api/backup` 복원. 서비스 운영자만.
      비밀번호 해시·세션/재설정/확인 토큰은 담지 않는다.
      로컬 검증: 테스트 50/50(변이 검사 6종 통과), 브라우저 14단계 확인.
      **배포본에서는 아직 확인하지 않았다.**
- [x] `T16` **오래된 기록 정리** — **완료** (2026-09-16)
      audit·notifications 에 지우는 코드가 없어 무한히 쌓이던 것을 막았다.
      `lib/model.ts` 의 `prune()`. 영수증 7일 / 운영 이력 90일 /
      읽은 알림 30일 / 안 읽은 알림 180일. 한 요청당 200건 상한.
      **경기·출석·골 기록은 지우지 않는다**(테스트로 확인).
- [x] `T17` **공개 전환 준비 — 정보 노출 막기** — **완료** (2026-09-16)
      errorHint 제거(내부 오류 원문이 사용자에게 가던 것),
      /api/health 는 운영자에게만 자세히. 공개 응답은 build·database 둘뿐.
- [x] `T18` **홈 화면 추가 안내** — **완료** (2026-09-16)
      `app/install.tsx`. 아이폰은 "공유 → 홈 화면에 추가" 안내,
      안드로이드는 `beforeinstallprompt` 를 받아 `앱 설치` 버튼.
      이미 설치됐거나 닫은 뒤에는 뜨지 않는다.
      **실기기 확인은 아직** — 헤드리스 UA 흉내로만 확인했다.
- [x] `T19` **탈퇴자 표시 이름 가리기** — **완료** (2026-09-16)
      기본은 유지(팀의 공동 기록). 삭제 요구가 오면 운영자가 표시 이름만
      `탈퇴한 선수` 로 바꾼다. 경기·출석·골 기록은 남는다.
- [x] `T21` **설치 안내 고침 + 확정 매칭 배치** — **완료** (2026-09-16)
      설치 창을 React 보다 먼저 받아 두고, 창을 주지 않는 브라우저
      (삼성 인터넷 등)에는 손 동작 안내를 띄운다. 사용자가 갤럭시에서
      안내를 못 찾은 문제의 원인이었다.
      확정 매칭 탭을 모집 중 탭과 같은 카드 모양으로 맞췄다.
- [x] `T22` **로고 교체 + 첫 화면(스플래시)** — **완료** (2026-09-16)
      로고는 사용자가 보낸 그림을 보고 SVG 로 다시 그렸다(원본과 정확히 같지 않음).
      스플래시는 2초, `sessionStorage` 로 한 번만. 누르면 넘어간다.
      **광고 자리를 위쪽에 미리 잡아 두었다** — `data-ad="true"` 를 주면
      로고가 아래로 내려가고 광고 자리가 펼쳐진다(T10 광고 영역과 이어짐).
- [x] `T23` **MY 화면** — **완료** (2026-09-16)
      `우리팀` → `MY`. 내 전체 기록(전체 팀 합산), 프로필 관리,
      1:1 문의(운영자 답변), 서비스 공지(운영자 작성), 버전 표기.
      새 컬렉션 `inquiries`·`announcements` 추가. 팀이 없어도 보인다.
- [x] `T24` **문의 보유 기간** — **완료** (2026-09-16, 사용자 결정 1년)
      `KEEP.inquiry=365`. 답변 대기 중인 문의는 지우지 않는다.
      탈퇴하면 그 사람 문의는 함께 지운다.
- [x] `T25` **첫 화면 번쩍임 고침** — **완료** (2026-09-16)
      첫 화면을 React 밖으로 빼서 HTML 에 직접 넣었다. 첫 페인트부터 보인다.
      사용자 제보: "로딩화면 전에 팀킥 UI 가 먼저 살짝 보임".
- [x] `T26` **팀 정보(형식·실력·활동 때) 수정** — **완료** (2026-09-16)
      만들 때만 정할 수 있고 못 고치던 것을 `팀 정보 수정` 에서 고칠 수 있게 했다.
      `level` 은 매칭 화면에 보이면서도 어디에서도 정할 수 없던 값이었다.
      목록(`FORMATS`·`LEVELS`)으로 서버에서 검증한다.
- [x] `T27` 팀원이 팀 정보 수정을 요청 — **필요 없음으로 정리**(2026-09-16 사용자 결정)
- [x] `T28` **주로 뛰는 때 목록화 + 상관없음** — **완료** (2026-09-16)
- [x] `T29` **샘플 팀이 잠깐 보이던 것 고침** — **완료** (2026-09-16)
      `demo` 가 true 로 시작해, 불러오는 동안 로그인한 사람에게도 샘플이 보였다.
- [x] `T30` **역할 권한 확인** — 이미 되어 있었다. 테스트로 굳혔다.
      운영진은 경기 만들기·참여 알림 가능, 결과 확정·일정 변경은 주장만.
- [ ] `T31` **웹 푸시 알림** — `TODO` (사용자 요청 2026-09-16, 다음 작업)
- [ ] `T32` **개인정보 처리방침 보완** — `TODO`
      공식 작성지침 2025.4 와 대조해 빠진 항목을 `LEGAL.md` 2-3 에 적었다.
      국외 이전·파기 절차·쿠키·보호책임자·변경 고지가 빠져 있다.
- [ ] `T20` **구글·네이버 로그인 추가 후 이메일 가입 제거** — `TODO`
      (2026-09-16 사용자 결정) 네이버 메일 도달 문제를 우회하는 방법이기도 하다.
      구글·네이버 개발자 콘솔에서 클라이언트 ID·시크릿을 받아야 시작할 수 있다.
      기존 이메일 계정의 로그인은 유지한다.

## 다음 구현 대상

- [ ] `T14` **이메일 인증(가입 시 실재 확인)** — `TODO` (같은 메일 키 사용)

## 사용자 결정이 필요해 멈춘 항목

- [x] `T01` 일반 회원 직접 가입 — **완료**(위 참고)
- [ ] `T02-2` **사용자 제보 재현** — `BLOCKED`
      "내 선수 정보 수정에서 주장만 선택되고 다른 선택이 되지 않는다"를
      주장·일반 팀원 두 시점에서 조작했으나 재현되지 않았다.
      `HANDOFF.md`의 질문 참고.

## 배포·초기 운영 (원본 문서에서 이어받음)

- [x] 최종 빌드 및 비공개 배포 준비 (실제 배포 상태는 Sites 기준으로 확인)
- [ ] 소유자 로그인 후 운영자 초기 설정 — 일회용 코드 원문 필요(저장소에 없음)
- [ ] 첫 실제 팀 등록·승인·경기 입력
- [ ] 초대 전 사이트 접근 범위 확대
- [ ] 실제 팀원 계정으로 가입·투표 확인

## 후속

- [ ] 모바일 실기기·브라우저 시각·E2E 검증 (이번엔 헤드리스 Chromium만 사용)
- [ ] 반복 일정·게스트 선수·내보내기
- [ ] 팀 수 증가에 따른 DB 조회·이력 보관 개선
- [ ] 운영자 복구·백업 절차 구체화
- [ ] 스토어 배포·외부 로그인 검토
