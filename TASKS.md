# TASKS.md — TEAMKICK 작업 목록

최종 갱신: 2026-09-14

## 상태 정의

| 상태 | 의미 |
|---|---|
| `UNVERIFIED` | 구현 여부를 아직 확인하지 못함. **미구현으로 단정하지 않음** |
| `TODO` | 확인 결과 작업이 필요함 |
| `IN_PROGRESS` | 진행 중 |
| `BLOCKED` | 외부 입력·계정·결정이 없어 진행 불가 |
| `DONE` | 완료 조건을 **실제 실행 결과로** 확인함 |

## 현황 요약

| ID | 작업 | 상태 | 선행 |
|---|---|---|---|
| T-DOC | 개발 기준·인계 문서 작성 | `DONE` | — |
| T00 | 원본 확인과 실행 기준 확보 | **`BLOCKED`** | — |
| T01 | 구조와 권한·데이터 기반 | `UNVERIFIED` | T00 |
| T02 | 팀원 관리와 내 선수 정보 | `UNVERIFIED` | T00, T01 |
| T03 | 일정과 구장 주소 | `UNVERIFIED` | T00, T01 |
| T04 | 투표·출석 | `UNVERIFIED` | T00, T01, T03 |
| T05 | 팀 매칭 | `UNVERIFIED` | T00, T01, T03 |
| T06 | 용병 모집 | `UNVERIFIED` | T00, T01, T03 |
| T07 | 경기 결과와 통계 | `UNVERIFIED` | T00, T04, T05 |
| T08 | 공지와 알림·푸시 | `UNVERIFIED` | T00, T01 |
| T09 | 이미지·모바일 통합 검증 | `UNVERIFIED` | T00, T01 |
| T10 | 운영과 광고 확장 | `UNVERIFIED` | T00, T01 |
| T11 | 출시 준비 상태 정리 | `UNVERIFIED` | T00 |

**T00이 BLOCKED이므로 T01~T11은 착수할 수 없다.**
아래 순서는 기본안이며, 원본 확인 후 실제 코드 상태와 의존성에 따라
이 문서에서 조정할 수 있다. 기존 구현은 먼저 검증하고 필요한 부분만 수정한다.

---

## T-DOC. 개발 기준·인계 문서 작성 — `DONE`

- **완료 조건**: `CLAUDE.md`, `PROJECT_CONTEXT.md`, `ARCHITECTURE.md`,
  `TASKS.md`, `HANDOFF.md`가 존재하고 역할이 중복되지 않는다.
- **변경 파일**: `CLAUDE.md`, `PROJECT_CONTEXT.md`, `ARCHITECTURE.md`,
  `TASKS.md`, `HANDOFF.md`
- **검증 근거**: 파일 생성 확인, 커밋·푸시 완료.
  `REVIEW_PACKET.md`는 Astra 검토가 필요한 시점에만 작성한다(양식은
  `PROJECT_CONTEXT.md` 부록).
- **다음 행동**: 없음.

---

## T00. 원본 확인과 실행 기준 확보 — `BLOCKED`

- **완료 조건**
  1. TEAMKICK 원본 소스가 저장소에 존재한다.
  2. 설치·실행·빌드 명령을 **실제로 실행**해 결과를 기록했다.
  3. 시작 시점부터 있던 오류 목록을 기록했다.
  4. 기능별 상태를 작동 확인 / 코드만 존재 / 데모 데이터 / 미검증 / 미구현으로 구분했다.
- **현재 상태 (2026-09-14 확인)**
  - `/home/user/TEAMKICK`에는 `README.md`(10바이트) 1개와 초기 커밋 1개뿐.
  - 미커밋 변경 없음. `.openai/hosting.json`, `package.json`, 잠금 파일,
    앱 진입점, 서버·DB·인증·업로드 구성 모두 없음.
  - 접근 가능한 저장소는 `qkrwodus96-jpg/TEAMKICK` 하나이고 위와 같이 비어 있다.
  - **현재 폴더는 TEAMKICK 원본이 아니다.**
- **원인**: 원본 소스(ChatGPT Sites 저장 버전 v1, 73개 파일 보관본)가
  이 환경에서 접근 불가.
- **시도한 해결**: 작업 폴더 전체 탐색, git 전 브랜치 파일 목록 확인,
  계정에서 접근 가능한 저장소 목록 확인.
- **필요한 입력**: 아래 중 하나로 원본 소스를 전달 — `HANDOFF.md` 참고.
- **다음 행동**: 사용자가 소스를 전달하면 위 완료 조건 1~4를 수행하고
  `ARCHITECTURE.md` 1·2절을 실제 확인 결과로 덮어쓴다.

> 확보되지 않은 원본을 대신해 새 더미 앱을 만들지 않는다.
> 배포 성공이나 프로젝트 ID를 원본 확보의 근거로 쓰지 않는다.

---

## T01. 구조와 권한·데이터 기반 — `UNVERIFIED`

- **선행**: T00
- **완료 조건**
  - 실제 인증·DB·팀 소속·역할·운영자 계정 구조를 확인하고 필요한 부분을 보완했다.
  - 다음이 **서버에서** 차단됨을 실행으로 확인: 다른 팀 데이터 접근,
    본인 권한 상승, 가입 대기자의 팀 내부 접근(URL·API 직접 호출 포함).
  - 운영자 권한을 일반 가입·첫 가입자·클라이언트 값·프로필 수정으로
    얻을 수 없음을 확인.
  - 역할 변경·팀원 제외 후 **기존 세션에도 현재 권한이 적용**됨을 확인.
  - 관리자 비밀값이 코드에 하드코딩되어 있지 않음을 확인.
- **관련 요구사항**: `PROJECT_CONTEXT.md` B, 7절 검증 기준
- **변경 파일**: 미정 (T00 이후)
- **검증 근거**: 없음
- **다음 행동**: T00 완료 후 `ARCHITECTURE.md` 2절 체크리스트 4·5·6번부터 확인.

---

## T02. 팀원 관리와 내 선수 정보 — `UNVERIFIED`

- **선행**: T00, T01
- **완료 조건**
  - 사용자 제보("내 선수 정보 수정에서 주장만 선택되고 다른 선택이 되지 않는다")를
    **재현**하고 원인을 규명한 뒤 수정했다.
  - 선수 포지션·프로필과 팀 운영 역할이 분리되어 있고,
    본인 프로필 수정으로 주장·운영자 권한을 얻을 수 없다.
  - 승인된 팀원이 전체 선수 명단을 볼 수 있고,
    현재 팀원 / 가입 대기 / 과거 선수가 구분된다.
  - 팀원 제외와 서비스 계정 삭제가 구분되고,
    제외 후에도 과거 경기·출석·득점 기록이 보존된다.
- **관련 요구사항**: `PROJECT_CONTEXT.md` C
- **검증 근거**: 없음
- **다음 행동**: T00 완료 후 해당 화면과 API를 먼저 재현 시도.

---

## T03. 일정과 구장 주소 — `UNVERIFIED`

- **선행**: T00, T01
- **완료 조건**
  - 월간 달력 / 날짜별 목록 / 오늘 이동 / 같은 날 여러 경기 동작 확인.
  - 구장명 검색 → 후보 선택 → 주소 자동 입력 → 저장이 실제로 동작.
  - 동명 장소를 지역·주소로 구분해 선택 가능.
  - 주소 복사·지도 보기·자주 쓰는 구장 저장 동작.
  - **검색 API 미설정·장애 시에도 수기 입력으로 저장 가능.**
  - 일정 저장 후 **재로그인해 일시·장소가 유지됨**을 확인.
  - 자정을 넘는 경기 저장·표시 확인.
  - 시간 경과만으로 경기가 자동 완료되지 않음을 확인.
- **관련 요구사항**: `PROJECT_CONTEXT.md` D
- **외부 의존성**: 장소 검색 API 키 (미정, 사용자 계정 필요할 수 있음)
- **검증 근거**: 없음

---

## T04. 투표·출석 — `UNVERIFIED`

- **선행**: T00, T01, T03
- **완료 조건**
  - 참여 / 미참여 / 미정 / **미응답** 네 상태가 구분된다.
  - 마감 전 본인 응답 변경 가능, 타인 응답 변경 불가(서버 차단).
  - 기본 마감 = 경기 시작 시각, 주장이 앞당길 수 있음.
  - 출석 초안 생성 → 담당자 수정 → 확정 흐름 동작.
  - **자동 작업이 수동 수정 출석을 덮어쓰지 않음**을 확인.
  - 확정 재실행·중복 클릭에도 출석이 중복 집계되지 않음(`(경기,팀,선수)` 고유).
  - 출석률 분모·분자가 정의대로 계산됨.
    - 예: 대상 확정 4경기 중 출석 3회 → **75%**
    - 팀 출석 미확정 경기는 분모·분자에서 함께 제외하고 별도 표시
    - 미참여 투표를 이유로 분모에서 빼지 않음
    - 분모 0이면 `—`
    - 한 경기의 여러 쿼터는 출석 1회
  - 브라우저를 켜두지 않아도 출석 초안이 처리됨(서버 스케줄러).
- **관련 요구사항**: `PROJECT_CONTEXT.md` E
- **검증 근거**: 없음

---

## T05. 팀 매칭 — `UNVERIFIED`

- **선행**: T00, T01, T03
- **완료 조건**
  - 수락 시 양쪽 달력에 **공통 경기 ID** 하나로 연결되어 표시됨.
  - 각 팀의 투표·출석·내부 메모가 분리됨.
  - **동시 수락 시 한 상대팀만 확정**되고 나머지는 모집 종료(서버 검증).
  - 자기 팀 신청, 중복 신청, 철회·만료 신청, 정지 팀, 시간 충돌 차단.
  - 시간 충돌은 겹침으로 판정하고 **맞닿는 경우는 충돌 아님**.
  - 수락 직전 양 팀 시간 충돌 재확인.
  - 모집 조건 변경 시 기존 신청자 재동의.
  - 확정 후 공통 시간·장소·비용 변경은 상대 주장 수락 후 반영,
    이전 투표 이력 보존 + 재투표 요청.
  - 상대팀 연락처·참여자 명단·비공개 기록 비노출.
  - 외부팀은 이름 수기 입력으로 처리되고 가짜 계정이 생기지 않음.
- **관련 요구사항**: `PROJECT_CONTEXT.md` F
- **검증 근거**: 없음

---

## T06. 용병 모집 — `UNVERIFIED`

- **선행**: T00, T01, T03
- **완료 조건**
  - 신청 → 승인·거절 → 정원 도달 → **서버에서 마감** 흐름 동작.
  - 승인 대기자가 완료 인원에 포함되지 않음.
  - 목록·상세·관련 경기 화면에 **"용병 마감"이 동일하게** 표시됨.
  - **마지막 한 자리 동시 승인 시 정원 초과 없음**(실제 동시 요청으로 검증).
  - 마감 후 추가 신청·정원 초과 승인이 서버에서 차단됨.
  - 새로고침·다른 계정·다른 기기에서 같은 상태 표시.
  - 용병 승인과 정식 팀 가입 승인이 구분됨.
  - 용병에게 팀 내부 정보가 노출되지 않음.
  - 용병 기록과 팀원 기록이 중복 집계되지 않음.
  - 취소 후 재모집 정책 확정 (`PROJECT_CONTEXT.md` ASM-01 참고).
- **관련 요구사항**: `PROJECT_CONTEXT.md` G
- **검증 근거**: 없음

---

## T07. 경기 결과와 통계 — `UNVERIFIED`

- **선행**: T00, T04, T05
- **완료 조건**
  - 주장/기록 권한자만 입력, 일반 팀원은 정정 요청만 가능.
  - 출석 확정 선수 기준으로 골·어시스트 입력.
  - **미입력 결과와 실제 0:0 구분.**
  - 검증식 동작: `선수 골 합계 + 상대 자책골 + 득점자 미상 = 우리팀 스코어`,
    어시스트 합계 ≤ (우리팀 득점 − 상대 자책골), 모든 값 0 이상 정수.
  - 양 팀 결과 제안 → 확인 → 확정, **불일치 시 이견 상태로 덮어쓰지 않음**.
  - 결과 확인이 해당 결과 버전에 연결됨.
  - 외부팀 결과는 "팀 자체 기록"으로 표시.
  - **출석 확정이 상대팀 결과 확인을 기다리지 않음.**
  - 집계 검증
    - 승률 = 승리 수 / 결과 확정 완료 경기 수
    - 예: 완료 4경기 중 결과 확정 3경기, 2승 1무 → **66.7%**
    - 공격포인트 = 골 + 어시스트, 동률은 공동 순위 + 안정 정렬
    - 취소·미확정 경기를 무승부·패배로 넣지 않음
    - 기간 구분은 **한국 시간 실제 경기 시작일** 기준
    - 월말·연말·자정 경계, 과거 기록 정정 후 재집계 확인
  - 정정 시 수정자·시각·사유·변경 내용 기록.
  - 탈퇴·개명 후에도 선수 ID와 과거 표시 정보 보존.
- **관련 요구사항**: `PROJECT_CONTEXT.md` H
- **검증 근거**: 없음

---

## T08. 공지와 알림·푸시 — `UNVERIFIED`

- **선행**: T00, T01
- **완료 조건**
  - 팀 공지 작성·조회·수정·고정 동작, **주장단만 발송 가능**.
  - **다른 팀 주장단의 우리팀 발송 차단**(서버).
  - 서비스 운영자 공지가 팀 공지 권한과 분리됨.
  - 앱 내 알림함 저장과 푸시 전달 결과를 각각 확인 가능.
  - 수신 동의, 토큰 등록·갱신·삭제, 로그아웃·팀 탈퇴 후 처리, 실패·재시도.
  - **중복 요청·재시도로 같은 공지가 중복 발송되지 않음.**
  - 발송 시점 권한 재확인, **탈퇴자 수신 제외**.
  - 푸시 클릭 → 해당 공지 이동 + 권한 재검사.
  - 푸시 미동의·미지원이어도 앱 내 공지 확인 가능.
  - 투표 마감 24시간 전 미응답자 알림 1회, 취소 경기·응답자·중복 제외,
    일정 변경 시 예약 갱신.
  - **수신 가능한 테스트 기기에서 실제 수신과 공지 이동 확인.**
    확인하지 못하면 "실기기 수신 미검증"으로 기록.
- **관련 요구사항**: `PROJECT_CONTEXT.md` I
- **외부 의존성**: 푸시 서비스 계정·키, iOS는 Apple 개발자 계정 (미확인)
- **주의**: 실사용자에게 테스트 푸시를 발송하지 않는다.
  토큰·인증정보를 로그·문서에 남기지 않는다.
- **검증 근거**: 없음

---

## T09. 이미지·모바일 통합 검증 — `UNVERIFIED`

- **선행**: T00, T01
- **완료 조건**
  - 팀 로고·프로필 이미지 **실제 업로드** 후 새로고침·재로그인·
    다른 기기에서 확인됨.
  - 파일 종류·크기 검증과 접근 권한 적용, 저장 실패를 성공으로 표시하지 않음.
  - 개인 데이터가 공용 캐시에 남지 않음.
  - iPhone·Android 주요 흐름과 작은 화면 확인
    (글자·버튼·키보드·스크롤·하단 여백).
  - 로딩 / 빈 상태 / 승인 대기 / 오류 / 권한 없음 상태 제공.
  - **실기기 확인 범위와 미검증 범위를 명시.**
- **관련 요구사항**: `PROJECT_CONTEXT.md` A, J
- **검증 근거**: 없음

---

## T10. 운영과 광고 확장 — `UNVERIFIED`

- **선행**: T00, T01
- **완료 조건**
  - 운영자용 팀 승인·반려·정지·복구 화면 동작.
  - 팀·회원 현황, 처리 사유, 중요한 변경 이력 확인 가능.
  - 계정 탈퇴·주장 인계·과거 기록 처리 일관성 확인.
  - 오류 확인, 백업·복구, DB 변경·배포 방법 문서화.
  - 테스트 데이터와 운영 데이터 분리.
  - 공통 광고 영역 구성요소 준비 + 설정으로 활성/비활성 제어.
  - **비활성 시 빈 공간이 남지 않음**을 화면에서 확인.
  - 광고가 일정·투표·팀 관리 주요 행동을 방해하지 않음.
  - **광고 SDK·추적 기능·유료 상품은 도입하지 않음.**
- **관련 요구사항**: `PROJECT_CONTEXT.md` K
- **검증 근거**: 없음

---

## T11. 출시 준비 상태 정리 — `UNVERIFIED`

- **선행**: T00 (실질적으로 T01~T10 이후)
- **완료 조건**
  - 미완 기능과 외부 설정 의존성 정리.
  - **웹 실행 / 모바일 사용 / 네이티브 빌드 / 스토어 제출 준비를 구분**해 기록.
  - 사용자 권한 범위 안에서 배포하고 실제 결과를 확인.
- **관련 요구사항**: `PROJECT_CONTEXT.md` A, K
- **검증 근거**: 없음
