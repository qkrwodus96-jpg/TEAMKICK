# 팀킥 작업 상태

최종 갱신: 2026-09-24

상태 표기: `UNVERIFIED`(구현 여부 미확인) / `TODO` / `IN_PROGRESS` /
`BLOCKED` / `DONE`(실제 실행 결과로 확인). 요구사항 본문은
`PROJECT_CONTEXT.md`, 구조는 `ARCHITECTURE.md`를 본다.

## T73 탈퇴 정리 (2026-09-25, 1.10.3)

- [x] `T73-1` 탈퇴 시 카카오·구글·네이버 연결 끊기(다시 로그인해 받은 토큰으로). 로컬 검증 완료, **외부 연동 미검증**.
- [x] `T73-2` 백업 복원이 탈퇴자를 되살리지 않게(`closed_accounts`, 1년). 테스트 2개.
- [x] `T73-3` 처리방침 국외 이전 보유 기간 구체화, 3·4·10항 갱신, 적용일 2026-09-25.
- [ ] `T73-4` 운영에서 사업자별 새 테스트 계정으로 탈퇴 → "연결도 끊었어요" 확인 — `TODO`(사장님).
- [ ] `T73-5` Brevo 발송 기록 보관 기간 1개월 설정 — `TODO`(사장님).

## T72 아이콘 통일 · 공지 접기 · 등번호 표기 (2026-09-24, 1.10.0)

- [x] `T72-1` **앱 아이콘 통일**(manifest `any`·`maskable` 같은 그림, 새 파일 이름). 테스트: any-512 == maskable-512.
      배포 뒤 홈 화면 아이콘 재추가로 확인 필요(`UNVERIFIED`).
- [x] `T72-2` **팀 공지 최근 3개 + "이전 공지 더 보기"**(홈·MY).
- [x] `T72-3` **"이름#번호" → "이름 N번"**, 번호 없으면 "등번호 미정".
- [x] `T72-4` **알림 안내 문구**: 닫힌 크롬이 안 깨는 폰 → 삼성 인터넷 권장, 숫자는 크롬 "앱 설치" 아이콘에만.
- [ ] `T72-5` **taste 스킬 부분 적용** — `TODO` — **나중에 하기로 함**(2026-09-24). 할 때 색 방향(참고 앱 또는 색 비교 페이지)부터 정하고 범위 A/B/C.
- [x] `T72-7` **설치 앱 시작 화면 글자** — manifest `name` "팀킥". 시작 화면 자체는 안드로이드가 그려서 못 끈다.
- [ ] `T72-8` **설치 앱에서 우리 2초 첫 화면 빼기** — `TODO`(사장님 결정: 광고 자리 유지 vs 한 번만 보이기).
- [x] `T72-9` 팀원 목록 등번호 0 → "—", 이달의 골잡이 "NO. 0" → "등번호 미정".
- [x] `T72-10` 백업 내려받기 진행·완료·실패 표시(로컬 검증 완료).
- [x] `T72-11` 처리방침·약관 웹 주소 `/privacy` `/terms`(로컬 검증 완료, 배포 후 확인 필요).
- [ ] `T72-6` **알림 badge 전용 단색 아이콘** — `TODO`(선택).

## T71 실제 알림 즉시 발송 · 알림 한 통의 길 기록 (2026-09-24, 1.9.7)

- [x] `T71-1` **실제 알림을 끝까지 보낸 뒤 응답**(`waitUntil` 넘기기 삭제). 크롬 "3분 뒤 도착" 의 원인으로 판단.
      **검증**: 로컬 workerd 두 계정 — 저장 응답 전에 발송 기록 완료. 돌연변이(기다리지 않기) 잡힘.
- [x] `T71-2` **앱을 열 때마다 구독을 다시 올린다**(`KeepSubscription`).
- [x] `T71-3` **기기별 알림 기록 ①~④**(DB 열 6개, 서비스 워커 수신 신호, MY 화면 표시, 걸린 시간).
      **검증**: 테스트 3개 + 실제 크롬 CDP 푸시로 수신 기록까지. 남의 기록 못 읽음.
- [x] `T71-4` **주장 토스트에 푸시 발송 성공·실패 통수**.
- [x] `T71-5` **처리방침·LEGAL.md** 반영, 적용일 2026-09-24.
- [x] `T71-6` **운영에서 한 번 시험** — 서버 즉시 발송·구글 201 확인. 크롬을 **열어 두면 2초**, 닫아 두면
      열 때까지 안 옴(외부 연동 검증 완료, 사장님 캡처).
- [x] `T71-8` **닫힌 브라우저가 알림을 못 받는 원인** — 재시험 결과: "제한 없음" 이어도 닫힌 크롬은 안 깬다,
      닫힌 삼성 인터넷은 바로 받는다(외부 연동 검증 완료). 코드 문제 아님 → 안내 문구로 처리(T72-4).
- [x] `T71-7` **안드로이드 아이콘 숫자** — 크롬 "앱 설치" 로 깔면 붙는다(사장님 캡처로 확인). TWA 불필요.

## T70 실제 알림 회귀 수정 · 팀 찾기 로고 (2026-09-24, 1.9.6)

- [x] `T70-1` **실제 알림이 0ms 제한 시간으로 끊기던 회귀**(1.9.3 에서 Claude 가 만듦).
      **검증**: signal 을 지키는 가짜 fetch 로 재현 → 수정 → 통과, 되돌리면 실패.
- [x] `T70-2` **푸시를 1.5초까지는 응답 전에 끝낸다**. **검증**: 단위 테스트.
- [x] `T70-3` **토스트 문구** "폰 알림 켠 팀원 N명".
- [x] `T70-4` **가입 전 팀 찾기 로고**. **검증**: 테스트 + 가입 안 한 계정으로 화면 확인.
- [ ] `T70-5` **두 번째 계정으로 실제 공지 알림 재시험** — 배포 뒤 사장님.
- [ ] `T70-6` **크롬 앱 설치 아이콘 숫자** — 알림이 실제로 간 뒤 다시 확인.

## T69 갤럭시 푸시 해결 뒤 정리 (2026-09-24, 1.9.5)

- [x] `T69-0` **갤럭시 푸시 도착** — **외부 연동 검증 완료**(사장님 실기기). 원인: 삼성 인터넷
      "웹사이트 알림 자동 차단".
- [x] `T69-1` **받은 사람 수·폰 알림 켠 사람 수** 를 저장 응답과 토스트로. 숫자만, 이름 없음.
      **검증**: 테스트 2개(경로 통과·실제 등록으로 세기).
- [x] `T69-2` **브라우저별 "알림이 안 와요?" 안내**. **검증**: 삼성 인터넷·크롬 UA 로 화면 확인.
- [ ] `T69-3` **실제 공지·경기 알림** — 두 번째 계정으로 확인 필요(본인에게는 안 간다).
- [ ] `T69-4` **아이폰 푸시·아이콘 숫자** — 아이폰 실기기 필요.
- [ ] `T69-5` **갤럭시 아이콘 숫자** — 크롬 "앱 설치" 로 설치했을 때 뜨는지 실기기 확인.

## T68 구글→폰 배달 구간 좁히기 (2026-09-24, 1.9.4)

- [x] `T68-1` **`Urgency: high`** — 절전 중 FCM 지연 방지. **검증**: 단위 테스트(돌연변이 잡힘).
- [x] `T68-2` **시험을 이 기기로만** — 화면이 구독을 다시 올리고 그 주소로만 보낸다. 남의 주소 거절.
      **검증**: 단위 테스트(돌연변이 잡힘).
- [x] `T68-3` **폰이 받았는지 화면에 표시** — 서비스 워커 → 화면 메시지.
      **검증**: 실제 크롬에서 CDP 로 푸시를 넣어 알림 표시와 메시지 도착 확인.
- [x] `T68-4` **"이 폰에서 바로 띄워보기"** 단추. **미검증**: 헤드리스에선 이 단추가 보이는
      상태(구독 켜짐)를 만들 수 없다.
- [ ] `T68-5` **사장님 3차 시험** — `BLOCKED`. 바로 띄워보기 결과 + 시험 뒤 📱 줄 유무.

## T67 푸시 시간 초과 좁히기 · 투표 마감 수정 (2026-09-24, 1.9.3)

- [x] `T67-1` **구글 우회 주소** — `*.google.com/fcm/send/` 에 닿지 못하면
      `fcm.googleapis.com` 으로 한 번 더. **검증**: 단위 테스트(돌연변이 4건 모두 잡힘).
      **미검증**: 운영에서 실제로 닿는지.
- [x] `T67-2` **연결 확인(probe)·걸린 시간** 을 시험 결과에 붙인다.
- [x] `T67-3` **푸시를 저장 뒤에서 보낸다**(`waitUntil`, 없으면 예전처럼 기다림).
      **검증**: 단위 테스트 + 로컬 workerd 에서 서버가 정상 기동·저장 45ms.
- [x] `T67-4` **투표 마감 수정 단추** — 경기 정보의 투표 마감 줄 옆.
      **검증**: 로컬 화면에서 눌러 "팀 안내 · 투표 마감 수정" 창이 열리는 것 확인.
- [ ] `T67-5` **운영 시험 결과 받기** — `BLOCKED`(배포 뒤 사장님 시험 필요).

## T66 끝난 경기 표시 · 푸시 자동 재등록 (2026-09-20, 1.9.2)

- [x] `T66-1` **끝난 경기의 용병 모집을 닫는다** — `completeGame` 이 모집과 대기 신청을
      닫는다(승인된 용병은 유지). 화면에서도 끝난 경기에는 용병 배지를 붙이지 않는다.
      **검증**: 테스트 124/124 통과, 돌연변이 1건 잡힘, 일정 목록에서 확인.
- [x] `T66-2` **"경기 종료" 표시** — 종료 시각이 지난 예정 경기는 "경기 종료 · 기록 전",
      완료 처리된 경기는 "경기 종료". 점수가 확정돼도 상태 배지를 함께 보여준다.
      **검증**: 일정 목록에서 지난 경기 두 줄 모두 "경기 종료 · 기록 전" 확인.
- [x] `T66-3` **푸시 구독 자동 재등록** — 구독의 공개키가 서버 키와 다르면 조용히 다시
      등록한다. 키를 나중에 넣은 기기는 "켜짐" 인데 알림만 안 오는 상태였다.
      **미검증**: 실제 푸시 도착(외부 접속 차단).
- [ ] `T66-4` **갤럭시에서 푸시가 오지 않는 원인** — `BLOCKED`.
      필요한 입력: 시험 알림 뒤 화면에 남는 "시험 결과" 줄.
      함께 볼 것: 설정 → 앱 → 팀킥 → 알림 / 배터리 백그라운드 사용 제한.

## T65 1.9.0 배포 뒤 제보 (2026-09-19, 1.9.1)

- [x] `T65-1` **모집 상태를 경기 정보에 한 줄로** — 주장 전용 단추로만 알 수 있던 것을
      누구나 보게 했다. 상대팀이 정해지면 "상대팀 모집 마감 · 매칭 확정(팀이름)".
      **검증**: 모집 중 경기와 매칭 확정 경기 둘 다 화면에서 확인.
- [x] `T65-2` **투표 마감을 투표 상자 안에** — 홈에서 투표할 때도 보인다.
      **검증**: 홈 투표 상자에 "투표 마감 9월 22일 (화) 10:00" 확인.
- [x] `T65-3` **푸시 시험 결과를 화면에 남긴다** — 토스트만 띄워 원인을 물어볼 수
      없었다. 푸시 서버 주소도 함께 보여준다. 아이폰 안내 추가.
      **미검증**: 실제 푸시 도착. 이 환경은 외부 접속이 막혀 있다.
- [ ] `T65-4` **푸시가 실제로 오는지** — `BLOCKED`. 사장님이 시험 알림을 누른 뒤
      화면에 남는 "시험 결과" 줄이 필요하다.

## T64 사용자 요청 5가지 (2026-09-19, 1.9.0) — `DONE`(로컬 검증)

- [x] `T64-1` **경기 기록 입력 순서 뒤집기** — 새 명령 `matchRecord`.
      선수 골·도움 → 우리 팀 점수 **자동 합산** → 상대팀 득점만 손으로.
      점수 먼저 확정해야 하던 제약과 "합이 같아야 함" 오류가 사라졌다.
      결과 확정 시 개인 기록 확정을 무조건 풀던 것도 고쳤다(`clearStaleRecords`).
      **검증**: 테스트 3개 추가(123/123 통과), 돌연변이 2건 모두 잡힘,
      브라우저에서 저장까지 확인(3:2 확정 · 선수 2골 · 자책 1).
- [x] `T64-2` **고른 팀 유지** — `localStorage.teamkick_team`.
      **검증**: 팀 바꾸기 → 새로고침 → 탭 5개 순회까지 그대로 유지됨.
      권한은 서버가 다시 확인한다(내 팀이 아니면 `active[0]` 로 되돌아간다).
- [x] `T64-3` **"모집 마감" → "상대팀 모집 마감"**. **검증**: 단추 이름 확인.
- [x] `T64-4` **홈 카드 좌우 화살표** — 지난 경기·다음 경기 넘겨 보기.
      다른 화면에 갔다 홈으로 오면 다시 다음 경기.
      **검증**: 1/5 → 5/5 이동, 홈 복귀 시 1/5 로 복귀 확인.
- [x] `T64-5` **매칭 탭 한 줄 고정** — 줄바꿈 대신 가로 스크롤(`.tab-scroll`).
      확정 매칭 목록은 다가오는 경기 먼저.
      **검증**: 390px 에서 탭 5개가 모두 같은 줄(y=378)에 놓인 것 확인.
- [ ] `T64-6` **실기기 확인** — 배포 후 사장님 폰에서.

## T20 검토 후 보완 (2026-09-19)

- [x] 신규 이메일 가입 정책 분리: 명시적 `EMAIL_SIGNUP_ENABLED=true`만 허용, 기본 차단.
- [x] 비밀번호 재설정·이메일 확인의 토큰 동시 사용 차단 및 중간 실패 롤백.
- [x] 재설정 직접 진입·발송 실패 재시도·local 계정만 메일 복구.
- [x] 백업 형식 2에서 소셜 식별자 보존, 누락된 옛 백업 사전 거부.
- [x] 실제 인증·백업 함수 검증 포함 **116/116 통과**, 타입 검사 통과.
- [ ] 실제 새 소셜 가입 완료·실메일 수신은 실계정 확인 필요.

## 이번 세션에서 실제로 검증한 것

- [x] `T00` 원본 확보·기준 실행 — `DONE`
  - 원본 118개 파일 병합, `pnpm install` 성공
  - `node --test tests/core.test.mjs` **10/10 통과**, `tsc --noEmit` 통과
  - `pnpm lint` `no-explicit-any` **77건 오류 — 시작 시점부터 존재**(이번 작업과 무관)
  - 로컬 D1에 migration 적용 후 개발 서버·API 정상 동작 확인
- [x] `T02-1` 모바일 명단 표 레이아웃 결함 수정 — `DONE`
  - 변경 파일: `app/globals.css` (`.gap-grid>*,.form-grid>*{min-width:0}`)
  - 근거: 390px 화면에서 문서 폭이 456px로 벌어지고 표 가로 스크롤 불가 →
    수정 후 390px 유지, 표 컨테이너 316/353 스크롤 동작
  - 1440/768/390/360 폭 확인, 데스크톱·태블릿 레이아웃 변화 없음

## 원본 문서가 구현 완료로 기록한 항목 (이번 세션 재확인 결과)

아래는 원본 `TASKS.md`의 완료 목록이다. 코드와 테스트로 뒷받침되는 것은
`DONE`, 코드는 있으나 이번에 직접 실행하지 못한 것은 `UNVERIFIED`로 둔다.

| 항목 | 상태 | 근거 |
| --- | --- | --- |
| 반응형 5개 탭, 경기 상세·입력 | `DONE` | 4개 폭에서 실행 확인 |
| 서비스 운영자 팀 승인·반려·정지·복구 | `DONE` | 승인 흐름 실제 실행 |
| 주장 팀원 승인·역할 지정·인계, 다중 팀 소속 | `DONE` | 팀원 관리·제외 실행 확인 |
| 달력·목록, 주소 복사·지도 | `DONE` | 2026-09-18 브라우저에서 직접 조작 확인 |
| 투표·마감, 출석 초안·확정·정정 | `DONE` | `tests/core.test.mjs` 통과 |
| 모집·신청·철회·수락, 양쪽 달력 연결 | `DONE` | 동시 수락 테스트 통과 |
| 외부 상대 수기 입력, 결과 확인·일정 변경 | `DONE` | 테스트 통과 |
| 선수 골·어시스트·출석, 기간별 요약 | `DONE` | 테스트 통과 |
| 앱 내 알림·공지·만료 초대 링크 | `DONE` | 2026-09-18 브라우저에서 직접 조작 확인 |
| D1 저장, 서버 권한·동시성·중복 요청 방어 | `DONE` | 테스트 + 실제 API 확인 |
| PWA 자산과 오프라인 안내 | `DONE`(오프라인) / `미검증`(실기기 설치) | 2026-09-18 워커 등록·오프라인 화면 확인, 실기기 설치는 남음 |

## 이번 세션에서 구현한 것

- [x] `T13` **비밀번호 재설정** — `DONE` (**메일 키 설정 필요**)
  - 변경 파일: `lib/mail.ts`(신규), `lib/auth.ts`, `db/schema.ts`+`drizzle/0003`,
    `app/api/auth/route.ts`, `app/api/app/route.ts`, `app/page.tsx`,
    `app/teamkick.tsx`, `app/screens.tsx`
  - 로그인 화면에서 비밀번호 찾기 → 메일의 링크로 새 비밀번호 설정
  - 토큰은 서버에 해시만 저장, 1시간·1회용, 재설정하면 다른 기기 세션을 모두 끊는다
  - 가입 여부를 응답으로 알려주지 않고, 3분 안에 연달아 보내지 않는다
  - 메일이 설정되지 않으면 링크를 감추고 요청도 분명히 거절한다
  - 이메일 인증(실재 확인)은 T14 에서 넣었다(2026-09-15). 위 문장은 T14 이전 기준이다.

- [x] `T12` **법적 준수 기반** — `DONE` (약관 전문가 검토는 남음)
  - 변경 파일: `lib/legal.ts`(신규), `LEGAL.md`(신규),
    `db/schema.ts`+`drizzle/0002`(동의 시각), `lib/auth.ts`, `lib/model.ts`,
    `app/api/app/route.ts`, `app/screens.tsx`
  - 가입 시 약관·개인정보 동의와 만 14세 확인을 필수로 받고 동의 시각을 저장
  - 앱에서 약관·개인정보처리방침 전문 열람
  - 회원 탈퇴: 계정·비밀번호·세션·알림 삭제, 팀에서 나간 상태로 정리,
    선수 사진 연결 해제. 과거 경기·출석 기록은 팀 공동 기록으로 보존
  - **고친 결함**: 데모 모드에서 회원 탈퇴가 샘플 데이터에만 적용되고 실제로는
    삭제되지 않았다. 기존 팀 탈퇴와 같이 데모에서는 숨긴다
  - **남은 일**: `LEGAL.md` 3절 체크리스트(변호사 검토, 문의처 기입 등)

- [x] `T03` **구장명 검색 → 주소 자동 입력** — `DONE` (**키 설정 필요**)
  - 변경 파일: `lib/places.ts`(신규), `app/api/places/route.ts`(신규),
    `lib/model.ts`(좌표 저장), `app/screens.tsx`(VenuePicker),
    `app/api/app/route.ts`(검색 사용 가능 여부), `.gitignore`(`.dev.vars`)
  - 경기 만들기·일정 변경에서 구장명으로 검색 → 후보 선택 → 이름·주소·좌표 채움
  - 같은 이름은 도로명·지번 주소로 구분, 검색 결과에 없는 주소는 만들지 않음
  - 자주 쓰는 구장: 그 팀의 지난 경기에서 최근 구장을 뽑아 바로 선택
  - 키가 없거나 검색이 실패해도 수기 입력으로 일정 등록 가능
  - **사용자 확인 필요**: `KAKAO_REST_KEY` 발급·설정

- [x] `T04` **투표 마감 지정과 참여 알림 발송** — `DONE`
  - 변경 파일: `lib/model.ts`(createGame 마감·remindVote), `app/screens.tsx`
    (경기 만들기 마감 입력, 팀 설정 마감 수정, 참여 현황 알림 버튼),
    `tests/core.test.mjs`(테스트 2건)
  - **24시간 전 자동 알림 대신 주장·운영진이 직접 보내는 방식**으로 바꿨다(사용자 결정).
    미응답자만 받고, 마감·취소된 경기는 제외, 같은 경기는 6시간에 한 번.
  - 투표 마감을 경기 만들 때와 팀 설정에서 모두 지정할 수 있다.
    범위를 벗어나면 조용히 무시하지 않고 이유를 알려준다.
  - **고친 결함**: 팀 설정에서 지난 시각을 고르면 화면은 저장됐다고 하면서
    마감이 그대로였다(클라이언트가 값을 조용히 버림). 이제 서버가 판단해 알려준다.
  - **고친 결함**: 경기 만들기에서 마감 검증이 상태 변경 뒤에 있어,
    거부돼도 경기가 남을 수 있었다. 검증을 앞으로 옮겼다.
  - 결과적으로 **예약 작업(스케줄러)이 필요 없어졌다**(`PROJECT_CONTEXT.md` 4-2절)

- [x] `T09` **팀 로고·선수 사진 업로드** — `DONE`
  - 변경 파일: `lib/images.ts`(신규), `app/api/image/route.ts`(신규),
    `lib/model.ts`(setTeamLogo·setMemberPhoto), `app/screens.tsx`(ImageField·연결),
    `app/teamkick.tsx`(Crest 로고·PlayerPhoto), `app/globals.css`,
    `.openai/hosting.json`(r2 선언)
  - 실제 업로드·표시·삭제, 종류(바이트 확인)·2MB 제한, 브라우저에서 512px로 축소,
    이전 파일 자동 정리, 팀·팀원 경로를 벗어난 키 거부
  - 접근 권한: 팀 로고는 로그인 이용자, 선수 사진은 그 팀 활동 팀원만.
    비로그인 401, 외부 계정 403 확인
  - 저장소 미연결 시에도 앱은 정상 동작하고 업로드만 분명히 거절
  - **사용자 확인 필요**: 배포 환경에서 R2가 실제로 붙는지

- [x] `T01` **자체 회원가입** — `DONE`
  - 변경 파일: `lib/auth.ts`(신규), `app/api/auth/route.ts`(신규),
    `db/schema.ts`+`drizzle/0001_dizzy_slayback.sql`(accounts·sessions),
    `app/api/app/route.ts`(신원 교체), `app/screens.tsx`(AuthPanel·로그아웃),
    `app/teamkick.tsx`(로그인 화면 연결), `lib/model.ts`(운영자 재설정)
  - 이메일+비밀번호 가입·로그인·로그아웃, PBKDF2-HMAC-SHA256 600,000회,
    HttpOnly·Secure·SameSite=Lax 세션 쿠키(서버에는 토큰 해시만 저장),
    30일 만료, 로그인 10회 실패 시 15분 잠금
  - ChatGPT 로그인은 앱에서 사용하지 않는다(`app/chatgpt-auth.ts` 미사용 표시)
  - 운영자 재설정: 저장된 운영자 ID의 계정이 없을 때만, 설정 코드를 다시 제시하면
    재설정된다. 살아 있는 운영자 계정은 코드를 알아도 가져갈 수 없다(양방향 검증)
  - 검증: 테스트 20/20, 브라우저에서 가입→세션 유지→팀 등록→로그아웃→
    잘못된 비밀번호 거부→재로그인까지 확인
  - **미검증**: 실제 운영자 초기 설정(설정 코드 원문이 없어 확인 불가)

- [x] `T02-2` **사용자 제보 원인 규명·수정** — `DONE`
  - 원인 1: `Picker`(Radix Select)가 **이미 열린 모달 안에서 마운트될 때
    `onValueChange("")`를 스스로 호출해 저장된 포지션 값을 지웠다.**
    화면에는 현재 포지션 대신 "선택"이 뜨고, 그대로 저장하면 서버가
    길이 검증으로 거부한다. `app/teamkick.tsx`의 Picker에서 빈 값을 무시하도록 수정.
  - 원인 2: **주장이 팀원의 포지션·등번호를 바꾸는 기능이 아예 없었다.**
    팀원 관리에는 운영진 지정 / 주장 인계 / 내보내기만 있었다.
    `editMember` 명령과 "선수 정보 수정" 화면 추가(주장 권한).
  - 가입 시 포지션 입력은 원래부터 동작했다(`joinTeam`).

- [x] `T06` **용병 모집·마감** — `DONE`
  - 변경 파일: `lib/model.ts`(guests 컬렉션·명령 7종·노출), `app/screens.tsx`(용병 탭·경기 상세·신청 모달), `app/teamkick.tsx`(GuestBadge·경기 목록 표시), `tests/core.test.mjs`(테스트 4건)
  - 검증: 테스트 14/14 통과, 변이 테스트로 자동 마감·중복 신청 차단·권한 검사가
    실제로 실패를 잡는 것을 확인. 브라우저에서 모집 개설 → 신청 3건 → 승인 2건 →
    자동 마감 → 목록·상세의 "용병 마감" 표시 → 마감 후 승인 요청 409 거부 확인
  - 소속 팀 없는 사용자도 용병 탭에서 신청 가능하도록 라우팅 수정(ASM-05)

## 새 요구사항 — 현재 코드에 없음 (구현 대상)

전체 검색으로 **관련 코드가 0건**임을 확인했다.


- [x] `T08` **실제 휴대폰 푸시** — `T31` 로 구현함 (2026-09-16)
- [x] `T03` 구장명 검색 — **완료**(위 참고, 키 설정 필요)
- [x] `T09` 팀 로고·프로필 이미지 업로드 — **완료**(위 참고)
- [x] `T04` 예약 작업 — **필요 없음으로 정리됨**(위 참고)
- [ ] `T10` 광고 영역 준비 — **보류**(2026-09-15 사용자 결정, 나중에 진행)
- [x] `T15` **운영 데이터 백업·복원** — **완료** (2026-09-16)
      D1 이 사라지면 복구할 수단이 없어 만들었다.
      `GET /api/backup` 내려받기, `POST /api/backup` 복원. 서비스 운영자만.
      비밀번호 해시·세션/재설정/확인 토큰은 담지 않는다.
      로컬 검증: 테스트 50/50(변이 검사 6종 통과), 브라우저 14단계 확인.
      **배포본에서는 아직 확인하지 않았다.**
- [x] `T16` **오래된 기록 정리** — **완료** (2026-09-16)
      audit·notifications 에 지우는 코드가 없어 무한히 쌓이던 것을 막았다.
      `lib/model.ts` 의 `prune()`. 영수증 7일 / 운영 이력 90일 /
      읽은 알림 30일 / 안 읽은 알림 180일. 한 요청당 200건 상한.
      **경기·출석·골 기록은 지우지 않는다**(테스트로 확인).
- [x] `T17` **공개 전환 준비 — 정보 노출 막기** — **완료** (2026-09-16)
      errorHint 제거(내부 오류 원문이 사용자에게 가던 것),
      /api/health 는 운영자에게만 자세히. 공개 응답은 build·database 둘뿐.
- [x] `T18` **홈 화면 추가 안내** — **완료** (2026-09-16)
      `app/install.tsx`. 아이폰은 "공유 → 홈 화면에 추가" 안내,
      안드로이드는 `beforeinstallprompt` 를 받아 `앱 설치` 버튼.
      이미 설치됐거나 닫은 뒤에는 뜨지 않는다.
      **실기기 확인은 아직** — 헤드리스 UA 흉내로만 확인했다.
- [x] `T19` **탈퇴자 표시 이름 가리기** — **완료** (2026-09-16)
      기본은 유지(팀의 공동 기록). 삭제 요구가 오면 운영자가 표시 이름만
      `탈퇴한 선수` 로 바꾼다. 경기·출석·골 기록은 남는다.
- [x] `T21` **설치 안내 고침 + 확정 매칭 배치** — **완료** (2026-09-16)
      설치 창을 React 보다 먼저 받아 두고, 창을 주지 않는 브라우저
      (삼성 인터넷 등)에는 손 동작 안내를 띄운다. 사용자가 갤럭시에서
      안내를 못 찾은 문제의 원인이었다.
      확정 매칭 탭을 모집 중 탭과 같은 카드 모양으로 맞췄다.
- [x] `T22` **로고 교체 + 첫 화면(스플래시)** — **완료** (2026-09-16)
      로고는 사용자가 보낸 그림을 보고 SVG 로 다시 그렸다(원본과 정확히 같지 않음).
      스플래시는 2초, `sessionStorage` 로 한 번만. 누르면 넘어간다.
      **광고 자리를 위쪽에 미리 잡아 두었다** — `data-ad="true"` 를 주면
      로고가 아래로 내려가고 광고 자리가 펼쳐진다(T10 광고 영역과 이어짐).
- [x] `T23` **MY 화면** — **완료** (2026-09-16)
      `우리팀` → `MY`. 내 전체 기록(전체 팀 합산), 프로필 관리,
      1:1 문의(운영자 답변), 서비스 공지(운영자 작성), 버전 표기.
      새 컬렉션 `inquiries`·`announcements` 추가. 팀이 없어도 보인다.
- [x] `T24` **문의 보유 기간** — **완료** (2026-09-16, 사용자 결정 1년)
      `KEEP.inquiry=365`. 답변 대기 중인 문의는 지우지 않는다.
      탈퇴하면 그 사람 문의는 함께 지운다.
- [x] `T25` **첫 화면 번쩍임 고침** — **완료** (2026-09-16)
      첫 화면을 React 밖으로 빼서 HTML 에 직접 넣었다. 첫 페인트부터 보인다.
      사용자 제보: "로딩화면 전에 팀킥 UI 가 먼저 살짝 보임".
- [x] `T26` **팀 정보(형식·실력·활동 때) 수정** — **완료** (2026-09-16)
      만들 때만 정할 수 있고 못 고치던 것을 `팀 정보 수정` 에서 고칠 수 있게 했다.
      `level` 은 매칭 화면에 보이면서도 어디에서도 정할 수 없던 값이었다.
      목록(`FORMATS`·`LEVELS`)으로 서버에서 검증한다.
- [x] `T27` 팀원이 팀 정보 수정을 요청 — **필요 없음으로 정리**(2026-09-16 사용자 결정)
- [x] `T28` **주로 뛰는 때 목록화 + 상관없음** — **완료** (2026-09-16)
- [x] `T29` **샘플 팀이 잠깐 보이던 것 고침** — **완료** (2026-09-16)
      `demo` 가 true 로 시작해, 불러오는 동안 로그인한 사람에게도 샘플이 보였다.
- [x] `T30` **역할 권한 확인** — 이미 되어 있었다. 테스트로 굳혔다.
      운영진은 경기 만들기·참여 알림 가능, 결과 확정·일정 변경은 주장만.
- [x] `T31` **기기 푸시 알림** — **코드 작성 완료 / 로컬 검증 완료 / 실전달 미검증**
      (2026-09-16) 내용을 싣지 않는 푸시. 서비스 워커가 깨어나 직접 읽어온다.
      **실제 전달은 확인하지 못했다** — 이 환경은 푸시 서버에 닿을 수 없고
      크롬은 시크릿 모드에서 Push API 를 막는다. 실기기 확인 필요.
- [x] `T32` **개인정보 처리방침 보완** — **완료** (2026-09-17)
      빠져 있던 6개를 채웠다(파기 절차·국외 이전·쿠키·보호책임자·구제방법·변경).
      위탁 업체를 실명으로 바꿨다. **보호책임자 성명은 사용자 결정 대기.**
      **변호사 검토는 그대로 남아 있다.**
- [x] `T33` **팀 찾기 개선** — **완료** (2026-09-17)
      대소문자를 가려 `oz` 로 `FCOZ` 를 못 찾던 것을 고쳤다. 로고·지역·형식·실력 표시.
- [x] `T34` **경기 날짜 편의** — **완료** (2026-09-17)
      요일 표시, 시작을 정하면 종료가 2시간 뒤로 자동.
- [x] `T32-2` **개인정보 보호책임자 성명 기재** — **완료** (2026-09-17)
      사용자 확인을 받아 `박재연 (서비스 운영자)` 로 넣었다.
- [x] `T35` **구글·네이버 로그인 추가** — **코드 작성 완료 / 로컬 검증 완료 / 실제 로그인 미검증**
      (2026-09-17) `lib/social.ts`·`lib/social-route.ts` 로 제공자 설정을 한곳에 모았다.
      `/api/google`, `/api/naver` 가 같은 흐름을 쓴다. 계정은 `(provider, provider_id)`
      로 묶고, 카카오만 있던 시절의 `kakao_id` 는 앱이 뜰 때 한 번 옮긴다.
      구글에는 이메일을 요청하지 않는다(`scope: openid profile`).
      **이 환경은 구글·네이버에 닿을 수 없어 실제 로그인은 확인하지 못했다.**
      배포 뒤 확인이 필요하다.
- [x] `T36` **첫 화면 로딩 대기 단축** — **되돌림** (2026-09-17, 사용자 결정)
      2초를 그대로 유지한다. 나중에 이 자리에 광고가 들어간다.
      대신 그 과정에서 찾은 버그를 고쳤다: 눌러서 건너뛰면 첫 화면이 지워졌다가
      React 가 붙으면서 되살아나 결국 2초를 채우고 있었다.
- [x] `T38` **저장 왕복 절반으로** — **완료 / 로컬 검증 완료** (2026-09-17)
      "웹 안에서 로딩이 길다"는 제보의 원인. 저장 응답에 이미 새 상태가 들어 있는데
      화면이 그것을 버리고 전체를 한 번 더 읽고 있었다. 저장 44ms → 22ms.
      팀 로고 보관 기간도 5분 → 7일(주소가 바뀌면 새로 받으므로 낡을 일이 없다).
      선수 사진은 팀을 나가면 보이면 안 되므로 5분 그대로 둔다.
- [x] `T37` **앱 안 로고 교체** — **완료** (2026-09-17)
      머리말의 글자 `K` 대신 팀킥 로고(`BrandMark`)를 쓴다.

- [x] `T39` **배포 확인 수단 보완** — **완료** (2026-09-17)
      `/api/health` 가 `version` 을 주지 않아 배포 확인이 반쪽이었다. 넣었다.
      `googleReady`·`naverReady` 도 운영자에게만 보이도록 넣었다.

- [x] `T40` **머리말 로고 잘림·앱 아이콘** — **완료** (2026-09-17)
      기울인 글자가 왼쪽으로 밀리는 것을 고려하지 않아 로고 왼쪽 58px 이 잘려 있었다.
      로고 옆 글자는 사용자 요청대로 지웠다. 안드로이드 홈 화면용 maskable 아이콘 추가.
- [x] `T41` **이미지 업로드** — 사용자 확인 결과 **이미 잘 되고 있었다**(FCOZ 로고·선수 사진).
      내가 질문을 잘못 읽어 엉뚱한 답을 했다. 실제 질문은 "보내준 로고 파일을 쓸 수 있나" 였다.
- [x] `T42` **팀 로고 동그라미 + 앱 안에서 크기 맞추기** —
      **코드 작성 완료 / 로컬 검증 완료(브라우저 포함)** (2026-09-18)
      깃발 모양 clip-path 를 정원으로 바꾸고, 올릴 때 끌어 옮기고 크기를 맞추는
      `ImageCropper` 를 넣었다.
      **브라우저 확인 결과**: 900x300 가로로 긴 그림으로 1280px·390px 두 폭에서 확인했다.
      자르기 화면이 뜨고, 끌면 옮겨지고(가로 100px → 정확히 100px), 범위를 넘기면
      한계값에서 멈추고(720px 그림 · 240px 틀 → 한계 240), 세로는 딱 맞아 움직이지 않는다.
      막대로 1~3배까지 커진다. 저장한 파일은 512x512 PNG 이고 **투명한 픽셀이 0개**라
      빈 곳이 생기지 않는다. 화면에서 보던 부분(왼쪽 빨강)이 그대로 저장됐다.
      머리말 로고는 66x66(휴대폰 62x62) `border-radius:50%`, `clip-path:none`.

- [x] `T44` **알림 문구가 UTC 원문·영문 상태값을 그대로 보여주던 것** — **로컬 검증 완료** (2026-09-18)
      위 브라우저 확인 중에 발견했다. 알림함에
      `난지천공원 · 2026-09-22T01:00:00.000Z`, `크롭시험FC · active` 가 그대로 떴다.
      `lib/model.ts` 에 `seoulStamp`(Intl 없이 +9시간으로 계산)과 한국어 상태 표를 넣었다.
      정지·반려는 사유도 함께 알려준다. 날짜·해가 넘어가는 자리까지 테스트로 고정했다.
      **이미 쌓인 알림의 글은 바뀌지 않는다**(기록이라 소급해 고치지 않는다).

- [x] `T45` **재방문마다 뜨던 hydration 오류** — **로컬 검증 완료** (2026-09-18)
      첫 화면 스크립트가 React 보다 먼저 `<html>` 에 `data-splash="skip"` 를 붙이는데
      서버 HTML 에는 없어서, **이미 본 사람의 재방문마다** 콘솔 오류가 하나씩 떴다.
      `<html>` 의 속성 비교만 껐다(`suppressHydrationWarning`). 첫 화면 동작은 그대로다
      (첫 방문 2초 노출 → 재방문 건너뜀 확인).

- [x] `T46` **모달 부제가 전부 "우리 팀의 정보를 확인하고 관리하세요."** — **로컬 검증 완료** (2026-09-18)
      알림함·공지·문의처럼 팀 정보와 상관없는 창에도 같은 문장이 붙어 있었다.
      해당 창 12개만 제 설명을 갖게 했고 나머지는 기존 문구 그대로다.
- [x] `T43`/`T47` **로고 원본 파일 적용** — **로컬 검증 완료** (2026-09-18)
      사용자가 `public/TEAMKICK_fullname_black_512.png` 를 올려줬다. 앱 아이콘 3종·
      첫 화면·머리말·탭 아이콘이 모두 이 파일 하나를 쓴다. 코드로 다시 그리던 SVG 는
      지웠다. maskable 아이콘은 안전 원(204.8) 안에 들어가는 것을 브라우저에서 다시 쟀다
      (가장 먼 픽셀 196.4).

- [x] `T48` **소셜 로그인 단추가 서로 붙어 있었다** (제보 1) — **로컬 검증 완료**
      세로 간격 0px → 10px. 확인함.
- [x] `T49` **팀 공지가 오래된 것부터 나왔다** (제보 2) — **로컬 검증 완료**
      정렬이 아예 없었다. 고정 먼저, 그다음 최신순. 테스트로 고정.
- [x] `T50` **팀 공지 기기 푸시** (제보 3) — **이미 동작 중이었고 문구가 틀렸다**
      공지를 올리면 자동으로 푸시가 간다(따로 보내는 단추는 없다). 기기 알림 안내와
      공지 작성 창 안내를 실제 동작에 맞게 고쳤다.
- [x] `T51` **앱 설치 단추가 "설치할 수 없다"고 했다** (제보 4) — **코드만 고침**
      이미 설치된 상태를 기억하지 않아 계속 권했고, `prompt()` 재호출 예외를 아무도
      받지 않았다. 둘 다 고쳤다. **실제 설치 창은 헤드리스에서 확인 불가 — 미검증.**
- [x] `T52` **매칭 검색 줄** (제보 5·7) — **로컬 검증 완료**
      글자 잘림(칸 136px), 지역 분리 없음, 검색 단추 없음, 정렬 없음, 주소 검색 안 됨을
      모두 고쳤다. 가까운 경기부터 정렬되는 것과 주소 검색을 확인함.
- [x] `T53` **투표 마감에 요일이 없었다** (제보 6) — **로컬 검증 완료**
- [ ] `T54` **10월 10일 모집글이 안 보였다** (제보 8) — `원인 미확정 · 사용자 확인 대기`
      같은 절차로 재현되지 않았다. 정렬 없음·주소 검색 안 됨은 T52 에서 고쳤다.
      남은 가능성은 `상대팀을 모집할게요` 미체크. `HANDOFF.md` 의 질문 참고.


- [x] `T55` **로고를 누르면 홈으로** — **로컬 검증 완료** (2026-09-18)
- [x] `T56` **알림 개수를 숫자로 · 무슨 알림인지 한 줄로 · 홈 화면 아이콘 개수**
      — **로컬 검증 완료**(아이콘 개수는 실기기 미검증)
- [x] `T57` **알림을 누르면 그 화면으로 이동** — **로컬 검증 완료**
      알림에 갈 화면(`to`)을 저장한다. 매칭·홈·일정 이동을 브라우저에서 확인함.
- [x] `T58` **잠금화면 알림이 가장 오래된 소식을 띄우던 결함** — **코드만 고침**
      서비스 워커가 `unread[0]`(가장 오래된 것)을 최근으로 쓰고 있었다.
      실기기 푸시는 이 환경에서 확인 불가 — **미검증**.
- [x] `T59` **승인된 용병을 참여 인원에 포함** — **로컬 검증 완료**
      팀원 명단·출석·선수 통계에는 넣지 않는다(ASM-04 유지). 테스트로 고정.
- [x] `T60` **공지 알림 보내기 단추** — **로컬 검증 완료**
      주장만, 6시간에 한 번, 글쓴이 제외. 테스트로 고정.

- [x] `T20` **이메일 가입 제거** — **로컬 검증 완료** (2026-09-18, 사용자 지시)
      새 가입은 카카오·네이버·구글로만 받는다. **서버(`POST /api/auth`)가 403 으로 막는다** —
      화면을 우회한 직접 호출도 막히는 것을 확인했다.
      **소셜 키가 하나도 없으면 막지 않는다** (키를 넣기 전 배포·로컬에서 아무도 가입 못 하고
      운영자 초기 설정조차 못 하게 되는 것을 막기 위해서다).
      기존 이메일 계정의 로그인·비밀번호 찾기·이메일 확인은 그대로다. 마이그레이션 없음.
      `LEGAL.md` 갱신함(수집 항목이 늘지 않고 줄어든다).
      **미검증**: 배포에서 새 계정의 소셜 가입이 실제로 되는지(이 환경은 외부 API 차단).

- [x] `T61` **소셜 단추 간격·높이 균일** — **로컬 검증 완료** (2026-09-18)
      간격 10→14px, 높이 42/42/43 → 48/48/48.

- [ ] `T62` **GPT(Astra) 검토 답 반영** — `검토 대기`
      `REVIEW_PACKET.md` 참고. 질문 3개(차단 조건의 환경변수 의존, 잠기는 경로,
      소셜·이메일 계정 갈라짐). 답에 따라 `SIGNUP_MODE` 스위치 도입 여부를 정한다.

- [x] `T63` **사용자 제보 7가지** — **로컬 검증 완료** (2026-09-19)
      투표 마감 표시 / D-day 한국 날짜 / 용병 알림 탭 이동 / 경기 시작·종료 요일 /
      "우리 팀 매칭 내역" + 확정 시각 / 용병 검색란 / 기기 알림 권유 띠.
      셋은 **원인이 짐작과 달랐다**: 투표 마감은 저장이 잘 됐고(볼 곳이 없었다),
      매칭 내역은 이미 팀원에게 보였고(이름만 개인 것처럼 읽혔다),
      푸시는 고장이 아니라 **본인에게는 안 가는 설계**였다.

## 다음 구현 대상

- [x] `T14` **이메일 인증(가입 시 실재 확인)** — **완료** (2026-09-15)
      `TODO` 로 남아 있었으나 실제로는 그날 구현·검증이 끝나 있었다(`HANDOFF.md` T14 절).
      migration `0005`, 확인 전 팀 만들기·가입 차단, 24시간·1회용 토큰.
      **미검증**: 실제 메일 도착(외부 API 차단).

## 사용자 결정이 필요해 멈춘 항목

- [x] `T01` 일반 회원 직접 가입 — **완료**(위 참고)
- [ ] `T02-2` **사용자 제보 재현** — `BLOCKED`
      "내 선수 정보 수정에서 주장만 선택되고 다른 선택이 되지 않는다"를
      주장·일반 팀원 두 시점에서 조작했으나 재현되지 않았다.
      `HANDOFF.md`의 질문 참고.

## 배포·초기 운영 (원본 문서에서 이어받음)

- [x] 최종 빌드 및 비공개 배포 준비 (실제 배포 상태는 Sites 기준으로 확인)
- [ ] 소유자 로그인 후 운영자 초기 설정 — 일회용 코드 원문 필요(저장소에 없음)
- [ ] 첫 실제 팀 등록·승인·경기 입력
- [ ] 초대 전 사이트 접근 범위 확대
- [ ] 실제 팀원 계정으로 가입·투표 확인

## 후속

- [ ] 모바일 실기기·브라우저 시각·E2E 검증 (이번엔 헤드리스 Chromium만 사용)
- [ ] 반복 일정·게스트 선수·내보내기
- [ ] 팀 수 증가에 따른 DB 조회·이력 보관 개선
- [ ] 운영자 복구·백업 절차 구체화
- [ ] 스토어 배포·외부 로그인 검토
