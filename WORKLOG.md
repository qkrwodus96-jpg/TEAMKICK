# TEAMKICK 작업 이력

세션마다 무엇을 바꾸고 무엇을 결정했는지 한눈에 보기 위한 기록이다.
자세한 근거와 검증 결과는 각 커밋 메시지에 있고, 현재 상태와 다음 행동은
`HANDOFF.md`, 작업 목록은 `TASKS.md`에 있다.

이 파일은 **GitHub에 저장되므로 다른 도구(GPT 등)와 오가며 작업해도
여기까지의 경위를 그대로 이어받을 수 있다.** 대화 원문은 저장되지 않는다.

---

## 2026-09-15

| 커밋 | 내용 |
| --- | --- |
| `dfb20aa` | 투표 마감 직접 지정, 미응답자 참여 알림 발송 |
| `7a6de32` | 팀 로고·선수 사진 업로드 (R2) |
| `6149469` | 자체 회원가입 로그인으로 전환 |
| `0bbd036` | 로그인·공개 범위·이미지 저장소 결정 문서화 |

**결정**

- 로그인은 **자체 회원가입(이메일+비밀번호)**으로 간다. 앱 배포를 위해서다.
  ChatGPT Sites를 그대로 두고 가능하며 서버를 옮길 필요가 없다.
- 이미지 저장소는 **R2**를 쓴다. 지금 호스팅에 붙고 다운로드 요금이 없다.
- 사이트 공개 전환은 **개발이 끝난 뒤 마지막에** 한다.
- 투표 마감 전 24시간 자동 알림 대신 **주장·운영진이 직접 발송**한다.
  같은 경기는 6시간에 한 번(ASM-06, 사용자 확정).
- 선수 사진은 **그 팀 활동 팀원에게만** 보인다(팀 로고는 로그인 이용자 전체).

**고친 결함**

- 팀 설정에서 지난 시각을 고르면 저장됐다고 하면서 마감이 그대로였다.
- 경기 만들기의 마감 검증이 상태 변경 뒤에 있어 거부돼도 경기가 남을 수 있었다.

## 2026-09-14

| 커밋 | 내용 |
| --- | --- |
| `987cf40` | 포지션 선택값이 지워지던 문제, 주장의 선수 정보 수정 기능 |
| `f763116` | 용병 모집·승인·자동 마감 |
| `8afd8e6` | 모바일에서 명단 표가 화면 밖으로 밀리던 문제 |
| `48b361e` | 원본 소스(118개 파일) 병합 |

**결정**

- 용병 모집·승인 권한을 주장뿐 아니라 **운영진에게도** 열었다(ASM-03, 확인 대기).
- 용병은 팀원 명단·출석·선수 통계에 넣지 않는다(ASM-04).
- 소속 팀이 없는 사용자도 매칭 화면 용병 탭에 들어갈 수 있게 했다(ASM-05).

**규명한 원인**

- "내 선수 정보에서 포지션이 안 바뀐다" → Radix Select가 이미 열린 모달 안에서
  마운트될 때 스스로 빈 값 콜백을 호출해 저장된 값을 지우고 있었다.
